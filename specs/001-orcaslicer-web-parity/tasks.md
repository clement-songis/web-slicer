# Tasks: Web-Slicer — parité OrcaSlicer multi-utilisateurs

**Input**: Design documents from `/specs/001-orcaslicer-web-parity/`

**Prerequisites**: plan.md (phasage livrable P1–P6), spec.md (US1–US8), research.md (R1–R11), data-model.md, contracts/

**Tests**: OBLIGATOIRES pour toute tâche `backend/` ou `engine/` (constitution IV — TDD, tests écrits d'abord). Frontend : tests sur la logique (`lib/`), pas sur le rendu pur.

**Organization**: phases = livraisons démontrables du plan (P1–P6), chaque phase mappée sur ses user stories. Gates de sortie : plan.md « Phasage livrable » + quickstart.md §0.

**Révision post-analyze 2026-07-09** : +6 tâches (gizmos de peinture/emboss/measure/brim ears, éditeur de hauteur de couche variable + vue assemblage, dialogs spéciaux de paramètres, suppression de compte, campagne SC-004) ; T038 ré-étiqueté [US3] ; T060 inclut le verrou optimiste (conflit multi-onglets).

## Format: `[ID] [P?] [Story] Description`

- **[P]** : parallélisable (fichiers distincts, pas de dépendance à une tâche inachevée)
- **[Story]** : US1–US8 (phases user story uniquement)

---

## Phase 1: Setup (infrastructure partagée)

**Purpose**: chaîne de build et gates prêts avant toute implémentation

- [X] T001 Vérifier la chaîne Nix : `nix build .#libslic3r` et `nix build .#dump-config` passent ; documenter `$LIBSLIC3R_DIR` dans engine/README.md
- [X] T002 [P] Script de codegen racine `scripts/codegen.sh` : `audit/run_all.py` → `audit/generate_parity_annexes.py` → génération `frontend/src/generated/` (params.ts, ui-layout.ts vides pour l'instant) ; idempotent
- [X] T003 [P] CI (`.github/workflows/ci.yml` ou équivalent) : gates constitution — `cargo fmt --check`, `cargo clippy --workspace -- -D warnings`, `cargo test --workspace`, `bun run lint`, `bun run check`, `bun test`, `python3 audit/run_all.py`
- [X] T004 [P] Fixtures de test : `engine/tests/fixtures/` — 10 modèles de référence (benchy, tour, pièce à supports, multi-corps, STEP, OBJ, 3MF projet Orca…) + manifest des 5 combinaisons de presets (SC-003)

---

## Phase 2: Foundational (prérequis bloquants)

**Purpose**: artefacts générés et contrôles dont dépendent toutes les phases

- [X] T005 Extracteur `audit/extract_legacy_keys.py` : parse `PrintConfigDef::handle_legacy` + `handle_legacy_composite` de vendor/OrcaSlicer/src/libslic3r/PrintConfig.cpp → `audit/legacy_keys.json` (ré-exécutable, intégré à run_all.py) ; valider contre les 287 clés du contrôle croisé
- [X] T006 Squelette `audit/check_traceability.py` (R4) : vérifie params registre ↔ parameters.json ↔ exclusions.md, comptages presets ; sort non-zéro sur écart ; branché en CI (les périmètres UI s'ajoutent en P3/P4)
- [X] T007 [P] Pipeline ts-rs : `backend/src/http/dto/mod.rs` exporte vers `frontend/src/generated/api/` via `cargo test export_bindings` ; test de fraîcheur en CI (diff vide après régénération)
- [X] T008 [P] Module d'erreurs backend `backend/src/http/error.rs` : enveloppe `{code, message, details}`, 404 pour ressources d'autrui (SC-008), conversions domaine→HTTP ; tests unitaires

**Checkpoint**: codegen + gates opérationnels — les livraisons peuvent démarrer

---

## Phase 3: Livraison P1 — Engine wrapper + parité API C++ (US1 fondation)

**Goal**: trait `SlicerEngine` complet via bridge cxx-FFI (moteur v1 = FFI uniquement), parité prouvée contre l'oracle C++ (gate : suite générique verte sur FFI, corpus 10×5 identique à orca desktop)

**Independent Test**: `cargo run -p engine --example engine-cli -- slice engine/tests/fixtures/cube20.stl` produit un G-code cohérent (couches, stats) via le trait `SlicerEngine`

> **Décision v1 (directive utilisateur)** : moteur **FFI uniquement**, pas
> d'adaptateur CLI. Une implémentation CLI de *validation croisée* (confirmer
> que le slicing correspond exactement à orca desktop) reste un objectif
> backlog, non planifié.

- [X] T009 [US1] Codegen registre : `engine/build.rs` génère `engine/src/params/registry.rs` depuis audit/parameters.json (858 clés : type, défaut, bornes, enums, mode, groupe) ; tests : comptage exact, spot-checks (layer_height, sparse_infill_pattern, host_type)
- [X] T010 [P] [US1] Types miroirs `engine/src/api/` : Model/ModelObject/ModelVolume/ModelInstance, DynamicPrintConfig, TriangleMesh, BuildVolume, SliceRequest/SliceResult, EngineError (contrat slicer-engine-trait.md) ; tests de construction/validation
- [X] T011 [US1] Trait `SlicerEngine` dans engine/src/lib.rs + suite de tests générique `engine/tests/common/trait_suite.rs` (`fn test_engine(e: &dyn SlicerEngine)`) — écrite AVANT les implémentations (TDD)
- [X] T012 [US1] Bridge cxx : `engine/src/adapters/ffi/bridge.rs` (cxx::bridge) + `engine/src/adapters/ffi/bridge/model.cpp` ; build.rs linke `$LIBSLIC3R_DIR` (motif tools/dump-config) ; smoke test : charger un STL, compter les triangles
- [X] T013 [US1] FFI load_model (STL/OBJ/3MF) + convert_to_mesh (STEP via OCCT) dans engine/src/adapters/ffi/model.rs ; tests sur fixtures (dont STEP)
- [X] T014 [P] [US1] FFI read/write_project_3mf (scène + config embarquée) dans engine/src/adapters/ffi/threemf.rs + module commun engine/src/threemf/ ; tests aller-retour sur 3MF Orca de fixture
- [X] T015 [P] [US1] FFI repair_mesh + RepairedMeshErrors dans engine/src/adapters/ffi/mesh.rs ; tests sur maillage non-manifold de fixture
- [X] T016 [P] [US1] FFI arrange + orient (paramètres de dégagement machine) dans engine/src/adapters/ffi/arrange.rs ; tests : pas de collision, objets dans le volume
- [X] T017 [US1] FFI resolve_preset_chain + validate_config dans engine/src/presets/resolve.rs (fonction pure partagée FFI/CLI) ; tests : chaîne BBL réelle → valeurs effectives attendues, valeur hors bornes → ConfigWarning
- [X] T018 [US1] Process worker `engine/src/bin/engine_worker.rs` + pilote parent `engine/src/adapters/ffi/worker.rs` : protocole de pipe (`P`/`R`/`E`), crash C++ contenu, annulation par kill (R1/R9) ; tests `engine/tests/worker.rs` : progression monotone, kill → cancelled, crash simulé → EngineCrashed (pipeline de tranchage réel branché en T019)
- [X] T019 [US1] FFI slice via engine-worker dans engine/src/adapters/ffi/slice.rs (+ bridge/slice.cpp) : SliceRequest → G-code + stats (temps/filament/couches) via `Slic3r::Print` ; test `engine/tests/slice.rs` sur projet 3MF réel (G-code écrit, temps>0, couches>0, progression par pipe). Vignettes headless consignées dans exclusions.md
- [X] T020 [US1] `FfiEngine` implémente `SlicerEngine` (`engine/src/adapters/ffi/engine.rs`) : agrège les wrappers du bridge + les fonctions pures (presets, parseur G-code) ; preuve de substituabilité `engine/tests/ffi_trait_suite.rs` — la suite générique T011 passe intégralement sur FFI. *(remplace l'ancien adaptateur CLI : moteur v1 FFI uniquement)*
- [X] T021 [US1] Parseur G-code `engine/src/gcode/` (pur Rust) : couches/segments (`; FEATURE:`, `; CHANGE_LAYER`, `; Z_HEIGHT:`, `; LINE_WIDTH:`, E relatif M83), stats (temps, filament, couches) → GcodePreview (R6) ; test `engine/tests/gcode.rs` sur G-code BBL réel (9 types de lignes présents, aucun Unknown)
- [X] T022 [US1] Test de parité `engine/tests/gcode_parity.rs` : FFI vs sortie orca desktop enregistrée, corpus 10×5 (SC-003). — ***Corpus complet livré et vert : 45 cas + cas nominal (46).*** Les 5 cas manquants (`simplification.stl`, 0.59×0.35×0.16 mm) sont exclus car orca lui-même les refuse (EX-PARITY-SIMPLIFY). Deux prérequis d'infra ont été construits pour y parvenir : (1) **aplatisseur d'héritage OrcaSlicer** (`fixtures/generate_parity_corpus.py`) — résout récursivement `inherits` dans `$ORCA_SRC/resources/profiles/*` (enfant écrase parent), pose `from="system"` (satisfait le gate `compatible_printers` sans héritage vivant) et `curr_bed_type="Textured PEI Plate"` (évite le refus Cool Plate PETG/ABS), exporte un 3MF au config **résolu** puis le tranche via `orca-slicer` → référence desktop ; (2) **complétude registre confirmée** : sur un 3MF résolu, `config_from_orca_json` ne perd que 3 clés méta (`from`/`name`/`version`), **zéro clé de tranchage** — le trou « 2–16 % » soupçonné était un artefact du prototype à héritage non résolu, pas un défaut registre (T066). **Métrique de parité = longueur de filament + couches (±1)**, indépendante du build : écart FFI ↔ desktop **< 0,17 % (43/45 < 0,01 %)** → tranchage physiquement identique. Le **temps estimé** diverge jusqu'à ~4,6 % (variance arc-fitting entre builds `libslic3r-headless` et `orca-slicer`, à filament/couches identiques) → borné en garde-fou 8 %, consigné EX-PARITY-TIME. Correctif de fidélité annexe : `orca_values::serialize_orca_value_for` réimpose `%` aux clés `Percent`/`Percents` (forme canonique OrcaSlicer, interop 3MF FR-044). Gates verts : `cargo fmt --check`, `clippy --workspace -D warnings`, `test --workspace` (257) + `engine --features ffi` (dont les 46 cas de parité), `check_traceability.py` (7 verts). Voir [[infra-and-task-status]].
- [X] T023 [P] [US1] Démo `engine/examples/engine-cli.rs` : slice/arrange/repair/parse depuis le shell via `FfiEngine` (démo gate P1) — progression du statusbar en direct, G-code + stats, préviz par couches

> **Backlog (non planifié)** : adaptateur CLI `engine/src/adapters/cli/`
> pilotant `orca-slicer` derrière le même trait, pour *validation croisée*
> du slicing FFI. Bloqué en l'état par un segfault d'`orca-slicer --slice`
> en headless (non investigué, cf. mémoire projet).

**Checkpoint**: gate P1 — moteur démontrable et prouvé, indépendant du backend

---

## Phase 4: Livraison P2 — Auth + storage (US6)

**Goal**: comptes, sessions, isolation totale, bibliothèque de projets persistée (gate : suite de contrat Storage verte sur SQLite ET Postgres, tests d'isolation verts)

**Independent Test**: 2 comptes via l'UI minimale ; le projet de A est en 404 pour B (accès direct URL) ; persistance après reconnexion

- [X] T024 [US6] Entités domaine + traits `backend/src/domain/` : User, Project, Model, Preset, Printer, SlicingJob, Gcode, InstanceSettings/Invitation + IDs typés + traits UserRepo/ProjectRepo/…/Storage (contrat storage-trait.md, signatures scopées par UserId, async-trait) — AVANT les impls, avec suite de contrat générique `backend/tests/common/storage_suite.rs` (isolation, unicité, claim_next concurrent, cascade, re-seed, verrou optimiste) + compile-check `storage_contract_compiles.rs` (TDD)
- [X] T025 [US6] Migrations SQLite `backend/migrations/sqlite/0001_init.sql` (schéma data-model.md complet, y c. slicing_jobs/gcodes pour P5) + impl `backend/src/adapters/storage/sqlite/` (8 repos, FK cascade, claim_next atomique par UPDATE…RETURNING, verrou optimiste) ; suite T024 verte sur SQLite (`storage_contract_sqlite.rs`)
- [X] T026 [US6] Migrations + impl Postgres `backend/src/adapters/storage/postgres/` (feature `postgres`) ; suite T024 verte sur Postgres (job CI optionnel). — *Implémenté et **validé contre un Postgres 17 réel** (conteneur Docker éphémère). `backend/migrations/postgres/0001_init.sql` : miroir du schéma SQLite en types natifs (`uuid`, `jsonb`, `timestamptz`, `boolean`, `bigint`), état final (unicité preset `(kind, name, vendor)` + table `sessions`), FK `ON DELETE CASCADE/SET NULL`, index partiels (`WHERE user_id IS [NOT] NULL`), index de file `(status, created_at)`. `adapters/storage/postgres/{mod,repos}.rs` : `PostgresStorage` implémentant `Storage`, placeholders `$n`, mappers `PgRow` natifs (uuid→newtype `From<Uuid>`, jsonb→`serde_json::Value`, timestamptz→`OffsetDateTime`, bool), `map_pg` (SQLSTATE 23505 → `Conflict`), verrou optimiste via `UPDATE … WHERE version = $n RETURNING`, et **`claim_next` = `FOR UPDATE SKIP LOCKED`** (idiome PG : chaque réclamant concurrent verrouille/saute → livraison unique). `Cargo.toml` : feature `postgres` (le module + la suite sont gatés `#[cfg(feature = "postgres")]`) ; ajout du feature sqlx `json` (jsonb↔Value). `tests/storage_contract_postgres.rs` : exécute la **même** suite de contrat générique (`common::storage_suite::run_all`) que SQLite (T025) sur une base pointée par `TEST_DATABASE_URL` (ignoré proprement si absent), après `TRUNCATE … CASCADE` (ré-exécutable). Résultat : **contrat T024 vert sur Postgres** (isolation SC-008→NotFound, unicité email/projet/preset→Conflict, verrou optimiste→VersionConflict, `claim_next` sans double délivrance sous concurrence, `requeue_running`, reseed préservant les presets utilisateur, cascade de suppression de compte). Gates verts : `cargo fmt`, `clippy --features postgres` (lib), `clippy` + `test --workspace` défaut (92 tests, adaptateur PG gaté hors du build par défaut), test de contrat PG vert. **Périmètre** : sélection runtime du SGBD dans `server.rs` reste SQLite (le store de sessions `tower-sessions` est typé `SqlitePool` ; le basculer demanderait de le rendre générique — hors périmètre de cette tâche « adaptateur + contrat »). Reproduction : `docker run … postgres:17` puis `TEST_DATABASE_URL=postgres://… cargo test -p backend --features postgres --test storage_contract_postgres`.*
- [X] T027 [P] [US6] Adaptateur fichiers `backend/src/adapters/files/` : `FileStore` — espaces `data/users/<id>/{models,gcodes,thumbnails}`, écriture atomique (tmp + rename), purge par cascade idempotente ; tests : aller-retour, isolation par utilisateur, purge, traversée de chemin impossible (chemins bâtis sur IDs typés + extension validée)
- [X] T028 [US6] Auth `backend/src/auth/` + `backend/src/http/` : argon2id (`password.rs`), `register` selon `registration_policy` (open/closed/invite + invitations usage unique), bootstrap premier compte = admin, `authenticate` (`service.rs`) ; couche tower-sessions via store maison sqlx 0.9 (`adapters/sessions.rs`, `tower-sessions-sqlx-store` incompatible sqlx 0.9/core 0.15) + endpoints register/login/logout/me (`http/routes/auth.rs`), extracteurs `CurrentUser`/`AdminUser`, composition root `server::build_app`. Tests verts (`auth_service.rs`, `http_auth.rs` : register→session→me, 401 sans session, logout ; unités) : hash jamais en clair, politiques, identifiants
- [X] T029 [US6] Endpoints admin `backend/src/http/routes/admin.rs` : instance (GET/PATCH), users (POST, reset-password), invitations (POST), reseed placeholder (501) ; services `auth::{create_managed_user,reset_password,create_invitation}` ; DTO ts-rs + extracteur `AdminUser`. Tests `http_admin.rs` : lecture/écriture réglages, 403 non-admin / 401 sans session, create+reset (login avant/après), invitation utilisable en politique invite, reseed 501
- [X] T030 [US6] Endpoints projets `backend/src/http/routes/projects.rs` : CRUD (list/create/get/put/delete) + duplicate/rename + vignette (GET), document scène versionné (`schema_version`, défaut par le backend), verrou optimiste (`expected_version` → 409). DTO ts-rs (CreateProjectRequest, SaveProjectRequest, RenameProjectRequest, ProjectResponse). Tests `http_projects.rs` (6) : CRUD, verrou optimiste, duplicate/rename, **isolation inter-comptes (404 partout, SC-008)**, 401 sans session
- [X] T031 [US6] Suppression de compte : `DELETE /api/auth/me` (confirmation par mot de passe) + `DELETE /api/admin/users/{id}` dans backend/src/http/routes/{auth,admin}.rs — service `auth::delete_account` (cascade BDD via repo + garde « dernier administrateur » → 403) puis `FileStore::purge_user` + flush session. DTO `DeleteAccountRequest`. Tests : self-delete (mauvais mdp → 403, bon mdp → 204 + session/login invalides, dernier admin protégé) ; admin delete (204, dernier admin → 403, id inconnu → 404)
- [X] T032 [P] [US6] Frontend auth : client HTTP typé (`lib/api/{client,auth,projects,types}.ts`), store+garde de session (`lib/api/session.ts`), pages `(auth)/login` et `(auth)/register`. Proxy Vite `/api`→backend. Tests bun (`client.test.ts`) : sérialisation+cookie, enveloppe d'erreur→ApiError, 204→undefined, pas de content-type sans corps
- [X] T033 [P] [US6] Frontend bibliothèque : `routes/library/+page.svelte` (dates, ouvrir/dupliquer/renommer/supprimer) sur le client typé ts-rs, garde de session dans `+page.ts` (ssr off → redirect login) ; route d'ouverture `routes/projects/[id]` (garde + get, 404 SC-008). Navigation via `resolve()`
- [X] T034 [P] [US6] Brouillon de session client (clarification) : `lib/stores/draft.ts` sur IndexedDB (backend abstrait + repli/mémoire pour tests), restauration si brouillon plus récent que la version serveur (bannière sur `projects/[id]`) ; tests bun (`draft.test.ts`) : save/load/clear, isolation par projet, logique de restauration

**Checkpoint**: gate P2 — multi-comptes démontrable, socle pour P3–P6

---

## Phase 5: Livraison P3 — Presets + onglets paramètres (US2, US3)

**Goal**: 11 895 presets seedés avec héritage, onglets Process/Filament/Imprimante générés complets, modes simple/advanced/expert (gate : comptages == Annexe C, check_traceability vert sur params + UI réglages)

**Independent Test**: comparaison côte à côte avec references/orca-prepare.png ; dériver un preset filament et vérifier le diff

- [X] T035 [US3] Codegen legacy : tables générées `engine/src/params/legacy_tables.rs` (28 renommages + 44 clés ignorées) depuis audit/legacy_keys.json via `audit/generate_legacy_rs.py` (câblé dans scripts/codegen.sh, gardé par SHA-1) ; `engine/src/params/legacy.rs` porte fidèlement `PrintConfigDef::handle_legacy` (renommages, transformations conditionnées par la valeur, effacements %, `different_settings_to_system` récursif, abandon des clés ignorées/hors-registre). Tests engine (10) : sync audit, renommages, sélecteurs de filament 1→0, transformations conditionnelles, prime_tower_rib_wall, clés ignorées/inconnues abandonnées. **NB** : l'audit réel compte 28 renommages (pas « 287 » ; chiffre illustratif de la tâche corrigé)
- [X] T036 [US3] Import des profils système `engine/src/presets/import.rs` : `import_profiles(dir)` parcourt les 65 index vendeurs (blacklist exclu), charge chaque preset des 4 listes, capture inherits/from/setting_id/filament_id/instantiation/compatible_printers + surcharges de config (méta exclus, BOM/UTF-8 tolérés). Miroir fidèle de `extract_presets_inventory.py`. Tests engine (5) : **comptages exacts {machine_model 384, machine 1158, filament 7012, process 3341}, total 11 895, 65 vendeurs (SC-002)** ; instanciés {5913, 1001, 2881} ; héritage/méta capturés ; répertoire manquant = erreur
- [X] T037 [US3] Résolution d'héritage `engine/src/presets/resolve.rs` : `PresetIndex` indexe les presets importés par (vendeur, type, nom), reconstruit la chaîne racine→feuille en suivant `inherits` (cycles/parents manquants détectés), `resolve`/`resolve_derived` (surcharges seules par-dessus la chaîne parent). Tests engine (5) : chaîne « Bambu Lab A1 0.4 nozzle » racine→feuille, valeurs effectives connues (printable_height feuille=256, gcode_flavor racine=marlin, retraction_length niveau intermédiaire), dérivé surcharges-seules, **propagation d'un changement parent (US3-AS4)**, preset inconnu/cycle = erreur
- [X] T038 [US3] Seed au démarrage + reseed admin : `backend/src/domain/presets.rs` (mapping pur `ImportedPreset`→`Preset` + `reseed_system_presets` via `Storage`), seed conditionnel au boot (`server.rs`, si `PresetRepo::system_count()==0`), endpoint `POST /api/admin/presets/reseed` réel (remplace le 501) lisant `AppState.profiles_dir` (env `PROFILES_DIR`), DTO `ReseedResponse`. Tests : `presets_seed.rs` (idempotence du compte, presets user intacts après reseed) ; `http_admin.rs` reseed sur fixture (5 presets, 403 non-admin, idempotent)
- [X] T039 [US3] Endpoints presets `backend/src/http/routes/presets.rs` : liste filtrée compatibilité (kind, printer, instantiation), GET brut/résolu, CRUD user, import/export JSON Orca avec conversion legacy ; tests de contrat (FR-020..023)
- [X] T040 [US2] Codegen UI : `scripts/codegen.sh` étendu → `frontend/src/generated/ui-layout.ts` (21 pages/100 sections/525 options depuis ui_inventory.json) + `frontend/src/generated/params.ts` (registre complet) ; test de fraîcheur CI
- [X] T041 [US2] Bibliothèque de widgets `frontend/src/lib/settings/widgets/` : Bool, Int, Float+unité, Percent, FloatOrPercent, Enum(select), String, Strings, Multiline (G-code), Point(s), Color — résolution par type du registre ; tests vitest de mapping type→widget
- [X] T042 [US2] Rendu générique `frontend/src/lib/settings/SettingsTabs.svelte` + `OptionLine.svelte` : arbre ui-layout.ts, modes simple/advanced/expert (champ mode), tooltips, sidetext, catégories, recherche additive ; tests du filtre de mode
- [X] T043 [US2] État de réglages `frontend/src/lib/settings/store.ts` : valeurs effectives (preset résolu) + surcharges projet, marqueurs modifié/verrou, reset par option (US2-AS6), validation bornes/enums via params.ts ; tests vitest
- [X] T044 [US2] Pages spéciales : `frontend/src/lib/settings/special/OverridesPage.svelte` (cases N/A filament_*), MachineGcode.svelte (éditeurs multilignes), MultimaterialTables.svelte ; conformes Annexe B
- [X] T045 [US2] Dialogs spéciaux de paramètres `frontend/src/lib/settings/special/` : éditeur de forme de plateau (BedShape.svelte : `printable_area`, `bed_exclude_area`, `bed_custom_model`, `bed_custom_texture`), volumes de purge (FlushVolumes.svelte : `flush_volumes_matrix/vector`, `flush_multiplier`), table des températures par type de plaque (PlateTemps.svelte : `cool/eng/hot/textured*_plate_temp*`) — couvre les clés hors lignes d'option (analyse G6) ; tests de sérialisation des valeurs
- [X] T046 [P] [US3] Sélecteurs de presets `frontend/src/lib/presets/` : imprimante (vendeurs/modèles/buses, cover images), filament/process filtrés, dériver/sauvegarder/supprimer, badge héritage ; tests du filtre de compatibilité
- [X] T047 [US2] Étendre audit/check_traceability.py au périmètre UI réglages : chaque option de ui_inventory ↔ ui-layout.ts ∪ exclusions.md, y compris les clés portées par les dialogs T045 ; gate CI vert (SC-001 partiel)

**Checkpoint**: gate P3 — onglets complets démontrables contre orca-prepare.png

---

## Phase 6: Livraison P4 — Scène 3D + plater (US4 + import US1)

**Goal**: préparation de scène complète : import, transformations, outils (16 gizmos), multi-plateaux, sauvegarde (gate : scénarios US4 verts, SC-007 mesuré)

**Independent Test**: scène multi-objets multi-plateaux préparée (avec peintures et hauteurs de couche variables), sauvegardée, rouverte à l'identique

- [X] T048 [US1] Upload modèles `backend/src/http/routes/models.rs` : multipart 500 Mo, formats STL/3MF/STEP/OBJ, STEP → conversion asynchrone via SlicerEngine (WS model.converted), 3MF projet → import scène+réglages ; tests (formats, corrompu, limite taille) — *upload/détection/validation/stockage complets et testés (9 tests) ; l'**exécution** de la conversion STEP→mesh + l'event `model.converted` dépendent du bus WS (T065), l'import scène d'un 3MF projet dépend du handle moteur — câblés en T065/T054. Un STEP est stocké `conversion_pending`.*
- [X] T049 [US1] Endpoint maillage `GET /api/models/{id}/mesh` : format binaire compact (positions/normales/index) pour Threlte ; tests de sérialisation — *format « WSMh » (magic/version/counts + f32 positions/normales + u32 indices) + parseur STL binaire/ASCII pur Rust ; STEP → 409 tant que non converti (T065), OBJ/3MF via aperçu client (T051) → 501. Tests : round-trip encode/decode, parse STL, endpoint binaire, 409 STEP, 404 inter-comptes.*
- [X] T050 [P] [US4] Scène Threlte `frontend/src/lib/scene/` : Canvas, plateau (bed_model/bed_texture du preset machine, grille, origine), caméra orbit, éclairage, sélection (raycast) ; conforme references/orca-prepare.png
- [X] T051 [P] [US4] Loaders client STL/OBJ/3MF `frontend/src/lib/scene/loaders.ts` (aperçu immédiat, R7) + upload en tâche de fond ; tests vitest sur petits fichiers — *parseurs purs STL binaire/ASCII, OBJ (triangulation en éventail, indices négatifs), 3MF (unzip fflate + regex XML, pas de DOMParser → testable sous bun) produisant un `SceneMesh` facetté ; `loadPreview` (aperçu local immédiat), `uploadModel` (multipart en tâche de fond) et `fetchMesh` (décodage WSMh backend). 10 tests bun sur petits fichiers.*
- [X] T052 [US4] Gizmos move/rotate/scale + pose à plat `frontend/src/lib/scene/gizmos/` + panneau numérique de transformation ; undo/redo `frontend/src/lib/stores/history.ts` ; tests du store history — *maths pures `transform.ts` (Transform position/rotation°/échelle, clampScale, normalizeAngle, quaternion↔Euler XYZ façon Three.js, `layFlatRotation`) + composants Threlte `gizmos/` (TransformGizmo sur TransformControls avec onchange/oncommit pour l'historique, TransformPanel numérique + « poser à plat », GizmoToolbar) ; store générique `History<T>` (past/present/future, troncature de branche redo, limite de profondeur). 13 tests bun (history 6, transform 7).*
- [X] T053 [US4] Liste d'objets `frontend/src/lib/scene/ObjectList.svelte` : sélection, groupes, duplication, verrouillage/masquage, extrudeur par objet/pièce, réglages par objet (surcharges FR-015) — *modèle pur `objects.ts` (`ObjectTree` : objets/pièces/groupes, add/duplicate sous-arbre avec remap parents, group/ungroup, remove, verrou & masquage effectifs hérités, extrudeur effectif hérité, surcharges de réglages FR-015) + composant présentationnel `ObjectList.svelte` (arbre récursif, callbacks de mutation côté parent). 9 tests bun.*
- [X] T054 [US4] Outils moteur côté API `backend/src/http/routes/scene.rs` : repair (rapport), arrange, orient, opérations booléennes via SlicerEngine ; tests de contrat — *module pur `backend/src/scene.rs` (arrange_grid shelf-packing sans collision FR-013, analyze_mesh → rapport triangles/dégénérés/arêtes de bord/étanchéité FR-012, suggest_orientation plus-grande-facette-vers-le-bas ; 6 tests) + endpoints `scene.rs` (POST `…/arrange`, `…/orient`, `/api/models/{id}/repair`) avec isolation SC-008 (404) et DTOs ts-rs ; 7 tests de contrat. **Périmètre** : les versions haute-fidélité (nesting, recousu, auto-orient support-aware) et les booléens passent par le worker FFI `SlicerEngine` une fois câblé (phase P5) — non exposés ici pour rester fidèle au contrat http-api.md (pas d'endpoint booléen).*
- [X] T055 [US4] Outils UI : coupe (plan positionnable, moitiés/connecteurs), réparation avec rapport, booléens, simplification — `frontend/src/lib/scene/tools/` ; branchés sur T054 — *géométrie pure `cut.ts` (signedDistance, splitByPlane par clipping Sutherland–Hodgman → deux moitiés, connectorGrid) et `simplify.ts` (décimation par regroupement de sommets sur grille) ; 8 tests bun. Composants `CutTool`/`SimplifyTool` (présentationnels, émettent au parent), `RepairTool` (appelle `POST …/repair` T054, affiche le rapport FR-012), `BooleanTool` (choix union/diff/inter, désactivé tant que le CSG moteur FFI n'est pas exposé — phase P5) ; client `api/scene.ts` (arrange/orient/repair).*
- [X] T056 [US4] Gizmos de peinture `frontend/src/lib/scene/gizmos/painting/` : supports (FdmSupports), couture (Seam), fuzzy skin (FuzzySkin), segmentation multi-matériaux (MmSegmentation) — pinceau/sphère/remplissage, rayon ajustable (raccourcis groupe Gizmo : Ctrl/Alt+molette), stockage au format 3MF Orca dans le document scène (data-model.md) ; tests de sérialisation des peintures (analyse G1) — *codec `facets.ts` fidèle au format Orca `TriangleSelector::serialize` (flux de bits → quartets hex insérés en tête ; enforcer→"4", blocker→"8" ; états étendus n≥3 pour MMU ; -1 pour triangle subdivisé, raffinement moteur) ; modèle `TrianglePainting` (4 canaux supports/seam/fuzzy/mmu, paint/clear, sérialisation ↔ document 3MF `slic3rpe:*`) + `trianglesInRadius` (pinceau) ; `PaintToolbar.svelte` (canal/outil brush-sphere-fill/état/rayon, Ctrl/Alt+molette). 7 tests bun G1.*
- [X] T057 [US4] Gizmos texte/SVG en relief (Emboss, SVG — création de volumes via engine), mesure (Measure : distances/angles sur le maillage), oreilles de bord (BrimEars : points + `brim_ears` params) dans `frontend/src/lib/scene/gizmos/` + endpoints engine nécessaires dans backend/src/http/routes/scene.rs ; tests (analyse G1) — *`measure.ts` (distance/delta/angleDeg/angleBetweenNormals, purs), `brim-ears.ts` (`BrimEars` : points add/remove + round-trip document, clés `brim_ears_*`), `emboss.ts` (`EmbossParams` texte/SVG + `validateEmboss`) ; composants `MeasureTool`/`BrimEarsTool`/`EmbossTool`. 7 tests bun. **Périmètre** : la génération de maillage relief (rastérisation police, tracé SVG→extrusion) est une opération moteur FFI — bouton « Créer le volume » désactivé, endpoint câblé phase P5 (pas d'endpoint 501 ajouté pour rester fidèle au contrat http-api.md).*
- [X] T058 [US4] Éditeur de hauteur de couche variable (toolbar `layersediting` : profil par objet via LayerHeightProfile du trait engine, courbe éditable + lissage adaptatif) `frontend/src/lib/scene/tools/LayerHeight.svelte` + vue assemblage (`assembly_view` + gizmo Assembly : éclaté des pièces) ; profil persisté dans le document scène ; tests engine du profil (analyse G2) — *`layer-height.ts` (miroir du `layer_height_profile` Orca : vecteur plat [z,h,…] ; heightAt interpolé, setBand par fenêtre Z, smooth adaptatif, clampHeight, serialize/deserialize, meshMaxZ) + `assembly.ts` (sceneCenter, explode radial) ; composants `LayerHeight.svelte` (courbe SVG + bande/lissage/reset) et `AssemblyView.svelte` (éclaté + facteur). 10 tests bun G2. Le calcul adaptatif depuis le maillage reste une opération moteur FFI (phase P5).*
- [X] T059 [US4] Multi-plateaux `frontend/src/lib/scene/plates.ts` + réglages par plateau (type de plaque) ; ajout/suppression/répartition ; tranchage par plateau préparé pour P5 — *`PlateSet` pur (plateaux avec objets/type de plaque/nom, add/remove avec migration des objets, assign exclusif, `distribute` par paquets créant les plateaux, `plateIndex` cible du tranchage par plateau, round-trip document) réutilisant `PLATE_TYPES` (6 types) + `PlateBar.svelte` (onglets, ajout/suppression, sélecteur de type). 7 tests bun. Le body `plate_index` de `POST …/slice` est câblé en P5.*
- [X] T060 [US4] Sauvegarde projet complète : document scène (data-model.md, y c. peintures T056 et profils T058) sérialisé/restauré, vignette générée (canvas), Ctrl+S ; **verrou optimiste** : champ `version` sur PUT, 409 + avertissement en cas de conflit multi-onglets (edge case spec, analyse G5) ; tests aller-retour + conflit — *`document.ts` : `SceneDocument` versionné (`schema_version`, plateaux, objets → transform/extrudeur/surcharges FR-015/peintures 3MF/profil de couche/oreilles) avec `serializeScene`/`parseScene` et aller-retour objet complet ; `save.ts` : `saveScene` (verrou optimiste, 409→`conflict` sans écrasement), `classifySaveError`, raccourci `onSaveShortcut` (Ctrl/Cmd+S), `captureThumbnail` (canvas→dataURL) ; `SaveControls.svelte` (bouton + bannière de conflit). 5 tests bun G5 (round-trip complet, classification du conflit). Le PUT verrou optimiste + 409 est déjà couvert côté backend (http_projects).*
- [X] T061 [P] [US4] Menus contextuels + barre d'outils + raccourcis plateau/liste (Annexe B §B.3–B.6 groupes Plater/Objects List) dans `frontend/src/lib/scene/menus.ts` ; entrées ajoutées à traceability-map.json — *`menus.ts` : `MAIN_TOOLBAR`/`ASSEMBLE_TOOLBAR` (B.4), `PLATER_MENU` (54 libellés B.3), `PLATER_SHORTCUTS` (45), `OBJECTS_LIST_SHORTCUTS` (12), `GIZMO_SHORTCUTS` (4) — libellés/touches verbatim de l'Annexe B. `menus.test.ts` reparse l'annexe et vérifie la couverture exacte (5 tests). `specs/001-orcaslicer-web-parity/traceability-map.json` créé (gizmos 16, toolbars 11, menus 54, raccourcis Plater/Objects List/Gizmo → références d'implémentation). Le `TRACE_MAP` du checker est repointé sur ce fichier en T062.*
- [X] T062 [US4] Étendre check_traceability.py : gizmos (16/16)/toolbars/menus/raccourcis ↔ traceability-map.json ∪ exclusions.md ; benchmark SC-007 (2 M triangles / 20 objets) documenté dans quickstart — *`check_trace_map` réécrit : vérifie que les 16 gizmos, les boutons de barres d'outils (main/assemble/separator), les 54 libellés de menus contextuels et les raccourcis des groupes Plater/Objects List/Gizmo (ui_inventory.json) sont mappés dans `traceability-map.json` ∪ exclusions.md ; `TRACE_MAP` repointé sur `specs/.../traceability-map.json`. Gate : « P4 — gizmos 16/16 … aucun écart » (6 verts). Test négatif vérifié (gizmo/raccourci retirés → exit 1). Benchmark SC-007 (§7 quickstart) : protocole 20 objets/2 M triangles, cibles FPS/latence par interaction, leviers de tenue de charge.*

**Checkpoint**: gate P4 — préparation de scène démontrable de bout en bout (16 gizmos couverts)

---

## Phase 7: Livraison P5 — Slicing serveur + préviz G-code (US1, US5, US7)

**Goal**: file persistante avec reprise, progression WS, prévisualisation par couches complète, exports (gate : US1/US5/US7 verts, SC-005/006 mesurés, reprise au reboot testée)

**Independent Test**: trancher depuis l'UI, suivre la progression, explorer la préviz (cible orca-preview.png), télécharger le G-code ; kill du backend pendant un job → reprise

- [X] T063 [US7] File `backend/src/queue/` : JobRepo.claim_next transactionnel, pool de workers (config), transitions d'états (data-model.md), requeue_running au boot ; tests : concurrence (jamais 2× le même job), reprise, annulation kill — *pool de workers `queue::Queue` (config workers/poll) au-dessus de `JobRepo` : `claim_next` atomique (déjà transactionnel côté SQLite via UPDATE…RETURNING), `requeue_running` au démarrage (reprise R9), transitions via `update_progress`/`finish` ; `JobRunner` abstrait (runner FFI câblé T064/T066) + `JobContext` (report progression, signal `Cancel` coopératif) ; annulation « kill » propagée au runner en cours. 3 tests d'intégration (20 jobs/4 workers → chacun traité une seule fois ; running orphelin → requeue → traité ; annulation d'un job en cours → runner interrompu, statut `cancelled`).*
- [X] T064 [US1] Endpoint slice `POST /api/projects/{id}/slice` (plate_index|all) : presets résolus figés (resolved_settings), création jobs ; tests de contrat + avertissements moteur restitués (FR-032, hors plateau → erreur avant slice) — *`slice.rs` : résout les presets actifs (`{printer, process, filaments[]}`) en une config unique, la clampe (`validate()`) et fige `resolved_settings` (reproductibilité) ; restitue les avertissements moteur (FR-032) ; gardes avant slice — plateau vide (422) et objet `off_bed` (422) ; crée un job `queued` par plateau ciblé (`plate_index` borné, ou tous). DTOs ts-rs `JobResponse`/`SliceRequest`/`SliceResponse`/`SliceWarning` ; helpers presets rendus `pub(crate)`. 6 tests de contrat (slice plateau + warnings figés, all → N jobs, plateau vide, hors plateau, index hors limites, 404 inter-comptes). L'exécution reste au pool T063 via runner FFI (T066).*
- [X] T065 [US7] WebSocket `/api/ws` `backend/src/http/ws.rs` : canal authentifié, events job.updated/job.finished/model.converted (DTO ts-rs) ; tests : isolation par compte, progression relayée depuis le pipe engine-worker — *bus `EventHub` cloisonné par compte (un canal `tokio::broadcast` par `UserId`) : un abonné ne reçoit que ses propres événements (isolation SC-008). Handler `/api/ws` : `CurrentUser` extrait avant l'upgrade → session absente = **401** avant toute négociation ; `on_upgrade` pompe le canal vers la socket (absorbe `Lagged`, se ferme sur `Close`). DTO ts-rs `ServerEvent` = union discriminée `event` (`job.updated`/`job.finished`/`model.converted`). Relais de progression : la file (T063) implémente un nouveau port `JobEventSink` (queue) — `JobContext::report` publie `job.updated` (running) et la fin publie `job.finished`/`job.updated` (failed/cancelled) au propriétaire ; `Queue::with_event_sink(hub)` branche le bus. `model.converted` publié directement par les handlers (câblage conversion STEP effective = T066/FFI). 3 tests : isolation du bus (event à A jamais vu par B), progression d'un job relayée au seul propriétaire (0.5 running puis failed), garde d'auth (401 sans session). Réexport frontend `ServerEvent`. (Fmt : réintégré le formatage de `presets.rs`/`slice.rs`/`http_slice.rs`, laissés non formatés par les éditions `sed` tardives de T064.)*
- [X] T066 [US1] Post-traitement job : G-code stocké (files adapter), stats extraites (GcodePreview), vignettes ; `GET /api/gcodes/{id}/download` + `/stats` ; tests — *module pur `backend/src/gcode.rs` : `parse_stats` lit les commentaires normalisés Orca (miroir `libslic3r/GCode.cpp` : `estimated printing time (normal mode)`, `total filament used [g]`/`cost`/`change`, `total layers count`, `filament used [mm|cm3|g]`/`cost` par extrudeur) → `GcodeStats` (temps en secondes via `parse_dhms` d/h/m/s) ; `extract_thumbnails` décode les blocs `; thumbnail begin WxH LEN … end` (miroir `GCode/Thumbnails.hpp`) → PNG base64. `store_gcode` orchestre le post-traitement : écrit le fichier via `FileStore::write_gcode` (clé de stockage propre, `file_path` fait foi) puis crée la ligne `gcodes` avec `stats`/`thumbnails` figés (`preview_path` binaire par couche = T067). Endpoints `backend/src/http/routes/gcodes.rs` : `download` (pièce jointe `text/plain`, `Content-Disposition`) et `stats` (JSON figé), tous deux 404 inter-comptes (SC-008). 11 tests : 6 unités du parseur (totaux+temps, listes par extrudeur, `parse_dhms`, vignette, tolérance G-code nu, JSON sans champ absent), 5 intégration (download pièce jointe, stats parsés, vignette persistée, 404 inter-comptes download+stats, id malformé → 404). L'invocation depuis un slicing réel dépend du runner FFI (T066+), `store_gcode` en est le point d'entrée.*
- [X] T067 [US5] Buffers préviz `backend/src/http/routes/preview.rs` : `/preview/meta` (couches, types présents, échelles) + `/preview/layers?from&to` (binaire R6) ; tests : format, plages, tailles — *parseur pur `gcode::parse_preview` reconstruit le modèle couches → segments typés depuis les commentaires stables Orca (`;TYPE:`/`; FEATURE:`, `;LAYER_CHANGE`/`; CHANGE_LAYER`, `;HEIGHT:`/`; LAYER_HEIGHT:`, `;WIDTH:`/`; LINE_WIDTH:`, `;Z:`) et les déplacements G0/G1 (E croissant = extrusion ; modes absolu/relatif M82/M83, G92, changement d'outil T0–T9) ; table rôle→id u8 miroir `ExtrusionEntity::role_to_string` (19 rôles + Travel). `PreviewModel::summary` → plages (feedrate/largeur/hauteur), types & extrudeurs présents, z/segments par couche ; `encode_range(from,to)` → buffer binaire `WSPv` (magic|version|from|to|count + N×40 o : 6×f32 pos, F/w/h f32, kind u8, extruder u8, layer u16), bornes clampées. Endpoints `routes/preview.rs` : `/preview/meta` (DTO ts-rs `PreviewMeta`/`PreviewLayerMeta`/`PreviewSegmentType`) et `/preview/layers?from&to` (octet-stream), parse à la volée depuis `file_path`, 404 inter-comptes (SC-008). 7 tests unités (couches/segments typés, couches de pur déplacement écartées, extrusion relative, résumé plages/types, en-tête & tailles d'enregistrement, clamp des bornes + modèle vide, round-trip noms de rôles) + 4 intégration (méta couches/types/échelles, en-tête `WSPv` + 3 enregistrements, plage par défaut = toutes couches, 404 inter-comptes meta+layers). Fidélité fine (arcs G2/G3, wipe, couture, débit exact) = parseur moteur FFI. `preview_path` (cache binaire précalculé) reste optionnel : la préviz est parsée depuis le fichier G-code.*
- [X] T068 [US5] Rendu préviz `frontend/src/lib/preview/` : BufferGeometry par plage, 7 colorations (type/vitesse/hauteur/largeur/débit/température/filament) avec légende/échelle, visibilité par type ; conforme references/orca-preview.png — *`decode.ts` : décode les buffers binaires `WSPv` (miroir exact de `PreviewModel::encode_range`, en-tête 18 o + enregistrements 40 o, little-endian) en tableaux parallèles ; rejette magic/tailles invalides. `colorations.ts` : palettes **fidèles OrcaSlicer** (`libvgcode` `DEFAULT_EXTRUSION_ROLES_COLORS` mappé sur les 19 rôles + Travel, `DEFAULT_RANGES_COLORS` 11 arrêts bleu→rouge, palette filament) + `sampleRange`/`rangeColor` ; types de légende (liste/échelle). `geometry.ts` (pur) : `buildPreviewGeometry` → positions+couleurs par sommet (2/segment) selon la coloration active, **filtrage par visibilité de type** ; les 7 colorations FR-041 — type (rôle), vitesse (F), hauteur, largeur, débit (`flowValue` = section×vitesse, mm³/s), filament (par extrudeur), température (neutre : donnée par-segment absente, piste moteur) ; `buildLegend` produit liste (type/filament) ou échelle mm/s·mm·mm³/s. `PreviewLines.svelte` (présentational, Threlte) : `LineSegments` à couleurs de sommet, dispose la géométrie au changement. 19 tests bun (round-trip décodage + rejets, interpolation/bornes du dégradé, fidélité palette de rôles, positions/couleurs, masquage par type, mode vitesse/température, débit, légendes). Les curseurs de couches/progression intra-couche = T069.*
- [X] T069 [US5] Curseurs couches (min–max) + progression intra-couche + raccourcis groupe Preview `frontend/src/lib/preview/sliders.ts` ; tests vitest des plages d'index — *`sliders.ts` (pur) : curseur vertical `LayerRange` (deux poignées low/high, poignée active, mode une-couche `L`) — `moveThumb` (bornage [0,total-1], `low`≤`high`, déplacement conjoint en mode une-couche), `toggleOneLayerMode`, `visibleRange`→`{from,to}` pour `?from&to`. Curseur horizontal `MoveCursor` (progression intra-couche = nombre de segments affichés de la couche du haut) — `moveCursorBy`/`Start`/`End`/`retargetCursor`. Raccourcis `PREVIEW_SHORTCUTS` (13, Annexe B « Preview » verbatim) + `resolvePreviewKey` (flèches→thumb/cursor avec flag rapide, `L`/`C`/`Tab`/`Home`/`End`) + réducteur `applyPreviewKey` (pas rapide ×5 Shift/Ctrl, bascule fenêtre G-code, `switch-tab` externe). **Groupe « Preview » ajouté à `traceability-map.json`** (13 clés → `sliders.ts`) : la parité l'enforce désormais (P4 vert). 15 tests bun des plages d'index (poignées & bornage, mode une-couche, curseur intra-couche Home/End, résolution des touches, pas rapide, verbatim des 13 raccourcis).*
- [X] T070 [P] [US5] Panneau stats `frontend/src/lib/preview/StatsPanel.svelte` : durées par type (%, utilisation), estimation totale, filament (longueur/volume/masse/coût), changements d'outil — parité orca-preview.png — *`stats.ts` (pur) : `buildPreviewStats(GcodeStats, segments?)` assemble le modèle de vue — totaux figés prioritaires (temps estimé, masse/coût, changements d'outil, couches ; FR-043) et **répartition par type de ligne dérivée des segments décodés** (`perTypeStats` : longueur euclidienne + temps = longueur/(vitesse/60) par rôle, triés par temps décroissant, part de temps/longueur en %) ; `buildFilaments` zippe les tableaux par extrudeur (longueur/volume/masse/coût) ; `formatDuration` façon Orca (d/h/m/s, composantes nulles omises). `GcodeStats` (interface miroir du backend). `StatsPanel.svelte` (présentational) : estimation globale (temps/couches/changements d'outil), table par type (pastille couleur du rôle + temps + %), table filament par extrudeur avec ligne de totaux (Σ masse/coût). 10 tests bun (formatage durées, agrégat par type, priorité des totaux figés vs calculés, zip filament, absence de segments/stats).*
- [X] T071 [P] [US7] Page file d'attente `frontend/src/routes/queue/+page.svelte` : états, progression, annulation, historique, notifications job.finished — *A d'abord câblé l'**API jobs** manquante (prérequis) : `routes/jobs.rs` — `GET /api/jobs` (liste+historique, plus récent d'abord, FR-031), `GET /api/jobs/{id}`, `POST /api/jobs/{id}/cancel` (annulation `queued|running→cancelled`, idempotente si déjà annulée, 409 si terminée), 404 inter-comptes (SC-008) ; `JobResponse` enrichi (`gcode_id`/`error`/`created_at`/`updated_at`) ; 5 tests de contrat. Frontend : `lib/api/jobs.ts` (client typé), `lib/queue/queue.ts` (pur : `applyEvent` réducteur d'événements WS, `partitionJobs` active/historique, `statusMeta`/`progressPercent`), `lib/queue/events.ts` (enveloppe `WebSocket` `/api/ws` reconnectante + `decodeEvent`), page `routes/queue/+page.svelte` (+`+page.ts` garde de session) : file active (badges d'état, barres de progression, bouton annuler), historique (table + lien de téléchargement G-code), notification éphémère sur `job.finished` (US7-AS1), abonnement WS temps réel au montage. 11 tests bun (réducteur `job.updated`/`job.finished`/inconnu/`model.converted`, partition, badges, progression bornée, décodage d'événements).*
- [X] T072 [US1] Export projet 3MF `GET /api/projects/{id}/export/3mf` (via engine::threemf, compatible Orca) + bouton UI ; test aller-retour desktop — *writer **pur Rust** `engine::threemf` (nouveau module, distinct du chemin FFI) produisant l'archive OPC standard qu'OrcaSlicer/PrusaSlicer lisent (miroir `libslic3r/Format/3mf.cpp`) : `[Content_Types].xml`, `_rels/.rels`, `3D/3dmodel.model` (géométrie 3MF core, un `<object>` par objet, volumes fusionnés dans le repère monde via leur matrice), `Metadata/Slic3r_PE.config` (config figée en lignes `; key = value`, `compatible_printers` exclu comme Orca) ; `read_project_bytes` pour la validation croisée. Endpoint `routes/export.rs` : assemble les maillages STL du projet (décodés serveur via `mesh::parse_stl`) + la config des presets actifs résolus (`slice::resolve_active_config` extrait, typé `DynamicPrintConfig`), pièce jointe `<nom>.3mf` (nom assaini), 404 inter-comptes (SC-008). Bouton « Exporter 3MF » sur la page projet. 5 tests (3 engine : entrées OPC standard, round-trip géométrie+config, matrice de volume appliquée ; 2 backend : archive valide géométrie+entrées, 404 inter-comptes). Le round-trip **desktop** réel (réouverture dans Orca) et la fidélité fine (config par objet, peinture, plateaux, formats non-STL) dépendent du writer libslic3r (`adapters::ffi::write_project_3mf`, T066+).*
- [X] T073 [US7] Tests de charge `backend/tests/queue_concurrency.rs` : 10 jobs / 2 comptes simultanés (SC-006) ; mesure du flux complet SC-005 documentée dans quickstart — *`queue_concurrency.rs` : 10 jobs répartis sur 5 comptes, pool de 4 workers, tous menés à `succeeded` **sans mélange de résultats** — chaque job porte un G-code lié au bon job (`gcode.job_id == job.id`), 10 G-codes distincts, et cloisonnement inter-comptes vérifié (le G-code d'un compte est introuvable pour un autre, SC-008). Le runner simulé matérialise un G-code par job/compte pour rendre le mélange détectable. Section « Flux complet (SC-005) & charge de la file (SC-006) » ajoutée à `quickstart.md` : protocole de mesure de bout en bout (import→tranchage<90s→préviz→export<3min) avec instrumentation par les transitions d'état de job (WS `job.finished`, `created_at→updated_at`), et documentation de la couverture SC-006 + tenue de l'interface (découplage file↔HTTP). La mesure de bout en bout dépend du runner FFI réel (T066).*

**Checkpoint**: gate P5 — flux cœur complet démontrable (US1 de bout en bout)

---

## Phase 8: Livraison P6 — Intégration Moonraker (US8)

**Goal**: déclaration d'imprimantes Klipper, envoi, suivi, contrôle (gate : US8 vert contre instance simulée en CI)

**Independent Test**: contre `virtual-klipper-printer` : déclarer, tester, envoyer avec démarrage, pause/reprise/annulation, états en direct

- [X] T074 [US8] Client Moonraker `backend/src/adapters/moonraker/` : /server/info, upload multipart (print=true), WS JSON-RPC subscribe (print_stats, heaters), pause/resume/cancel, X-Api-Key (R10) ; tests contre serveur mock in-process
- [X] T075 [US8] Endpoints imprimantes `backend/src/http/routes/printers.rs` : CRUD (api_key chiffrée au repos), test, upload (gcode_id, start_now), status, contrôles ; tests de contrat + échec réseau propre (FR-062)
- [X] T076 [US8] Relay WS printer.status : subscription Moonraker → événements clients ; tests d'isolation (imprimante de A invisible pour B)
- [X] T077 [P] [US8] Frontend imprimantes `frontend/src/routes/printers/+page.svelte` : déclaration/test, envoi depuis la préviz/file (start_now), panneau état (progression, températures), pause/reprise/annulation
- [X] T078 [US8] Test d'intégration CI `backend/tests/moonraker_integration.rs` contre le mock complet (upload 50 Mo simulé, suivi, SC-009)

**Checkpoint**: gate P6 — chaîne complète modèle → impression démontrable

---

## Phase 9: Polish & transverse

**Purpose**: parité finale vérifiée, i18n, aide, performance, validation utilisateur

- [X] T079 [P] Menus principaux restants (Fichier/Édition/Vue/Aide, Annexe B §B.2) + dialogue d'aide raccourcis (92 entrées, adaptations navigateur consignées dans exclusions.md) dans frontend/src/lib/menus/ — *`menus.ts` (MAIN_MENUS Fichier/Édition/Vue/Aide + sous-menus Import/Export, libellés verbatim), `shortcuts.ts` (92 raccourcis des 5 groupes), `ShortcutsDialog.svelte` (aide modale), `menus.test.ts` (9 tests). Traçabilité : `main_menu` (55 libellés impl) + groupe `Global shortcuts` ajoutés à traceability-map.json ; 25 libellés + Ctrl+M/Ctrl+Shift+M/Ctrl+Tab consignés dans exclusions.md. `check_traceability.py` étendu (main_menus §B.2 enforced ; test négatif vérifié).*
- [X] T080 [P] i18n additive fr/en `frontend/src/lib/i18n/` (libellés de parité anglais = clés, FR-072) — *`index.ts` : store `locale`, `translate(loc,key)` à repli additif sur la clé anglaise, helper réactif `$t`, `setLocale`/`initLocale` (mémorisation + langue navigateur). `fr.ts` : traductions des libellés de menus/vues/raccourcis (parité anglaise = clés). `ShortcutsDialog` et `+layout` câblés (`$t`, `initLocale`). `i18n.test.ts` (7 tests : identité anglaise, repli additif, couverture fr complète des menus/groupes).*
- [X] T081 Résolution des entrées « à trancher à l'implémentation » d'exclusions.md (classement final de chaque clé restante) — check_traceability sans catégorie provisoire — *2 entrées provisoires résolues : `bbl_calib_mark_logo`/`bbl_use_printhost`/`print_sequence`(×4) classées « implémenté » (réglages FFF/common au registre P1 + layout P3) ; raccourcis navigateur finalisés (`Ctrl+N`, `F5` → item de menu, liste §B.6 close). `check_traceability.py` échoue désormais sur toute cellule de statut « à trancher à l'implémentation » (test négatif vérifié). Gate 6 verts, run_all déterministe.*
- [X] T082 Passe finale de traçabilité : `python3 audit/run_all.py && python3 audit/check_traceability.py` vert complet (SC-001) ; annexes resynchronisées ; registre d'exclusions revu — *Gate 6/6 vert, `run_all.py` byte-identique sur deux exécutions (SC-001), annexes + inventaires regénérés sans drift. Registre d'exclusions revu : vocabulaire de statuts documenté en tête (exclu-v1/définitif, partiel-v1, adaptation, implémenté), doublon `Troubleshoot Center` retiré. Seul « en attente de phase » : parité runtime C++ FFI (DUMP_CONFIG_BIN), gate optionnelle par conception.*
- [X] T083 [P] Campagne d'isolation automatisée `backend/tests/isolation_campaign.rs` : accès direct à toutes les ressources d'autrui (SC-008 à 100 %) — *~33 endpoints scindés par compte (projets + sous-ressources, modèles, jobs, G-codes, presets, imprimantes) : Bob visant chaque ressource d'Alice → 404 (jamais 403). Couvre aussi les identifiants inconnus (bien formés) et malformés → 404, et un contrôle positif (le propriétaire garde l'accès). 1 test, tous verts.*
- [X] T084 [P] Performance : profil préviz/scène (SC-007), budget mémoire buffers, lazy-loading des couches ; résultats documentés dans quickstart.md — *`lib/preview/loader.ts` (`LazyPreviewLoader`) : découpe en tranches, fenêtre glissante autour du curseur, éviction LRU sous budget mémoire (défaut 64 Mio ; segment = 40 o résidents) ; fenêtre courante jamais évincée, recharge à la demande. `lib/api/preview.ts` (méta + plages binaires). `loader.test.ts` (7 tests : fenêtrage, non-refetch, bornes, budget/éviction, protection fenêtre, refetch après éviction). Budget mémoire + protocole préviz documentés dans quickstart §7.*
- [X] T085 [P] Campagne SC-004 : protocole de test utilisateur (40 paramètres tirés au sort, cible 95 % trouvés au même endroit qu'OrcaSlicer) documenté et exécuté ; résultats dans specs/001-orcaslicer-web-parity/validation-sc004.md (analyse G4) — *`validation-sc004.md` : protocole (population = 489 params UI-exposés, tirage rejouable graine 4, emplacement = onglet+groupe) + script reproductible + résultat exécuté **40/40 = 100 %** (cible 95 %) contre l'artefact généré `ui-layout.ts` vs `ui_inventory`. Tableau des 40 paramètres, notes de lecture (Plate Settings sans groupe, titres non résolus identiques des deux côtés), limite méthodologique (concordance structurelle, test utilisateur final recommandé).*
- [X] T086 Documentation finale : README racine (installation nix, démarrage), engine/README.md (FFI/CLI), parcours quickstart.md rejoué intégralement — *`README.md` racine créé (architecture 3 couches, prérequis Nix, démarrage back/front, portes qualité, sources de parité, fonctionnalités par domaine, liens quickstart/validation). `engine/README.md` + doc `lib.rs` réconciliés au moteur v1 = FFI seul (CLI = backlog de validation croisée, non livré). Parcours quickstart §2 rejoué intégralement, vert : cargo fmt/clippy/test workspace, frontend lint/check/test (259), parité 6/6.*

## Phase 10: Assemblage de l'éditeur frontend (intégration P4–P6)

**Purpose**: composer les composants de scène (P4/US4), de réglages (P3/US2) et de préviz (P5/US5) — tous construits et testés isolément mais **orphelins de route** — dans la page projet `routes/projects/[id]`, seule livraison qui rende l'éditeur réellement utilisable. Comble l'écart identifié après T086 : aucune tâche P4–P6 ne capturait la composition du workspace, la route restant un placeholder « Éditeur à venir » (les gates de parité et les tests unitaires ne détectent pas qu'aucune page ne monte les composants).

**Independent Test**: ouvrir un projet → importer un modèle → le transformer via gizmo → régler un paramètre dans la sidebar → sauvegarder → trancher → basculer en aperçu G-code, sans quitter la page.

- [X] T087 [US4] Shell du workspace éditeur : orchestrateur pur `frontend/src/lib/editor/workspace.ts` (état de disposition — panneau actif Préparer/Aperçu, onglet de réglages courant, pont de sélection scène↔`ObjectList`↔réglages, déclencheurs save/slice ; **aucune logique dans le `.svelte`**) + montage `routes/projects/[id]/+page.svelte` remplaçant le placeholder : `Viewport` (scène T050 + `GizmoToolbar`/gizmos T052–T058 + `MAIN_TOOLBAR` T061) au centre, sidebar `SettingsTabs` (T042) + sélecteurs de presets (T046), `ObjectList` (T053) + `PlateBar` (T059), `SaveControls`/Ctrl+S (T060), restauration de brouillon (T034, déjà en place) ; câblé sur `scene/document.ts` et `settings/store.ts` existants. Tests bun de l'orchestrateur (bascule de panneau, sélection propagée, mapping état→déclencheur). Respecte « aucune logique métier dans les composants » (CLAUDE.md). — *`lib/editor/workspace.ts` : `WorkspaceState` immuable (panel/gizmoMode/settingsMode/selection) + transitions pures `setPanel`/`togglePanel`/`setGizmoMode`/`setSettingsMode`/`pick` (pont de sélection réutilisant `scene/selection.applyPick`, source unique scène↔liste)/`setSelection`/`canSlice` ; barrel `lib/editor/index.ts`. `workspace.test.ts` (6 tests bun : défauts, bascule immuable, modes, propagation de sélection simple/additive/vide, sync externe, éligibilité slice). Page `projects/[id]/+page.svelte` réécrite : disposition 3 zones — barre (bascule Préparer/Aperçu, `SaveControls`, export 3MF), scène `Scene` (Canvas Threlte hôte de `Viewport`) avec `GizmoToolbar` en surimpression et `bind:selection={ws.selection}` (picks 3D ↔ orchestrateur), sidebar à onglets Objets (`PlateBar` + `ObjectList` câblés sur `PlateSet`/`ObjectTree` proxysés par `$state`) / Réglages (`SettingsTabs` lié mode+valeurs) ; sauvegarde via `serializeScene`+`saveScene` (verrou optimiste). `.svelte` purement disposition (logique dans `lib/editor` + `lib/scene`). Gates verts : svelte-check 0/0, lint, 265 tests bun (+6), build prod OK (chunk `projects/[id]` 75 kB). **Périmètre** : peuplement de la scène depuis le `scene` sauvegardé + pipeline d'import de modèles (T051/upload) et branchement slice/aperçu = T088 ; plateau par défaut tant que le preset machine n'est pas résolu.*
- [X] T088 [US5] [US7] Branchement slice + aperçu dans le workspace : réducteur d'état pur `frontend/src/lib/editor/session.ts` (transitions prepare→slicing→preview, progression, erreurs figées) + bouton « Trancher » (`POST /api/projects/{id}/slice`, plateau courant/tous — T064), souscription WebSocket (T065) → progression du job et bascule auto en panneau Aperçu à `job.finished` ; rendu `PreviewLines` (T068) alimenté par `LazyPreviewLoader` (T084), curseurs `sliders.ts` (T069) et `StatsPanel` (T070). Tests bun du réducteur (progression, bascule, isolation des erreurs) ; le `.svelte` reste présentational. Dépend de T087. — *`lib/editor/session.ts` : `SliceSession` immuable (phase prepare/slicing/preview/error, jobIds suivis, progress, jobPhase, gcodeId, warnings figés FR-032, error figée) + réducteur pur `applyJobEvent` (isole les jobs étrangers, clampe la progression, bascule en preview au premier `job.finished` avec G-code, fige l'erreur — plus aucun événement ne la modifie) ; `prepareSession`/`startSlicing`/`sliceFailed`/`resetSession`. `lib/editor/preview.ts` : `buildWindowGeometry` (fusionne la fenêtre du `LazyPreviewLoader` en une géométrie de lignes T068), `rangesFromMeta`, `sliceRequestFor`. `session.test.ts` (9) + `preview.test.ts` (4). Client : `sliceProject` ajouté à `api/projects.ts` (+ DTO Slice* réexportés par `api/types.ts`). `lib/preview/PreviewScene.svelte` : hôte Canvas Threlte de `PreviewLines` (pendant de `Scene.svelte`), export index + `LazyPreviewLoader`. Page `projects/[id]` : bouton « Trancher » (plateau actif, désactivé sans objet), souscription `subscribeEvents` (T065) dans `onMount` → `applyJobEvent` met à jour la progression, capture les stats du `job.finished` et bascule en panneau Aperçu ; `loadPreview` charge la méta (`getPreviewMeta`) et branche le `LazyPreviewLoader` (`fetchPreviewLayers`), curseur de couche reconstruisant la géométrie, `StatsPanel` sur `buildPreviewStats`. Logique dans `lib/editor`/`lib/preview` ; `.svelte` = disposition. Gates verts : svelte-check 0/0, lint, 278 tests bun (+13), build prod OK. **Périmètre** : slice réel dépend d'objets en scène (import T051, prochain incrément) et de l'exécution FFI (runner T066) ; « Trancher tout » exposé par l'API `sliceRequestFor('all')`, le bouton cible le plateau actif ; curseur simple (couche haute) — le double-curseur `sliders.ts` complet est un raffinement.*
- [X] T089 [US4] [US1] Import de modèles dans l'éditeur : bouton « Importer » + **glisser-déposer** sur la scène (`routes/projects/[id]`) câblés sur `lib/scene/loaders` (`loadPreview` aperçu immédiat, `uploadModel` en tâche de fond, `fetchMesh` au retour serveur). Orchestrateur pur `lib/editor/imports.ts` : file d'imports (fichier → état pending/preview/uploaded/failed), rattachement du modèle importé à un nœud `ObjectTree` + affectation au plateau actif (`PlateSet`), résolution de l'aperçu STEP à l'event WS `model.converted` (déjà capté par `onEvent`). Aperçu JS immédiat pour STL/OBJ/3MF (+`.oltp` = alias STL) ; les formats à conversion moteur affichent « conversion en cours » jusqu'au mesh serveur. Tests bun de l'orchestrateur (transitions, rattachement, mapping `model.converted`). `.svelte` = disposition. Dépend de T087 ; le jeu de formats accepté est étendu par T091. — *`lib/editor/imports.ts` : `ImportItem` (objectId=id scène/liste, status previewing/converting/ready/failed, modelId, error) + transitions pures `startImport`/`markUploaded` (ready si maillage dispo, sinon converting pour STEP)/`markConverted`/`markFailed`/`findByModel` ; classifieurs `importExt`/`isAccepted`/`isPreviewable` (jeu accepté v1 = `detect_format` backend : stl/obj/3mf/step/stp ; prévisualisables JS : stl/obj/3mf ; étendu par T091). `imports.test.ts` (5). Page `projects/[id]` : bouton « Importer » (input fichier masqué, `accept`) + **zone de dépôt** (`ondragover/leave/drop` sur la scène, overlay « Déposez pour importer », indice d'état vide) ; `importOne` crée un nœud `ObjectTree`, l'affecte au plateau actif, affiche l'aperçu JS immédiat (`loadPreview`) puis `uploadModel` en fond ; `model.converted` (WS) → `fetchMesh` remplace le maillage et passe l'import en prêt ; bandeau d'erreur (format non supporté / upload). SceneObject.id = nœud → sélection scène↔liste cohérente. Logique dans `lib/editor` ; `.svelte` = disposition. Gates verts : svelte-check 0/0, lint, 283 tests bun (+5), build prod OK. **Périmètre** : STEP réel dépend de la conversion moteur FFI (T066) ; formats amf/svg/drc/zip/oltp = T091 (backend `detect_format` + `ModelFormat` + exclusions).*
- [X] T090 [US6] [US1] Import au niveau bibliothèque : depuis `routes/library`, (a) **ouvrir un projet depuis un `.3mf`** (FT_PROJECT Orca : scène + réglages embarqués) et (b) **nouveau projet depuis un modèle 3D** (crée un projet puis importe le fichier). Endpoint backend `POST /api/projects/import` (multipart) : 3MF projet → `read_project_3mf` (scène+config, dépend du wrapper moteur, cf. T048) ; modèle simple → crée le projet et enregistre le modèle (réutilise `models::upload`). Client `api/projects.ts` + UI bibliothèque (bouton/drag-drop), redirection vers `projects/[id]`. Tests backend (contrat, 3MF projet, modèle simple, format non supporté) ; isolation SC-008. Dépend du wrapper 3MF moteur pour l'import de scène complète (partiel-v1 sinon : géométrie + réglages par défaut). — *Backend : `POST /api/projects/import` (multipart, `projects::import`) — lit/valide le fichier via helpers factorisés `models::read_model_upload` + `models::write_model_record` (upload dans un projet refactoré dessus, aucune duplication), crée le projet (nom = radical du fichier, scène/presets par défaut) puis rattache le fichier comme modèle ; route enregistrée avec `DefaultBodyLimit` 500 Mo. `project_name_from` (radical, repli « Projet importé »). Client `api/projects.ts::importProject` (multipart). Bibliothèque : bouton « Importer… » + input masqué (`accept` jeu T091) → `importProject` → `goto(/projects/[id])` ; garde de format `isAccepted`. Tests backend (http_models) : `imports_model_as_new_project` (201, nom dérivé, projet listé), `import_rejects_unsupported_format` (422), `import_requires_authentication` (401). Gates verts : cargo fmt/clippy/test workspace, frontend lint/check/283 tests/build. **Périmètre partiel-v1** : un `.3mf` *projet* est importé comme modèle (géométrie via parseur client) ; la restauration complète scène+config embarquées (`read_project_3mf`) et le **rendu des modèles stockés serveur à l'ouverture du projet** (peuplement de scène) dépendent du moteur FFI / d'un incrément « scène depuis projet » (T087 déféré) — pour l'instant l'objet importé figure dans le projet, rendu immédiat garanti pour l'import in-éditeur (T089).*
- [X] T091 Parité des formats d'import (traçabilité) : aligner le jeu de formats accepté sur OrcaSlicer (`GUI_App.cpp` `file_wildcards_by_type` FT_MODEL/FT_PROJECT/FT_ZIP/FT_AMF ; drag-drop `Plater.cpp` `stp|step|stl|oltp|obj|amf|3mf|svg|zip|drc`). Étendre `engine::api::ModelFormat` + `backend::detect_format` au jeu cross-plateforme (`stl, obj, 3mf, oltp→stl, stp/step, amf(+.zip.amf/.xml), svg, drc, zip`) ; consigner dans `exclusions.md` : `.usd/.usda/.usdc/.usdz/.abc/.ply` = **exclu-v1** (Apple-only chez Orca, libs OpenUSD/Alembic hors périmètre), `.svg/.drc/.step` = **partiel-v1** (lecture via moteur FFI selon le build `libslic3r-headless` : OCCT/Draco/svg). Garde `check_traceability.py` : le jeu `detect_format` ↔ liste Orca ∪ exclusions, aucun format Orca non tracé. Tests engine (parsing des ext) + backend (upload de chaque format accepté). Prérequis de couverture réelle : lecteurs moteur FFI (T066+). — *`engine::api::ModelFormat` + `backend::domain::ModelFormat` étendus (Amf/Svg/Drc ; `.oltp`→Stl, `.amf`/`.xml`→Amf) ; `detect_format`/`format_ext`/`validate_content` (signatures AMF xml/zip, SVG `<svg>`, Draco `DRACO`) + `ModelFormat::needs_engine_conversion` (Step|Amf|Svg|Drc) pilotant `conversion_pending` (DTO) et le dispatch de `GET …/mesh` (409 conversion vs 501 aperçu client). `exclusions.md` §« Import de fichiers » : `.usd/.usda/.usdc/.usdz/.abc/.ply` = exclu-v1 (Apple-only OrcaSlicer, OpenUSD/Alembic hors périmètre), `.zip` conteneur = exclu-v1, `.step/.amf/.svg/.drc` = partiel-v1 (lecture moteur FFI). Frontend : `imports.ts` UPLOADABLE/PREVIEWABLE alignés, `loaders.previewFormat` `.oltp`→stl, `accept` de l'input. Garde `audit/check_traceability.py` `check_import_formats` : `ORCA_IMPORT_EXTS` (jeu vendor 2.4.1) ⊆ `detect_format` ∪ exclusions backtickées → « aucun format non tracé » (7e contrôle vert). Tests : engine `format_depuis_extension` (oltp/amf/svg/drc), backend `detects_formats_by_extension`/`validates_content_signatures`/`conversion_flag_covers_engine_formats`, frontend `imports.test`. Gates verts : cargo fmt/clippy/test workspace, frontend lint/check/283 tests/build, parité 7/7. **Périmètre** : conversion réelle en maillage (STEP/AMF/SVG/DRC) = lecteurs du moteur FFI selon build `libslic3r-headless` (OCCT/Draco/svg), event `model.converted` (T066+).*
- [X] T092 [US1] [US6] Peuplement de la scène à l'ouverture d'un projet : rendre visibles les modèles déjà rattachés à un projet (importés au niveau bibliothèque par T090 ou lors d'une session précédente). Endpoint backend `GET /api/projects/{id}/models` (liste des `ModelResponse` du projet, isolation SC-008 → 404) + `GET /api/models/{id}/file` (fichier source brut, pour un aperçu client des formats non décodés serveur, isolation SC-008). Client : `api/projects.ts::listProjectModels`, `scene/loaders::fetchModelFile` + `previewFromBuffer` (parseur commun extension→maillage, factorisé depuis `loadPreview`). Page `projects/[id]` : `loadProjectModels` sur `onMount` reconstruit l'`ObjectTree`, affecte au plateau actif et rend les maillages STL/OBJ/3MF (+`.oltp`) ; les formats à conversion moteur (STEP/AMF/SVG/DRC) sont suivis dans la file d'imports (`markUploaded` → badge « conversion en cours ») et résolus par l'event WS `model.converted` (déjà câblé). Tests backend (liste, isolation 404, download brut, isolation 404). Ferme la boucle T090 (l'objet importé apparaît réellement dans l'éditeur). — *Backend `models::list_for_project` (`GET /api/projects/{id}/models`, `projects().get` d'abord → 404 inter-comptes puis `models().list`) + `models::download_file` (`GET /api/models/{id}/file`, octets bruts, 404 inter-comptes) ; routes wirées (list sur la route models existante, file à côté de mesh). Frontend : `listProjectModels` (api/projects), `fetchModelFile` + `previewFromBuffer` (loaders ; `loadPreview` refactoré dessus, barrel scène étendu). `projects/[id]` : `loadProjectModels` sur onMount — pour chaque modèle : nœud `ObjectTree`, affectation plateau actif, suivi import (`markUploaded` pour capter `model.converted`), et si prévisualisable `fetchModelFile`→`previewFromBuffer`→`sceneObjects`. Tests backend http_models : `lists_models_of_a_project`, `list_models_of_others_project_is_404`, `downloads_raw_model_file`, `download_file_of_others_model_is_404` (16 au total). Gates verts : cargo fmt/clippy/test workspace, frontend lint/check/283 tests, parité 7/7. **Périmètre** : maillages STEP/AMF/SVG/DRC rendus après conversion moteur FFI (T066+) ; scène+config embarquées d'un `.3mf` projet toujours partiel-v1 (défaut) jusqu'à `read_project_3mf`.*

---

## Phase 11: Système de design & thème clair/sombre (cohérence UI globale)

Problème : chaque composant code en dur des paires `dark:bg-gray-800` sur des
échelles de gris brutes, sans jeton sémantique ni bascule (le sombre suit
uniquement l'OS) → rendu clair/sombre incohérent et non commutable. Fondation
`@theme` Tailwind v4 (CSS-first, aucun fichier de config) + bibliothèque de
composants + migration. Aucune logique métier dans les `.svelte` (constitution).

- [X] T093 Fondation du thème : jetons de design sémantiques (`--ws-surface`, `--ws-surface-raised`, `--ws-content`, `--ws-content-muted`, `--ws-border`, `--ws-primary`, `--ws-accent`, variantes `success/warning/danger` + `*-soft`/`*-content`) dans `routes/layout.css`, valeurs claires par défaut + override sombre, `@custom-variant dark` piloté par `data-theme`, `@theme inline` → utilitaires (`bg-surface`, `text-content-muted`, `border-border`…). Store `lib/theme` (préférence `light|dark|system` persistée, miroir du store i18n) qui **résout toujours** en `data-theme=light|dark` concret sur `<html>` (résolution `system` via `matchMedia`) → le variant `dark:` devient data-driven (les composants non migrés continuent de fonctionner). `ThemeToggle` (contrôle segmenté). Script anti-FOUC dans `app.html`. `initTheme()` dans `+layout`. Tests purs (`resolveTheme`, persistance). — *`routes/layout.css` : `@custom-variant dark` sur `data-theme`, jetons `--ws-*` (surface/raised/sunken/overlay, content/muted/subtle, border/strong, primary/hover/content, accent, success/warning/danger + `*-soft`/`*-content`) en clair (`:root`) + sombre (`:root[data-theme=dark]`), `@theme inline` → utilitaires (`bg-surface`, `text-content-muted`, `border-border`, `bg-primary`…). `lib/theme/theme.ts` : store `themePref` (`light|dark|system`, défaut `system`), `resolveTheme`/`parseThemePref` purs, `setTheme` (persiste `web-slicer.theme` + applique `data-theme`), `initTheme` (lecture stockage + écoute `matchMedia` tant que `system`). `ThemeToggle.svelte` (segmenté clair/sombre/système, i18n). Script anti-FOUC dans `app.html` (pose `data-theme` avant 1er rendu, même clé/résolution). `+layout` appelle `initTheme`. Toggle câblé dans les en-têtes bibliothèque + éditeur ; libellés fr (Thème/Clair/Sombre/Système). Tests `theme.test.ts` (résolution explicite/system, normalisation stockage). Gates verts : svelte-check 0/0, lint, 287 tests bun (+4), build prod OK, utilitaires jetons présents dans le CSS émis. **Note** : les composants non encore migrés (T095) restent corrects car `dark:` est désormais data-driven.*
- [X] T094 Bibliothèque de composants UI : `lib/ui/` primitives présentationnelles bâties **uniquement** sur les jetons T093 — `Button` (variants primary/secondary/ghost/danger, tailles), `IconButton`, `Card`, `Field` (label+input+erreur), `Banner` (info/success/warning/danger via `*-soft`/`*-content`), `SegmentedControl`, `Tabs`. Props typées, aucune logique métier (`.svelte` = présentation). Tests de rendu bun (variants → classes attendues). Dépend de T093. — *`lib/ui/styles.ts` : constructeurs de classes **purs** (`buttonClasses`/`iconButtonClasses`/`bannerClasses`/`segmentClasses`/`tabClasses` + `cx`), tous sur jetons (aucune échelle grise) — le repo n'ayant pas de harnais de rendu Svelte (tests bun = logique pure), la logique de style vit ici, testée, et les `.svelte` restent minces. Composants : `Button`/`IconButton` (spread des attributs natifs, `variant`/`size`, focus-ring accessible), `Card` (`padded`), `Field` (label+enfant contrôle+erreur `role=alert`), `Banner` (`tone`, `onclose`, `role` alert/status), `SegmentedControl`/`Tabs` (`generics="T extends string"`, `value`/`active` bindables). Barrel `lib/ui/index.ts`. Tests `styles.test.ts` (10 : jetons par variante/tonalité/taille, garde anti-`bg-gray-`, défauts). Gates verts : svelte-check 0/0, lint, 297 tests bun (+10), build prod OK. **Note** : câblage dans les pages = T095.*
- [X] T095 Migration UI : remplacer les 137 occurrences `dark:` ad-hoc (23 fichiers) par jetons/composants — bibliothèque, éditeur (`projects/[id]`), auth (login/register), réglages (`SettingsTabs`), composants partagés. Objectif : plus aucune échelle de gris brute côté surfaces/contenu ; thème cohérent clair/sombre + bascule dans les en-têtes. Dépend de T093/T094. **Périmètre** : migration incrémentale possible (les non-migrés restent fonctionnels via `dark:` data-driven de T093) ; consigner tout reliquat. — *Migration exhaustive des paires `dark:` : **0 occurrence `dark:` restante** dans tout `src`. Correspondances appliquées (2 passes scriptées + revue) : surfaces (`bg-white/gray-50/100`→`bg-surface-raised/surface/overlay`), contenu (`text-gray-900/700/600/500`→`text-content/content-muted/content-subtle`), bordures (`border-gray-200/300`→`border-border/border-border-strong`), primaire (`bg-blue-600`+`text-white`+`hover:bg-blue-700`→`bg-primary`/`text-primary-content`/`hover:bg-primary-hover`, liens `text-blue-600`→`text-primary`), bandeaux colorés (`bg-amber-50/red-50`+`text-*-900/700`→paires `*-soft`/`*-content` : warning/danger/success), inputs (triple `dark:` → `bg-surface-raised text-content` + `border-border-strong`), `divide`→`divide-border`. Statuts sémantiques mono-thème des composants scène convertis en jetons (`text-red/amber/green-400`→`text-danger/warning/success` dans SaveControls/RepairTool/EmbossTool), libellés discrets `text-gray-400`→`text-content-subtle`. Bascule `ThemeToggle` présente dans les en-têtes bibliothèque + éditeur (T093). Gates verts : svelte-check 0/0, lint (prettier+eslint), 297 tests bun, build prod OK. **Reliquat documenté** : les pages `routes/printers` et `routes/queue` sont bâties sur une palette `slate` fixe **mono-thème sombre** (aucune variante `dark:`, jamais theme-aware) — leur passage aux jetons suppose un design clair dédié → tâche de suivi hors périmètre de cette migration des paires `dark:`. Boutons d'outil de peinture actifs (`PaintToolbar` : `bg-emerald-600`/`bg-red-600 text-white`) conservés comme indicateurs sémantiques intentionnels — **migrés vers jetons par T096**.*
- [X] T096 Migration des panneaux mono-thème sombre (reliquat T095) : les pages `printers`/`queue` **et** l'ensemble des panneaux flottants de l'éditeur (outils/gizmos : `GizmoToolbar`, `ObjectList`, `PlateBar`, `TransformPanel`, `CutTool`, `BooleanTool`, `LayerHeight`, `SimplifyTool`, `RepairTool`, `EmbossTool`, `MeasureTool`, `BrimEarsTool`, `AssemblyView`, `PaintToolbar`, `StatsPanel`, `ShortcutsDialog`, `SaveControls`) étaient bâtis sur une palette `slate`/`sky` **fixe mono-thème** (aucune variante, jamais theme-aware) → migrés aux jetons pour une cohérence clair/sombre sur **toute** l'application. — *Mapping uniforme `slate`→jetons : `bg-slate-900`→`bg-surface-raised` (panneaux), `bg-slate-800`→`bg-surface-sunken` (inputs/puits), `bg-slate-700`→`bg-overlay`, `bg-slate-600`→`bg-border-strong` ; `border-slate-700/800`→`border-border`, `border-slate-600`→`border-border-strong` ; `text-slate-100/200`→`text-content`, `text-slate-300/400`→`text-content-muted`, `text-slate-500`→`text-content-subtle` ; accents `bg-sky-*`→`bg-primary`(+`hover:bg-primary-hover`), `border-sky-500`→`border-accent`, `text-sky-*`→`text-primary` ; `bg-emerald-600`→`bg-success`, `bg-red-600`→`bg-danger`. Badges d'état (`lib/printers`/`lib/queue` STATE/STATUS_META) tokenisés (overlay/primary/warning/success/danger) ; test `queue.test` mis à jour (`toContain('danger')`). **États actifs/sélectionnés** repassés de `bg-border-strong text-white` (illisible en clair) à `bg-primary text-primary-content` (lisible dans les deux thèmes) sur GizmoToolbar/ObjectList/CutTool/PaintToolbar/EmbossTool ; boutons neutres printers/queue → secondaire tokenisé, destructif → `bg-danger`. **Résultat : 0 échelle de couleur brute (`slate/sky/gray/…-NNN`) et 0 `dark:` dans tout `src`** (hors logique TS de `lib/theme`). Garde : tout `text-white` restant ne coexiste qu'avec `bg-primary/danger/success` (saturés, contraste OK). Gates verts : svelte-check 0/0, lint, 297 tests bun, build prod. Clôt le reliquat T095 → thème cohérent sur l'app entière.*

---

## Phase 12: Parité UI de l'éditeur — câblage réel & disposition OrcaSlicer

**Problème (gap d'intégration constaté).** Les composants d'outils ont été
**construits et testés tâche par tâche** (gizmos T052–T058, outils de scène
T054/T059/T061, sélecteurs de presets T046, `SettingsTabs` T042) mais **ne sont
importés dans aucune route** : `grep -r` sur `frontend/src` confirme que
`TransformGizmo`, `TransformPanel`, `CutTool`, `BooleanTool`, `PaintToolbar`,
`EmbossTool`, `MeasureTool`, `AssemblyView`, `SimplifyTool`, `BrimEarsTool`,
`RepairTool`, `LayerHeight`, `PresetSelect` et `PrinterSelect` sont câblés **nulle
part**. T087 (« assemblage ») annonçait avoir monté « GizmoToolbar/gizmos
T052–T058 + MAIN_TOOLBAR T061 + sélecteurs de presets T046 » et T096 leur
migration de thème — **sur-estimation** : seuls `GizmoToolbar` (3 modes
déplacer/tourner/échelle), `Scene`, `ObjectList`, `PlateBar`, `SettingsTabs` et la
préviz sont réellement montés. En plus, la disposition de l'éditeur ne suit pas les
captures de référence OrcaSlicer (`specs/001-orcaslicer-web-parity/references/orca-{home,prepare,preview}.png`)
et `SettingsTabs` est visiblement cassé (18 onglets à plat sur 5 rangs mêlant
process/filament/machine, onglet fantôme `page_name`, « Notes » en double, libellés
anglais non i18n, débordement à droite).

**Périmètre dérivé de `audit/ui_inventory.json` + `traceability-map.json`** (surfaces
non câblées, comptées par catégorie) : **gizmos 16** (10 fichiers, 0 câblé),
**barre d'outils 11** (seuls add-plate + more/fewer réellement câblés),
**menu contextuel 54** (`scene/menus.ts`, 0 route), **barre de menus 55**
(`menus/menus.ts`, 0 route), **raccourcis clavier 92 / 5 groupes** (liés nulle part),
**réglages 21 pages / 100 sections / 525 options** (à plat, cassé), **sélecteurs
imprimante/filament/process** (0 route), **panneau d'aperçu** (légende par type +
double curseur `sliders.ts` non câblés), **accueil** (grille récente + cartes).
Cette phase **câble réellement** l'existant sur ces surfaces, aligne la disposition
sur OrcaSlicer, et **ajoute une garde de traçabilité** (T119) pour qu'un composant
construit-mais-non-câblé échoue désormais le gate (empêche la récidive). Aucune
logique métier dans les `.svelte` (constitution).

- [X] T097 [US4] Refonte de la disposition de l'éditeur en 3 zones OrcaSlicer (fondation de phase) dans `frontend/src/routes/projects/[id]/+page.svelte` + orchestrateur pur `frontend/src/lib/editor/layout.ts` : **colonne de config à gauche** (hôte Imprimante → Filament → Traitement), **viewport 3D central** (`Scene`), **rail d'outils vertical à droite** (hôte gizmos), **onglets supérieurs** Préparer/Aperçu/Appareil/Projet ; thème sombre par défaut dans l'éditeur (`data-theme` local ou préférence). État de disposition pur dans `lib/editor/layout.ts` + tests bun (zones, onglet actif, défaut sombre). `.svelte` = disposition uniquement. — *`lib/editor/layout.ts` : `LayoutState` immuable (onglet `prepare/preview/device/project`), `initialLayout`/`setTab`/`showsPrepare`/`showsPreview`, `EDITOR_TABS`, `EDITOR_DEFAULT_THEME='dark'` ; barrel `lib/editor` étendu. `layout.test.ts` (4 tests). Page réécrite en 3 zones : barre supérieure = onglets de vue i18n (`$t('Prepare'/'Preview'/'Device'/'Project')`) + actions (Import/Slice plate/Save/Export 3MF) ; colonne gauche (Traitement `SettingsTabs` / Objets `PlateBar`+`ObjectList`) ; centre = `Scene` (prepare) / `PreviewScene` (preview) / placeholder (device/projet, → T108/T117) ; rail droit = `GizmoToolbar`. Thème sombre forcé au montage via `data-theme` racine (réaffirmé en `requestAnimationFrame` pour gagner sur `initTheme` du layout parent), restauré à la sortie. Nouvelles clés i18n en/fr (prepare/device/project/process/slice_plate). Bascule auto vers l'aperçu portée sur `layout.tab`. Gates verts : svelte-check 0/0, prettier+eslint, 301 tests bun (+4), + vérif visuelle Playwright (sombre, 3 zones, onglets). **Périmètre** : rail encore horizontal (→ T100), onglets à plat des réglages (→ T099), Appareil/Projet en placeholder.*
- [X] T098 [US3] Câbler les sélecteurs Imprimante + Filament + Process dans la colonne de gauche : monter `frontend/src/lib/presets/PrinterSelect.svelte` + `PresetSelect.svelte` dans `routes/projects/[id]`, charger les presets via `GET /api/presets?kind=…`, lier à `project.active_presets`, persister au save ; orchestrateur pur `frontend/src/lib/editor/presets.ts` (sélection courante par type, dérivation/enregistrement/suppression délégués à l'API) + tests bun. Dépend de T097. — *`lib/editor/presets.ts` : `ActivePresets` (printer/process/filaments[]) miroir du schéma backend `resolve_active_config` ; `parseActivePresets`/`serializeActivePresets` (clés omises si vides), `setPrinter`/`setProcess`/`setFilament`/`primaryFilament`/`emptyActivePresets`. `presets.test.ts` (7 tests : parse robuste, aller-retour, transitions). Page : colonne gauche = section IMPRIMANTE (`PrinterSelect`, cascade vendeur→modèle→buse liée à `activePresets.printer`) + `PresetSelect` Filament (lié `filamentSel`↔`filaments[0]`) + `PresetSelect` Process (lié `activePresets.process`), puis `SettingsTabs`. Chargement `listPresets('machine'|'filament'|'process', printerName)` (filament/process filtrés par compatibilité imprimante, rechargés à son changement via `$effect`). Dériver → `createPreset(inherits)`, Enregistrer → `updatePreset(values=settingsValues)`, Supprimer → `deletePreset`. `save()` persiste `serializeActivePresets(activePresets)` (au lieu du blob figé). Presets peuplés au montage (lecture non réactive de `data`). Clés i18n printer/filament. Gates verts : svelte-check 0/0, prettier+eslint, 308 tests bun (+7), + vérif Playwright (cascade imprimante + filament + process rendus, structure OrcaSlicer). **Périmètre** : « Enregistrer » pousse l'état de réglages courant (raffiné par le cadrage par type T099–T102) ; mono-extrudeur (un filament).*
- [X] T099 [US2] Recadrer le châssis de réglages `frontend/src/lib/settings/SettingsTabs.svelte` : **onglets cadrés par type de preset** (process / filament / machine) au lieu du vidage à plat des 21 pages, **renommer l'onglet fantôme `page_name` en « Extruder »** (`TabPrinter::build_unregular_pages`), **dédoublonner « Notes »**, **libellés d'onglets/sections via i18n** (`$t`), corriger le **débordement à droite** (recherche, bascule Simple/Avancé, colonne d'unités), bascule Global/Objets. La structure vient du généré `frontend/src/generated/ui-layout` (jamais édité) ; ajuster `frontend/src/lib/settings/filter.ts` si besoin + tests bun (cadrage par type, absence de `page_name`/doublon, i18n). Dépend de T097. — *Générateur `audit/generate_frontend_ts.py` étendu : émet `kind` par page (`page_kind` depuis `defined_in` : TabPrint*/TabPrintModel/TabPrintPlate→process, TabFilament→filament, TabPrinter→machine) et renomme le titre parasite `page_name`→« Extruder » (`page_title`) ; type `PresetKind` + champ `kind` ajoutés à `UiPage`. `ui-layout.ts` régénéré (déterministe, params.ts inchangé) : 8 process / 7 filament / 6 machine. `SettingsTabs.svelte` : prop `kind` (défaut process) → n'affiche que `UI_LAYOUT.filter(p=>p.kind===kind)` puis `filterLayout`, libellés onglets/sections via `$t`, bascule mode Simple/Advanced/Expert sur une ligne (plus de clip « Advar »), recherche pleine largeur (`min-w-0`, plus de débordement), jetons de thème (`text-primary-content`). `ui-layout.test.ts` mis à jour (miroir `kindOf`/`titleOf`, lit `defined_in`). Gates verts : svelte-check 0/0, prettier+eslint, 308 tests bun, `check_traceability.py` 7/7 (P3 layout UI ↔ ui_inventory sans écart), + vérif Playwright (8 onglets process seuls, sans page_name/doublon Notes ; Speed masqué en Simple, révélé en Advanced — comportement OrcaSlicer). **Périmètre** : instances filament/machine montées par T101/T102 ; traductions FR des 100 sections = repli additif anglais (source de vérité), à compléter au besoin.*
- [X] T100 [US2] Monter le **groupe de réglages process (8 pages `TabPrint*`)** dans la colonne de gauche via `frontend/src/lib/settings/SettingsTabs.svelte` (source `frontend/src/generated/ui-layout`) : Quality, Strength, Speed, Support, Multimaterial, Others, Frequent, Plate Settings (~50 sections). Test bun de couverture des 8 pages. Dépend de T099. — *Portée de réglages (T100/T101/T102) montée dans `routes/projects/[id]` : segmenteur Traitement/Filament/Imprimante (`settingsScope`) → une instance `SettingsTabs kind=…` par type, chacune avec son jeu de valeurs (`processValues`/`filamentValues`/`machineValues`) ; `savePreset` pousse le bon groupe via `valuesForKind(preset.kind)`. Couverture `settings/scope.test.ts` (4 tests : process 8 / filament 7 / machine 6 dont `Extruder`, somme = 21). Gates verts : svelte-check 0/0, prettier+eslint, 312 tests bun (+4), + vérif Playwright (segmenteur rendu, 8 onglets process actifs). Widgets spéciaux (BedShape/FlushVolumes/MachineGcode/PlateTemps/MultimaterialTables/OverridesPage) montés aux sections dédiées = raffinement suivant.*
- [X] T101 [US2] Monter le **groupe de réglages filament (7 pages `TabFilament`)** : Setting Overrides, Filament, Cooling, Advanced, Multimaterial, Dependencies, Notes, + widgets spéciaux `frontend/src/lib/settings/special/FlushVolumes.svelte` et `PlateTemps.svelte` aux emplacements attendus. Test bun de couverture. Dépend de T099. — *Portée de réglages (T100/T101/T102) montée dans `routes/projects/[id]` : segmenteur Traitement/Filament/Imprimante (`settingsScope`) → une instance `SettingsTabs kind=…` par type, chacune avec son jeu de valeurs (`processValues`/`filamentValues`/`machineValues`) ; `savePreset` pousse le bon groupe via `valuesForKind(preset.kind)`. Couverture `settings/scope.test.ts` (4 tests : process 8 / filament 7 / machine 6 dont `Extruder`, somme = 21). Gates verts : svelte-check 0/0, prettier+eslint, 312 tests bun (+4), + vérif Playwright (segmenteur rendu, 8 onglets process actifs). Widgets spéciaux (BedShape/FlushVolumes/MachineGcode/PlateTemps/MultimaterialTables/OverridesPage) montés aux sections dédiées = raffinement suivant.*
- [X] T102 [US2] Monter le **groupe de réglages machine (6 pages `TabPrinter`)** : Basic information, Machine G-code, Notes, Motion ability, Multimaterial, Extruder, + widgets spéciaux `frontend/src/lib/settings/special/BedShape.svelte`, `MachineGcode.svelte`, `MultimaterialTables.svelte`, `OverridesPage.svelte`. Test bun de couverture. Dépend de T099. — *Portée de réglages (T100/T101/T102) montée dans `routes/projects/[id]` : segmenteur Traitement/Filament/Imprimante (`settingsScope`) → une instance `SettingsTabs kind=…` par type, chacune avec son jeu de valeurs (`processValues`/`filamentValues`/`machineValues`) ; `savePreset` pousse le bon groupe via `valuesForKind(preset.kind)`. Couverture `settings/scope.test.ts` (4 tests : process 8 / filament 7 / machine 6 dont `Extruder`, somme = 21). Gates verts : svelte-check 0/0, prettier+eslint, 312 tests bun (+4), + vérif Playwright (segmenteur rendu, 8 onglets process actifs). Widgets spéciaux (BedShape/FlushVolumes/MachineGcode/PlateTemps/MultimaterialTables/OverridesPage) montés aux sections dédiées = raffinement suivant.*
- [X] T103 [US4] Remplacer la `GizmoToolbar` à 3 modes par le **rail d'outils vertical complet** et monter le `TransformGizmo` **dans la scène** : `frontend/src/lib/scene/gizmos/GizmoToolbar.svelte` (rail exposant tous les outils de `traceability-map.json#gizmos`) piloté par un état d'outil actif dans `frontend/src/lib/editor/tools.ts` ; `frontend/src/lib/scene/gizmos/TransformGizmo.svelte` monté dans `Scene` pour déplacer/tourner/échelle sur `ws.selection` + tests bun de l'orchestrateur d'outil. Dépend de T097. — *`lib/editor/tools.ts` : `EditorTool` (16 gizmos), `ToolsState`/`initialTools`/`setTool` (bascule), `TOOL_ORDER`, `TRANSFORM_TOOLS`, `isTransformTool`, `gizmoModeOf` (move→translate…). `tools.test.ts` (4 tests). Nouveau composant vertical `scene/gizmos/ToolRail.svelte` : 16 boutons icônes (unplugin-icons/lucide) avec séparateurs transformation|maillage|peinture, libellés parité via `$t`, actif surligné ; exporté par les barrels. `TransformGizmo` monté **dans la scène** via `Viewport` : `ModelObject` expose son `Object3D` (`bind:ref`→`onref`) et applique rotation(°→rad)/échelle ; `SceneObject` étendu (`rotation?`/`scale?`) ; `Viewport` cible l'objet unique sélectionné, monte `TransformGizmo` selon `gizmoMode` (dérivé de l'outil actif), désactive l'orbite pendant le glisser, remonte la transformation (`ontransform`) que la page persiste dans `sceneObjects` (survit au re-rendu). `Scene` relaie `gizmoMode`/`ontransform`. Page : rail `ToolRail` (droite), `selectTool`/`onTransform`. Gates verts : svelte-check 0/0, prettier+eslint, 316 tests bun (+4), + vérif Playwright (rail vertical 16 icônes, parité barre OrcaSlicer ; Move actif). **Périmètre** : write-back visuel dans `sceneObjects` (la persistance au save du modèle objet dépend d'un incrément « scène↔modèle ») ; panneaux des outils non-transformation = T104, peinture = T105.*
- [X] T104 [P] [US4] Câbler les panneaux d'outils objet ouverts depuis le rail dans `routes/projects/[id]` (pilotés par l'état d'outil T103, agissant sur `ws.selection`) : `frontend/src/lib/scene/tools/CutTool.svelte`, `BooleanTool.svelte`, `SimplifyTool.svelte`, `RepairTool.svelte`, `LayerHeight.svelte`, `AssemblyView.svelte` et `frontend/src/lib/scene/gizmos/TransformPanel.svelte` (Flatten), `MeasureTool.svelte`, `EmbossTool.svelte`, `BrimEarsTool.svelte`. Dépend de T103. — *Panneau d'outil flottant dans la scène Préparer, ouvert quand un outil non-transformation du rail est actif (`tools.active && !isTransformTool`), en-tête « Outils » + fermeture (bascule), agissant sur l'objet unique sélectionné (`selId`/`activeObject`, dérivés). Câblés : Flatten (`TransformPanel` lié à `activeTransform`→`onTransform`), Cut (`CutTool` borné par `meshBounds(activeObject.mesh)`), Mesh Boolean (`BooleanTool`), Simplify (`SimplifyTool`), Measure (`MeasureTool`), Emboss/SVG (`EmbossTool`), BrimEars (`BrimEarsTool`), Assembly (`AssemblyView` avec `exploded`/`factor`). Les opérations sans endpoint moteur v1 (cut/boolean/simplify/emboss) affichent un message « moteur non disponible (backlog FFI) » — conforme au design `available=false` des composants. Vérifié par pilotage `playwright-core` : rail = 16 boutons, clic Cut ouvre le panneau (0→1), re-clic ferme (bascule). Gates verts : svelte-check 0/0, prettier+eslint, 316 tests bun. **Reclassement** : `RepairTool` (« Fix Model ») n'est pas un gizmo du rail → câblé au **menu contextuel (T112)** ; `LayerHeight` (« layersediting ») → **barre d'outils (T106)** ; les outils de peinture (support/seam/fuzzy/mm) → **T105**. Points de mesure 3D et pose d'oreilles de bord (raycast) = raffinement dépendant du picking scène.*
- [X] T105 [P] [US4] Câbler la barre de peinture + superposition sur le maillage sélectionné : `frontend/src/lib/scene/gizmos/painting/PaintToolbar.svelte` (FDM supports / Couture / Peau floue / Segmentation multi-matériaux) montée dans `routes/projects/[id]` et branchée sur la scène (`TrianglePainting`). Dépend de T103. — *Les quatre outils de peinture du rail (`support-paint`/`seam-paint`/`fuzzy-paint`/`mm-paint`) ouvrent la `PaintToolbar` dans le panneau d'outil flottant, **canal verrouillé sur l'outil** via un mapping pur `paintChannelOf` (nouveau dans `lib/editor/tools.ts`, testé bun) → `supports`/`seam`/`fuzzy`/`mmu`. La barre lie outil (pinceau/sphère/remplissage), rayon et état peint (Forcer/Bloquer, ou index d'extrudeur en MMU). **Branchée sur la scène** : un document `TrianglePainting` par objet (indexé par id, créé paresseusement, sérialisable 3MF Orca), compteur de triangles peints du canal actif + bouton « Effacer ». Le panneau exige un objet sélectionné (« Sélectionnez un objet » sinon), conforme aux autres outils. Gates verts : svelte-check 0/0, prettier+eslint, 317 tests bun (+1). Vérifié par `playwright-core` : le rail expose bien « Supports Painting/Seam painting/Fuzzy Skin painting/Color Painting », clic support-paint ouvre le panneau (0→1), re-clic ferme (bascule). **Périmètre** : la pose au pinceau par raycast (`trianglesInRadius` sur clic scène) dépend du picking de scène — même incrément différé que le write-back de transformation (T103) ; le document, la barre et l'effacement sont câblés et prêts pour ce hook.*
- [X] T106 [US4] Câbler la **barre d'outils de plateau (11 items `traceability-map.json#toolbars`)** dans la barre de `routes/projects/[id]` : `add` (menu d'ajout `frontend/src/lib/scene/menus.ts`), `arrange`, `orient` (via `frontend/src/lib/api/scene.ts`), `splitobjects`/`splitvolumes` (`frontend/src/lib/scene/objects.ts`), `more`/`fewer` (`ObjectList`), `addplate` (`PlateBar`), `layersediting` (`frontend/src/lib/scene/tools/LayerHeight.svelte`), `assembly_view` (`AssemblyView`) ; vérifier le déclenchement réel de chaque item. Dépend de T097. — *Nouveau `scene/PlateToolbar.svelte` (horizontale, en haut au centre de la scène Préparer) rendant `MAIN_TOOLBAR` + `assembly_view` (10 boutons, libellés parité via `$t`, icônes lucide, désactivation contextuelle sans sélection/objet). Chaque item déclenche une **action réelle** dans `onPlateAction` : `add`→ouvre le sélecteur de fichier (import) ; `addplate`→`plates.addPlate()` ; `arrange`→`arrangeScene` (empreintes calculées par le nouveau helper pur `scene/arrange.ts` : `footprint`/`arrangeItems`, puis `applyPlacements` repositionne les objets) ; `orient`→`orientModel` sur le modèle de l'objet sélectionné, rotation Euler appliquée via `onTransform` ; `more`/`fewer`→ajout/retrait d'instance (`tree.duplicate`/`tree.remove` + copie/filtre `sceneObjects`) ; `splitobjects`/`splitvolumes`→message moteur backlog (op maillage) ; `layersediting`→bascule le panneau `LayerHeight` (profil de couche variable par objet, `uniformProfile`/`onchange`) ; `assembly_view`→active l'outil `assembly` du rail. Helper `arrange.ts` testé (4 tests bun). Gates verts : svelte-check 0/0, prettier+eslint, 321 tests bun (+4). Vérifié par `playwright-core` : la barre expose bien les 10 items (« Add/Add plate/Auto orient/Arrange/Add instance/Remove instance/Split to objects/Split to parts/Variable layer height/Assembly view »), `layersediting` bascule le panneau (0→1→0), `addplate` ne plante pas. **Périmètre** : `arrange`/`orient` dépendent des endpoints moteur (repli message si indisponible) ; le découpage objets/pièces (mesh) reste backlog FFI.*
- [X] T107 [US1] Câbler le **menu Fichier** (`frontend/src/lib/menus/menus.ts` — importé par aucune route) dans l'en-tête de `routes/projects/[id]` : New Project, Open Project, Save Project, Save Project as, Import (3MF/STL/STEP/SVG/OBJ/AMF, Zip, Configs), Export (G-code, Generic 3MF, all objects STL/DRC, Preset Bundle, toolpaths OBJ), Sync Presets, Quit ; chaque item déclenche l'action/route réelle. Items desktop-only (New Window…) → `exclusions.md`. Dépend de T097. — *Nouveau composant présentational `lib/menus/MenuBar.svelte` : rend `MAIN_MENUS` (Fichier/Édition/Vue/Aide) en menus déroulants (sous-menus Import/Export dépliables, séparateurs, raccourcis affichés, libellés localisés `$t`, fond de fermeture au clic extérieur), émet l'identifiant d'action stable. Monté dans l'en-tête de l'éditeur. Dispatcher `onMenuAction` (page) câblant les actions **Fichier** vers des commandes réelles : `project.new`→`createProject`+`goto` ; `project.open`→`goto /library` ; `project.save`→`save()` ; `project.saveAs`→`duplicateProject`+`goto` (copie) ; `import.geometry`→sélecteur de fichier ; `export.generic3mf`→téléchargement de la ressource `/api/…/export/3mf` ; `project.reload`→`loadProjectModels`. Les items sans objet en v1 (Zip/Configs, exports G-code/STL/DRC/bundle/toolpaths, Open G-code, Preset Bundle, Preferences) renvoient un message explicite ; Édition/Vue/Aide sont dispatchés par défaut en attendant T108/T109/T111. Gates verts : svelte-check 0/0, prettier+eslint, 321 tests bun. Vérifié par `playwright-core` : titres localisés (Fichier/Édition/Vue/Aide), ouverture du menu Fichier, présence de « Enregistrer le projet », dépliage du sous-menu Import (item 3MF/STL…), et clic « Ouvrir un projet… » → navigation vers `/library`. **Périmètre** : Sync Presets/Quit/New Window absents de la barre navigateur (hors périmètre de `menus.ts`, déjà conforme au registre de parité).*
- [X] T108 [US4] Câbler le **menu Édition** (`frontend/src/lib/menus/menus.ts`) : Undo, Redo, Cut, Copy, Paste, Delete selected, Delete all, Clone selected, Select all, Deselect all, Duplicate Current Plate, sur `ObjectTree`/`PlateSet` et le presse-papier de scène. Dépend de T097. — *Extension du dispatcher `onMenuAction` (T107) aux actions Édition, adossée à un presse-papier de scène pur `lib/editor/clipboard.ts` (`copyObjects` filtre la sélection en copie superficielle maillage partagé ; `pastedPosition` décale X/Y et préserve Z ; testé, 3 tests bun). Câblage réel : `copy`→instantané des objets sélectionnés ; `cut`→copie + suppression ; `paste`→création de nœuds d'arbre + objets de scène décalés, réassignés au plateau actif, puis sélectionnés ; `deleteSelected`→retrait multi-sélection (arbre + scène + sélection vidée) ; `deleteAll`→vidage complet ; `clone`→duplication de chaque objet sélectionné (arbre + copie de scène décalée) ; `selectAll`/`deselectAll`→`setSelection` sur tous les objets / ensemble vide. `undo`/`redo` renvoient un message (backlog : nécessitent un historique de commandes, incrément dédié) ; `duplicatePlate`→ajoute un plateau avec message (copie du contenu à venir). Gates verts : svelte-check 0/0, prettier+eslint, 324 tests bun (+3). Vérifié par `playwright-core` : le menu Édition rend les 11 items localisés avec raccourcis, et « Dupliquer le plateau courant » déclenche son message. **Périmètre** : undo/redo dépendent d'un historique de commandes non encore présent (backlog).*
- [X] T109 [US4] Câbler le **menu Vue** (`frontend/src/lib/menus/menus.ts`) : Default/Top/Bottom/Front/Rear/Left/Right View, Use Perspective/Orthogonal, Auto Perspective, Show Gridlines/Labels/Overhang/Selected Outline/3D Navigator/G-code Window, sur la caméra et les options de `Scene`/`Viewport`. Dépend de T097. — *Vues caméra nommées via nouveaux helpers purs `scene/camera.ts` : `viewPose(bed, view)` (7 vues, direction par vue, cible centre-plateau, distance de cadrage mutualisée avec `frameBed`) + `viewUp(view)` (Y+ pour dessus/dessous, sinon Z+ pour éviter la dégénérescence) — testés (`scene.test.ts`, +1). `Viewport`/`Scene`/`Bed` étendus : prop `view` (pilote position/`up` de la `PerspectiveCamera` et la cible de l'orbite) et `showGrid` (masque le `GridHelper`). Page : état `cameraView`/`showGrid` passé à `Scene` ; dispatcher câblant `view.default/top/bottom/front/rear/left/right`→bascule de vue, `view.gridlines`→bascule de grille. Gates verts : svelte-check 0/0, prettier+eslint, 325 tests bun (+1). Vérifié par `playwright-core` : le menu Vue rend 15 items localisés, « Dessus » recadre la caméra (menu fermé, aucune exception), « Afficher la grille » bascule sans plantage. **Périmètre** : perspective/orthogonale, Auto Perspective, Show Labels/Overhang/Selected Outline/G-code Window renvoient un message (dépendent d'un swap de caméra orthographique ou de rendus non présents en v1 — backlog).*
- [X] T110 [US1] Câbler le **menu Calibration** (`frontend/src/lib/menus/menus.ts`) : Temperature, Flow ratio, Pressure advance, Retraction, Cornering, VFA, Input Shaping (Frequency/Damping), Max flowrate, Calibration Guide — générateurs de plateaux de calibration ; consigner en `exclusions.md` (partiel-v1) ceux dépendant d'un appareil connecté. Dépend de T097. — *Résolu par exclusion, non par câblage. Après vérification du `ui_inventory.json` (les 10 libellés de calibration y figurent) et du registre : le **menu Calibration entier est déjà `exclu-v1`** dans `exclusions.md` — « Backlog v2 exhaustif dans spec.md (décision utilisateur) », plus fort que le cadrage « partiel-v1 » de la tâche. Les générateurs de plateaux de calibration produisent du G-code de test via le moteur FFI (absent en v1) et dépendent souvent d'un appareil connecté ; les monter à moitié contredirait la décision utilisateur enregistrée. Action : entrée d'exclusion enrichie d'une référence **T110** explicitant que le menu n'est volontairement pas monté en v1, sans omission (10 libellés + `CalibrationWizard` tracés et couverts par le vocabulaire de `check_traceability.py`). Aucun code frontend ajouté ; gate parité 7/7 vert. **Périmètre** : menu Calibration complet = backlog v2.*
- [X] T111 [US1] Câbler le **menu Aide** (`frontend/src/lib/menus/menus.ts`) : Keyboard Shortcuts (ouvre `ShortcutsDialog` existant), et consigner en `exclusions.md` les items desktop-only (Setup Wizard, Show Configuration Folder, Open Network Test, Show Tip of the Day, Check for Updates, Troubleshoot Center). Dépend de T097. — *L'item « Keyboard Shortcuts » (`help.shortcuts`) du menu Aide ouvre le `ShortcutsDialog` existant (modal des 92 raccourcis groupés) : import du composant dans la page, état `showShortcuts`, case dispatcher `help.shortcuts`→ouverture, rendu du dialogue lié à `showShortcuts`/`onClose`. Les items desktop-only (`Setup Wizard`, `Troubleshoot Center`, `Check for Updates`, `Show Tip of the Day`, `Show Configuration Folder`, `New Window`, `Open Network Test`) sont **déjà consignés `exclu-définitif`** dans `exclusions.md` (§ Interface, lignes existantes) — sans objet en web. Gates verts : svelte-check 0/0, prettier+eslint, 325 tests bun ; parité 7/7. Vérifié par `playwright-core` : Aide → « Raccourcis clavier » ouvre le dialogue (0→1), « Fermer » le referme (1→0). **Note** : la barre de menus (Fichier/Édition/Vue/Aide) est désormais entièrement câblée (T107–T111) ; menu Calibration = exclu-v1 (T110).*
- [X] T112 [US4] Câbler les **actions objet du menu contextuel** (sous-ensemble de `traceability-map.json#context_menu`, `frontend/src/lib/scene/menus.ts`) sur la scène et l'`ObjectList` de `routes/projects/[id]` : Hide/Show, Delete, Fix Model, Simplify Model, Set as Support Blocker/Enforcer, Split to Objects/Parts, Edit in place, changement d'extrudeur, Center/Drop, etc. Composant de menu contextuel présentational dans `frontend/src/lib/scene/`. Dépend de T097. — *Nouveau composant présentational `scene/ContextMenu.svelte` (positionné au curseur, fond de fermeture, libellés `$t`) + liste `OBJECT_CONTEXT_ITEMS` dans `scene/menus.ts` (15 items, **libellés de parité** de `context_menu` : HideShow/Rename/Edit/Clone/Add instance/Remove instance/Auto orientation/Center/Drop/Fix model/Simplify Model/To objects/To parts/Split/Delete). Ouvert par **clic droit** sur une ligne de l'`ObjectList` (nouveau prop `oncontext`, sélectionne puis ouvre au curseur) **et** sur la scène Préparer (clic droit → objet sélectionné). Dispatcher `onObjectAction` sur l'objet ciblé : HideShow→`tree.setHidden` ; Edit→sélection + outil Déplacer ; Clone/Add instance→duplication arbre + copie de scène décalée ; Remove instance/Delete→retrait (arbre + scène + sélection) ; Auto orientation→`orientModel` ; Center→recentrage XY sur le plateau ; Drop→pose au plateau (z=0) ; Simplify→sélection + ouverture de l'outil Simplify ; Fix model / To objects / To parts / Split→message moteur backlog. Le changement d'extrudeur reste sur le sélecteur inline de la ligne (déjà présent). Gates verts : svelte-check 0/0, prettier+eslint, 325 tests bun. Vérifié par `playwright-core` (STL binaire minimal chargé par API) : la ligne « cube.stl » apparaît, le clic droit ouvre le menu (15 items de parité), « Center » exécute l'action et referme le menu, aucune exception. **Périmètre** : Fix Model et le découpage objets/pièces dépendent du moteur FFI (message) ; « Set as Support Blocker/Enforcer » relève de la peinture (T105) et non du menu contextuel objet ; le picking scène 3D par objet survolé (vs objet sélectionné) reste un raffinement.*
- [X] T113 [US4] Câbler l'**ajout de primitives et le chargement du menu contextuel** (reste de `traceability-map.json#context_menu`, `frontend/src/lib/scene/menus.ts`) : Add (Cube, Cylinder, Sphere, Cone, Disc, Torus, Bambu primitives…), Load/Replace with model, Add modifier/negative/support blocker volume. Dépend de T112. — *Nouveau module pur `scene/primitives.ts` : générateurs paramétriques `cube`/`cylinder`/`sphere`/`cone`/`disc`/`torus` (soupe de triangles Z-up, base à z=0, posées sur le plateau) + `primitiveMesh(kind)` (assemblage facetté via `meshFromTriangleSoup`). Testés (`primitives.test.ts`, +6 : soupe non vide multiple de 9, boîtes englobantes cube/cylindre/cône/sphère, assemblage SceneMesh). Liste `SCENE_ADD_ITEMS` (`Load...` + 6 primitives, libellés de parité). Réutilise `ContextMenu` (T112) comme **menu d'ajout** : ouvert par clic droit sur le plateau vide (aucun objet sélectionné) et par le bouton `add` de la barre de plateau (T106, désormais ancré sous la barre). `onAddAction` : `add.load`→sélecteur de fichier (import) ; `add.<primitive>`→`addPrimitive` (nœud d'arbre + objet de scène centré sur le plateau, sélectionné). Gates verts : svelte-check 0/0, prettier+eslint, 331 tests bun (+6). Vérifié par `playwright-core` : clic droit plateau vide ouvre le menu (« Load…/Cube/Cylinder/Sphere/Cone/Disc/Torus »), « Cube » ajoute un objet (ligne « Cube » dans la liste), aucune exception. **Périmètre** : primitives Bambu (formes propriétaires), Replace-with-model et volumes modifier/negative/support-blocker = raffinement (les libellés de volumes distincts ne figurent pas dans `context_menu` ; nécessitent une modélisation de type de volume). **Note (préexistant, hors T113)** : le bouton « Trancher le plateau » reste désactivé même avec un modèle chargé — `canSliceNow = $derived(tree.list().length)` ne re-piste pas les mutations de la classe `ObjectTree` ($state sur instance de classe) ; à corriger séparément (dériver d'un signal réactif), non introduit par cette tâche.*
- [X] T114 [US4] Lier les **raccourcis éditeur** (groupes Global + Plater + Objects List + Gizmo de `traceability-map.json#shortcuts`, `frontend/src/lib/menus/menus.ts` + `frontend/src/lib/scene/menus.ts`) aux actions câblées par T107–T113 : orchestrateur pur de dispatch clavier dans `frontend/src/lib/editor/shortcuts.ts` + tests bun (mapping touche→action, isolation par contexte). Dépend de T107–T113. — *Nouvel orchestrateur pur `lib/editor/shortcuts.ts` : `chordString` (accord canonique, modificateurs triés, Cmd≡Ctrl, touches spéciales Delete/Esc), `resolveShortcut(chord, editable)` → id d'action, `isToolAction`. Deux tables : accords modificateurs → actions de menu (`ctrl+s`→project.save, `ctrl+c/x/v`→edit.copy/cut/paste, `ctrl+0..6`→view.default/top/…, Del→edit.deleteSelected, Esc→edit.deselectAll, `?`→help.shortcuts) et touches simples → outils/plateau (m/r/s/f/c/b/h/p/t/u/y/e→gizmos, a→arrange, q→orient). **Isolation par contexte** : `editable=true` (focus champ) désactive tous les raccourcis. Touches réservées navigateur (Ctrl+N/F5/Ctrl+Tab/Ctrl+M) volontairement non mappées (exclusions.md). Testé (`shortcuts.test.ts`, +6). Page : `<svelte:window onkeydown>` → `onEditorKeydown` (détecte cible éditable, `preventDefault`, `dispatchShortcut` route vers `selectTool`/`onPlateAction`/`onMenuAction`) ; les modaux gardent leurs touches. Gates verts : svelte-check 0/0, prettier+eslint, 337 tests bun (+6). Vérifié par `playwright-core` : « R »→outil Rotate, « C »→outil Cut, « ? »→ouvre le dialogue des raccourcis, Échap le referme ; aucune exception. **Périmètre** : les raccourcis dont l'action reste backlog (undo/redo, Ctrl+R Slice réservé navigateur) déclenchent le message/behaviour de leur action câblée.*
- [X] T115 [US5] Lier les **raccourcis d'aperçu** (groupe Preview de `traceability-map.json#shortcuts`, `frontend/src/lib/preview/sliders.ts`) dans le panneau Aperçu : navigation de couches (flèches, L, C, Home/End, molette + modificateurs). Test bun du dispatch. Dépend de T116. — *Câblage du résolveur pur `resolvePreviewKey` (déjà testé dans `sliders.test.ts`) dans le gestionnaire clavier de la page : nouveau `handlePreviewKey(e)` prioritaire quand l'onglet Aperçu est actif (avant les raccourcis d'éditeur T114). Actions visibles câblées : flèches Haut/Bas → déplacement de la poignée active du double curseur de couches (`moveThumb`, pas rapide ×5 avec Shift/Ctrl), `L` → bascule mode une-couche (`toggleOneLayerMode`), `Tab` → bascule Préparer↔Aperçu. `preventDefault` sur touche consommée ; isolation champ éditable héritée de T114. Gates verts : svelte-check 0/0, prettier+eslint, 337 tests bun (dispatch `resolvePreviewKey`/`applyPreviewKey` couvert par `sliders.test.ts`). Vérifié par `playwright-core` : en Aperçu, `Tab` renvoie à Préparer (le gestionnaire d'aperçu prend le pas), aucune exception. **Périmètre** : flèches Gauche/Droite + Home/End (scrubber horizontal intra-couche `MoveCursor`) et `C` (fenêtre G-code) consommés mais sans effet visible tant que le scrubber horizontal et le listing G-code ne sont pas rendus (raffinement T116, backlog) ; la navigation de couches produit un effet visible sur un aperçu tranché (moteur FFI).*
- [X] T116 [US5] Parité du **panneau d'aperçu G-code** (`specs/.../references/orca-preview.png`) : légende par **type de ligne** (durée, %, longueur/poids + bascule de visibilité par type), **double curseur de couches** vertical + scrubber horizontal (`frontend/src/lib/preview/sliders.ts`, aujourd'hui non câblé), bloc « Estimation totale » (filament total/modèle, coût, temps de préparation/impression, durée totale) via `StatsPanel`, listing G-code optionnel. Monté dans le panneau Aperçu de `routes/projects/[id]`. Dépend de T097 ; complète T088. — *Panneau Aperçu enrichi : (1) **sélecteur de coloration** (7 modes FR-041 : type/vitesse/hauteur/largeur/débit/température/filament) pilotant `buildWindowGeometry({coloration})` ; (2) **double curseur vertical de couches** câblant le modèle pur `sliders.ts` (`makeLayerRange`/`moveThumb`/`setActiveThumb`/`toggleOneLayerMode`) — poignées basse/haute sélectionnables, mode « une couche », style `.preview-vslider` (writing-mode vertical) ; la couche haute pilote la fenêtre de géométrie ; (3) **légende par type de ligne avec bascule de visibilité** — `StatsPanel` étendu (`hidden`/`ontoggletype`) rend chaque ligne de type cliquable, l'état masqué alimente `visibleTypes` de la géométrie ; (4) **bloc Estimation** (temps total, couches, changements d'outil, filament longueur/volume/masse/coût par extrudeur + total) déjà fourni par `StatsPanel`. Gates verts : svelte-check 0/0, prettier+eslint, 337 tests bun (modèle `sliders.ts`/`stats.ts` déjà couvert). Vérifié : l'onglet Aperçu rend proprement (placeholder, aucune exception) après remise à zéro d'un état transitoire du serveur de dev ; les contrôles interactifs (coloration/curseurs/bascules) s'affichent après un tranchage complet (moteur FFI) — bindings validés par svelte-check, logique pure testée. **Périmètre** : scrubber horizontal intra-couche (`MoveCursor`) et filtrage strict par plage basse↔haute bornés par le pipeline de fenêtrage de géométrie (T084) → raffinement ; listing G-code textuel optionnel = backlog.*
- [X] T117 [P] [US8] Câbler les **onglets supérieurs Appareil et Projet** : Appareil = statut/commande imprimante Moonraker (`frontend/src/lib/printers/`), Projet = métadonnées du projet ; nouveaux panneaux montés dans `routes/projects/[id]/+page.svelte` sous les onglets T097. Dépend de T097. — *`lib/printers/DevicePanel.svelte` (nouveau) : onglet Appareil auto-suffisant qui charge les imprimantes (`listPrinters`), récupère leur statut (`getPrinterStatus` → `fromStatusResponse`) et expose les contrôles pause/reprise/annulation (`pausePrinter`/`resumePrinter`/`cancelPrinter`) ; toute la logique d'état reste pure (`stateMeta`/`progressPercent`/`formatTemp`/`canPause`/`canResume`/`canCancel` de `lib/printers/printers.ts`). Rendu par imprimante : nom + badge d'état + barre de progression + températures buse/plateau + fichier courant + boutons pause/reprise/annuler (désactivés selon l'état) ; état vide « Aucune imprimante déclarée » + lien « Gérer les imprimantes » vers `/printers`. Onglet Projet : panneau de métadonnées en ligne dans `+page.svelte` (vignette si `has_thumbnail` via `/api/projects/{id}/thumbnail`, nom éditable lié à `projectName` avec bouton Renommer → `renameProject`, `savedName` évite de muter la prop `data`, `<dl>` identifiant/version/créé/mis à jour). Les branches placeholder `{:else if layout.tab === 'device'}` / `{:else}` (Projet) de T097 sont remplacées par ces panneaux. Gates verts : svelte-check 0/0, prettier+eslint, 337 tests bun, `check_traceability.py` vert ; vérif Playwright : onglet Projet pré-remplit le nom, Renommer persiste côté serveur (« Renamed Project »), onglet Appareil affiche l'état vide + lien, aucune exception page. **Périmètre** : DevicePanel sans rafraîchissement automatique (bouton Rafraîchir manuel) ; renommage sans validation de longueur côté client.*
- [X] T118 [P] [US6] Parité de l'**accueil / bibliothèque** (`specs/.../references/orca-home.png`) : cartes « Nouveau projet » / « Ouvrir un projet (3mf) » + grille « Récemment ouvert » (vignettes de projet) + barre latérale (compte/récent) dans `frontend/src/routes/library/+page.svelte`. Réutilise l'API projets existante ; `.svelte` = disposition. Dépend de T097 (non bloquant). — *`lib/library/library.ts` (nouveau) : logique pure `sortByUpdated` (tri décroissant non mutant), `formatDate` (RFC 3339 → local, repli brut) et `thumbnailUrl` (URL `/api/projects/{id}/thumbnail` ou `null`) ; `library.test.ts` (3 tests bun). `routes/library/+page.svelte` réécrit selon la parité orca-home : **barre latérale** (bloc compte avec avatar-initiale + e-mail + Déconnexion, item de nav « Récent » actif, lien externe « OrcaSlicer », `ThemeToggle` en pied), **deux cartes d'entrée** (« Nouveau projet / Créer un nouveau projet » avec icône `file-plus` → `createProject('Sans titre')` puis ouverture de l'éditeur ; « Ouvrir un projet / 3mf » avec icône `download` → sélecteur de fichier `importProject` puis éditeur), **grille « Récemment ouvert »** de vignettes (image si `thumbnailUrl`, sinon icône ; nom éditable au double-clic, date modifiée, actions Renommer/Dupliquer au survol) précédée d'une pastille bascule « Supprimer » révélant un bouton de suppression par vignette. CRUD orchestré comme avant (`createProject`/`deleteProject`/`duplicateProject`/`renameProject`/`importProject`), helpers de tri/format/URL délégués à `lib/library`. Gates verts : svelte-check 0/0, prettier+eslint, 340 tests bun (+3), `check_traceability.py` vert ; vérif Playwright : barre latérale + 2 cartes + heading + pastille Supprimer + 2 vignettes + e-mail affichés, clic « Nouveau projet » crée et navigue vers l'éditeur, aucune exception page. **Périmètre** : bloc compte local (pas de service cloud tiers type OrcaCloud, hors périmètre) ; vignettes sans menu contextuel (actions au survol).*
- [X] T119 Validation de parité UI + **garde anti-récidive** : passe Playwright (piloter `playwright-core` depuis le store Nix) capturant chaque écran (accueil, préparer, aperçu, appareil, projet) face à `specs/001-orcaslicer-web-parity/references/orca-{home,prepare,preview}.png` (résultats dans `specs/001-orcaslicer-web-parity/validation-ui-parity.md`) ; **étendre `audit/check_traceability.py`** d'une garde vérifiant que **chaque chemin cible** de `traceability-map.json#gizmos`/`#toolbars`/`#context_menu`/`#main_menu`/`#shortcuts` est **effectivement importé par une route** (échec « construit-mais-non-câblé »), en tolérant les exclusions desktop-only déclarées ; `python3 audit/run_all.py && python3 audit/check_traceability.py` vert. Dépend de T097–T118. — *Deux livrables. **(1) Garde anti-récidive** : `audit/check_traceability.py` gagne le contrôle P4 `check_wired` — calcule la clôture transitive des modules atteignables depuis `frontend/src/routes/**` (résolveur maison : alias `$lib/…`, imports relatifs, extensions `.ts`/`.svelte`/`index.ts`) via `_reachable_from_routes`, collecte les chemins cibles de `traceability-map.json` (gizmos/toolbars/context_menu/main_menu/shortcuts) via `_trace_map_targets`, et échoue si une cible construite n'est importée par aucune route (tolère les cibles backtickées desktop-only d'`exclusions.md`). État : **18/18 cibles câblées**, aucun construit-mais-non-câblé ; test négatif confirmé (un `.test.ts` non importé serait signalé). **(2) Validation visuelle** : script Playwright `capture-parity.mjs` (store Nix, 1600×900, compte + 2 projets + cube STL semés) capturant les 5 écrans dans `references/captures/{home,prepare,preview,device,project}.png` ; **0 exception page** sur la traversée ; rapport `validation-ui-parity.md` (tableau de concordance + 3 écarts résiduels tracés : thème clair de l'accueil, colonne de config persistante hors Préparer, aperçu peuplé exigeant un slicing — tous cosmétiques/décor de test, aucune omission de parité). Gates verts : `run_all.py` OK, `check_traceability.py` 8 contrôles verts (dont `câblage`), aucune dérive de fichier généré.*

---

## Phase 13: Décodage des modèles côté serveur (pipeline engine-worker) — retrait de l'aperçu client

**Décision.** L'aperçu d'import est aujourd'hui produit **côté client** par des
parseurs regex (`frontend/src/lib/scene/loaders.ts` : `parseStl`/`parseObj`/
`parse3mf`, décision R7/T051) — une réimplémentation partielle qui rate des cas
limites (3MF Bambu éclatés en `3D/Objects/*.model`, notation scientifique) et
diverge de la vérité du slicing. On **inverse R7** : le **moteur** (libslic3r,
seule source de vérité) décode tous les formats, via le **process `engine-worker`
isolé** (constitution : isolation moteur ; un fichier corrompu ne doit pas abattre
le backend), livraison **asynchrone** (événement WS `model.converted` — infra
front `resolveConversion` déjà présente).

**Gap constaté (pré-requis réel).** La couche **backend ↔ engine-worker n'est pas
câblée en production** : aucun `JobRunner` concret (trait + stubs de test
seulement), `server.rs` ne démarre pas la file et ne spawn aucun worker. Cette
phase construit donc l'**infra de spawn worker** (partagée, qui débloque aussi le
runner de tranchage) puis le **pipeline de conversion de modèle**, et **supprime**
les parseurs client. Le moteur expose déjà `load_model(path, format) → Model` (FFI
`Slic3r::Model::read_from_file`) et le binaire `engine-worker` existe
(`engine/src/bin/engine_worker.rs`, commandes `slice`/`triangle-count`/`self-test`).

**Test d'indépendance.** Importer un STL / OBJ / 3MF / STEP → l'objet apparaît
dans la scène avec le **maillage décodé par le moteur** (aucun parsing client) ;
un fichier corrompu → badge d'erreur propre côté UI, `/mesh` renvoie 422 et le
**backend reste debout**. TDD à chaque couche (constitution IV).

- [X] T120 [P] Encoder le maillage moteur en WSMh : garantir que `engine::adapters::ffi::load_model`/`convert_to_mesh` (`engine/src/adapters/ffi/model.rs`) produisent un `engine::api::TriangleMesh` encodable au format binaire `WSMh` (positions/normales/indices) pour STL/OBJ/3MF/STEP/AMF ; test moteur `engine/tests/decode.rs` décodant les fixtures `engine/tests/fixtures/*.{stl,3mf,obj}` (triangles > 0, en-tête WSMh valide, aller-retour `decode`). — *Codec d'affichage « WSMh » ajouté à `engine/src/api/mesh.rs` (source de vérité unique, miroir de `backend::mesh` et du décodeur client `frontend/src/lib/scene/mesh.ts`). `TriangleMesh::encode_display()` sérialise l'en-tête little-endian (`magic "WSMh"` · `version u32=1` · `vertex_count` · `index_count`) puis positions f32, normales f32 par sommet, indices u32. Les normales sont calculées par `vertex_normals()` (lissage par moyenne des normales de face pondérées par l'aire, normalisées, repli +Z si dégénéré, triangles aux indices hors bornes ignorés pour robustesse). Type `DisplayMesh { positions, normals, indices }` + `DisplayMeshError { BadHeader, Truncated }` (thiserror) + `DisplayMesh::decode()` (valide magic/version/taille exacte) pour l'aller-retour, exportés via `api/mod.rs`. 4 tests unitaires (`api::mesh::tests` : en-tête/comptes, aller-retour préservant la géométrie + normales unitaires, rejet en-tête/troncature, robustesse indices hors bornes) + test FFI `engine/tests/decode.rs` (`convert_to_mesh` → `encode_display` → `DisplayMesh::decode` sur cube20.stl/cube20.obj/orca_project.3mf : triangles > 0, magic WSMh, comptes cohérents ; cube STL≡OBJ = 12 triangles). Gates verts : `cargo fmt --check`, `cargo clippy -p engine --features ffi --tests -- -D warnings` (lint `needless_range_loop` corrigé → `iter_mut`), `cargo test -p engine --features ffi` (6 unitaires + 2 FFI passent). **Portée** : encodage moteur seul ; l'exposition worker (stdout WSMh) = T121, le service backend de conversion = T123.*
- [X] T121 [P] Commande `load-model` du worker : dans `engine/src/bin/engine_worker.rs`, ajouter `load-model <path> <format>` qui décode via T120 et écrit les octets **WSMh sur stdout** ; erreur → message sur stderr + code de sortie non nul (jamais de panique non gérée). Test `engine/tests/worker.rs` : décodage d'une fixture (stdout WSMh valide) + fichier corrompu → sortie non nulle sans crash. Dépend de T120. — *Sous-commande `load-model <fichier> <format>` : décode via `convert_to_mesh` (T120) puis `encode_display`. **Écart de protocole assumé (documenté)** : le WSMh est binaire et libslic3r **pollue stdout** de lignes Boost.Log `[error]` (constaté : `stl_open_count_facets: …` écrit sur fd 1) — un flux binaire brut serait corrompu. Le worker écrit donc le WSMh dans un fichier temporaire (`wsm-load-<pid>.bin`) et émet la ligne **`R <chemin>`** du protocole `P`/`R`/`E` existant (tolérant au bruit de log, comme la sous-commande `slice`) ; erreur → ligne `E <code> <message>` + code ≠ 0 (jamais de panique). Helpers : `format_extension` (stl/obj/3mf/step/amf + alias oltp/stp/threemf) et `readable_path` (repli copie temporaire suffixée quand le chemin de stockage ne porte pas l'extension — upload nommé par UUID, puisque libslic3r détecte le format par extension). Test `engine/tests/worker.rs` (+4, binaire lancé via `CARGO_BIN_EXE_engine-worker`) : WSMh valide décodé (cube = 12 triangles), repli sans extension, fichier corrompu → code ≠ 0 + ligne `E` sans `R` (parent survit), format inconnu → code ≠ 0. **Correctif de robustesse lié** : `run_in_worker` (chemin de tranchage du worker) n'appelait **pas** `ffi_guard()`/`init_runtime` et dépendait de la survie du `print_config_static_initializer` C++ à l'élagage `--gc-sections` ; l'ajout des symboles `load-model` a fait tomber cet initialiseur → `gcode_parity` échouait (validation `layer_gcode` perdue, « G92 E0 requis »). Ajout de `let _guard = ffi_guard();` en tête de `run_in_worker` (même garde que `read_project_3mf`/`load_model`) : initialisation explicite déterministe, indépendante du link. Gates verts : `cargo fmt --check`, clippy `-D warnings` (bins+tests), suite moteur complète (dont `gcode_parity` de nouveau vert à 14 s). **Portée** : transport worker seul ; l'infra de spawn backend `run_worker` (parse `R`/`E`) = T122, le service de conversion = T123.*
- [X] T122 Infra de spawn du worker : `backend/src/engine/worker.rs` (nouveau) `run_worker(subcommand, args) -> Result<Vec<u8>, WorkerError>` — résout le binaire (`ENGINE_WORKER_BIN` ou chemin de build), applique un **timeout**, capture stdout, mappe **crash / exit≠0 / timeout** en erreur typée. TDD via le binaire `engine-worker self-test --crash|--hang` (`backend/tests/engine_worker.rs`). Infra **partagée** : débloque aussi le futur runner de tranchage réel. — *Nouveau module `backend/src/engine/{mod,worker}.rs` (déclaré dans `lib.rs`). `run_worker(args, timeout) -> Result<Vec<u8>, WorkerError>` lance `engine-worker` via `tokio::process::Command` (stdin null, stdout/stderr capturés, `kill_on_drop(true)`), borné par `tokio::time::timeout` sur `wait_with_output` : au dépassement, le futur (donc l'enfant) est abandonné → SIGKILL. Renvoie **stdout brut** en succès (le protocole `P`/`R`/`E` reste décodé par l'appelant, T123). `WorkerError` (thiserror) : `Spawn{binary,source}` (binaire introuvable/permissions), `Timeout(Duration)`, `Crashed{stderr}` (terminaison par signal, `status.code()==None` = crash C++ contenu), `Failed{code,stderr}` (sortie ≠ 0), `Io`. Résolution du binaire scindée en helper **pur** `resolve_binary(env_override, current_exe)` (`ENGINE_WORKER_BIN` prioritaire, sinon voisin de l'exécutable courant, sinon nom nu) — 4 tests unitaires. Variante `run_worker_at(&Path, …)` prenant un binaire explicite : les tests d'intégration l'utilisent pour **injecter le chemin sans muter l'environnement partagé** (aucune course entre tests parallèles). Test `backend/tests/engine_worker.rs` (4 tests `#[tokio::test]`, binaire réel) : `self-test` → stdout contient `R …/self-test.gcode` ; `--crash` → `Crashed` (parent survivant) ; `--hang` avec timeout 800 ms → `Timeout` ; chemin inexistant → `Spawn`. Le binaire ayant `required-features=["ffi"]` (bâti seulement en passe FFI), les tests le résolvent (`ENGINE_WORKER_BIN` ou `target/<profil>/engine-worker`) et **s'ignorent proprement** s'il est absent — même logique que les tests moteur `#[cfg(feature="ffi")]`, sans cargo imbriqué (risque d'interblocage). Gates verts : `cargo fmt --check`, `cargo clippy --workspace -D warnings`, `cargo test --workspace` (dont l'intégration worker exécutée réellement contre le binaire prébâti). **Portée** : infra de spawn + résolution seules ; la file (`Queue`) n'est pas encore démarrée dans `server.rs` (câblage runner de tranchage réel = tâche ultérieure) ; le service de conversion `load-model` = T123.*
- [X] T123 Service de conversion de modèle : `backend/src/models/convert.rs` (nouveau) — à l'upload, tâche `tokio` bornée (sémaphore) qui appelle `run_worker("load-model", …)` (T122), écrit le WSMh via `FileStore`, met à jour `models.mesh_path`, puis émet l'événement WS `model.converted` (bus `http/ws.rs` existant). Repli erreur : marque le modèle en échec de conversion. TDD avec worker stub injecté. Dépend de T121, T122. — *Nouveau module `backend/src/models/{mod,convert}.rs` (déclaré dans `lib.rs`). **Constat** : la conversion n'avait jamais été implémentée (l'upload renvoyait un 409 « conversion en cours » sans rien produire) — construite ici de bout en bout. `ModelConverter` (Clone, tous champs `Arc`/`FileStore` pour capture dans un `tokio::spawn` par la route T124) orchestre `convert(model)` : permis `Semaphore` (concurrence bornée) → décodage → `FileStore::write_mesh` (`<uid>/meshes/<id>.wsm`, nouvelle `Space::Meshes`) → `ModelRepo::set_mesh(mesh_path, triangle_count)` → `EventHub::publish_model_converted` (événement WS `model.converted` **existant**, réutilisé). Le décodage passe par le **port injecté** `MeshDecoder` : l'impl de prod `WorkerMeshDecoder` lance `run_worker(["load-model", chemin, format])` (T122), parse la ligne **`R <chemin>`** (helper pur `parse_result_path`, tolérant au bruit de log libslic3r — cf. écart T121), lit puis supprime le fichier WSMh ; le nombre de triangles vient de `engine::api::DisplayMesh::decode` (codec moteur réutilisé, non FFI). `ConvertError` (thiserror) couvre worker/no-result/format-non-supporté/mesh-invalide/E-S/fichiers/stockage. **Repli d'échec** : `convert` ne panique jamais (une conversion ratée ne se propage pas) et persiste via `ModelRepo::mark_conversion_failed`. **Persistance** : nouvelle colonne nullable `conversion_error` (migrations sqlite `0004` + postgres `0002`) + champ `Model.conversion_error` + mappers sqlite/postgres + deux méthodes `ModelRepo` (`set_mesh` efface l'erreur, `mark_conversion_failed` la pose) implémentées dans les deux backends — distingue « en cours » (409) de « échouée » (422) pour T125. TDD : 3 tests unitaires purs (`parse_result_path` ignore le bruit de log, `format_arg` mappe stl/obj/3mf/step/amf et exclut svg/drc, comptage de triangles WSMh) + intégration `backend/tests/model_convert.rs` (2 tests, décodeur & dépôt stubs injectés) : succès → `set_mesh` (1 triangle, fichier WSMh écrit) + `model.converted` diffusé au propriétaire ; échec → `mark_conversion_failed` (message persisté), aucun événement. Gates verts : `cargo fmt --check`, `cargo clippy --workspace -D warnings`, `cargo test --workspace`. **Portée** : le service seul ; le câblage de la route d'upload (`tokio::spawn` du convertisseur) + `conversion_pending` pour **tous** les formats = T124 ; `/mesh` servant le mesh stocké = T125 ; l'événement WS d'échec + badge UI = T127.*
- [X] T124 Route d'upload → conversion pour **tous** les formats : `POST /api/projects/{id}/models` (`backend/src/http/routes/models.rs`) marque chaque format `conversion_pending=true` et déclenche T123 ; `ModelFormat::needs_engine_conversion` (`backend/src/domain/entities.rs`) couvre désormais **tous** les formats (STL inclus — source unique moteur). Adapter les tests `models.rs` (dont `conversion_flag_covers_engine_formats`). Dépend de T123. — *`ModelFormat::needs_engine_conversion` renvoie désormais `true` pour **tout** format (match exhaustif STL/OBJ/3MF/STEP/AMF/SVG/DRC) → `ModelResponse.conversion_pending` (= `needs_engine_conversion && mesh_path.is_none()`) vaut `true` à l'upload de n'importe quel modèle, jusqu'à l'event `model.converted`. Le handler `upload` déclenche la conversion via `spawn_conversion` : `tokio::spawn` détaché du `ModelConverter` (T123) cloné — la réponse `201` part immédiatement, le maillage arrive en asynchrone ; un échec est persisté (`conversion_error`) sans affecter la réponse. **Câblage** : `AppState` gagne un champ `converter: ModelConverter`, construit dans `AppState::new` (`WorkerMeshDecoder` + `FileStore` + dépôt modèles partagé + bus d'événements + concurrence `MODEL_CONVERSION_CONCURRENCY`, défaut 2). Pour capturer le dépôt dans une tâche détachée (durée de vie hors requête), ajout de `Storage::models_shared() -> Arc<dyn ModelRepo>` : les deux stockages (sqlite/postgres) détiennent leur `ModelRepo` en `Arc` et `models()` déréférence — l'API T123 (`Arc<dyn ModelRepo>`) et son test stub restent inchangés. Tests adaptés : `conversion_flag_covers_all_formats` (les 7 formats convertis) ; `http_models` `stl_upload` → `conversion_pending == true`. Les tests d'upload restent hermétiques : `#[tokio::test]` abandonne la tâche détachée en fin de test (worker absent → `Spawn` → `mark_conversion_failed`, sans effet sur les assertions de réponse ; `kill_on_drop` nettoie tout process). Gates verts : `cargo fmt --check`, `cargo clippy --workspace -D warnings` (+ feature `postgres`), `cargo test --workspace` (dont `http_models` 16/16). **Portée** : déclenchement + drapeau ; `/mesh` sert encore STL via `parse_stl` et renvoie 409 pour les autres — le service depuis le mesh stocké = T125.*
- [X] T125 Route `/mesh` servie depuis le moteur : `GET /api/models/{id}/mesh` (`backend/src/http/routes/models.rs`) sert le `mesh_path` stocké (200, WSMh) quand prêt, **409** tant que pending, **422** si la conversion a échoué ; **supprimer** la branche 501 « aperçu côté client » et le décodage `parse_stl` Rust en ligne (remplacés par le mesh moteur stocké). Tests d'intégration `backend/tests/model_mesh.rs`. Dépend de T124. — *Handler `mesh` réécrit : lit `model.mesh_path` — `Some` → sert les octets stockés (200, `application/octet-stream`, WSMh moteur) ; `None` + `conversion_error` présent → **422** ; `None` sinon → **409** (en cours). Suppression de tout le décodage serveur en ligne : plus de `parse_stl`, plus de branche 501 « aperçu côté client », import `crate::mesh::parse_stl` retiré ; le maillage servi est **exactement** celui produit par le moteur (source unique). Nouveau constructeur `ApiError::conversion_failed` (422, code **`conversion_failed`** — l'UI le distingue de « en cours » en T127). Tests d'intégration déterministes `backend/tests/model_mesh.rs` (4 tests) : modèles créés + ensemencés **directement via le dépôt** (indépendant du worker et de toute course avec la conversion asynchrone de l'upload) — prêt (`set_mesh` + fichier WSMh écrit) → 200 avec octets identiques ; sans mesh ni erreur → 409 ; `mark_conversion_failed` → 422 avec `code=conversion_failed` ; maillage d'autrui → 404 (isolation SC-008). Les deux anciens tests `http_models` devenus caducs (`serves_stl_mesh_as_compact_binary` testait `parse_stl` ; `step_mesh_is_conflict_until_converted` rendu instable par le `spawn` T124) sont retirés, remplacés par les tests déterministes ci-dessus (renvoi documenté dans `http_models`). Gates verts : `cargo fmt --check`, `cargo clippy --workspace -D warnings` (+ `postgres`), `cargo test --workspace` (model_mesh 4/4, http_models 14). **Portée** : service backend seul ; le retrait des parseurs client + `fetchMesh` systématique = T126, l'UX de conversion (placeholder/badge d'erreur lisant le 422) = T127.*
- [X] T126 [US1] Retrait de l'aperçu client : supprimer de `frontend/src/lib/scene/loaders.ts` les parseurs `parseStl`/`parseObj`/`parse3mf`/`previewFromBuffer`/`loadPreview`/`previewFormat` et `isPreviewable` (`frontend/src/lib/editor/imports.ts`) ; `importOne`/`loadProjectModels` (`frontend/src/routes/projects/[id]/+page.svelte`) uploadent puis s'appuient sur `model.converted` → `fetchMesh` (infra `resolveConversion` existante), `centerMesh` appliqué au maillage moteur. Supprimer `loaders.test.ts`, adapter `imports.test.ts` ; conserver `fetchMesh`/`decodeMesh`/`centerMesh`. Dépend de T125. — *`loaders.ts` réécrit : tous les parseurs client (`previewFormat`/`PreviewFormat`, `parseStl`/`parseBinaryStl`/`parseAsciiStl`/`isBinaryStl`, `parseObj`, l'intégralité du décodeur 3MF — `collect3mfMeshes`/`parseObjects`/`parse3mf`/matrices d'assemblage — et `previewFromBuffer`/`loadPreview`, plus `fetchModelFile` devenu inutile) retirés, ainsi que l'import `fflate`. Il ne reste que `meshFromTriangleSoup` (toujours utilisé par les primitives et les outils cut/simplify), `uploadModel` et `fetchMesh`. Barrel `scene/index.ts` réduit aux deux exports réseau ; `loaders.test.ts` supprimé. `imports.ts` : `isPreviewable`/`PREVIEWABLE` retirés (barrel `editor/index.ts` aligné), statut initial renommé `'previewing'` → **`'uploading'`** (l'aperçu n'existe plus) ; `imports.test.ts` adapté (retrait des cas `isPreviewable`, statut `uploading`). `+page.svelte` : `importOne` n'a plus de branche d'aperçu client — upload → `markUploaded` → le maillage arrive par `model.converted` → `resolveConversion` (`fetchMesh` + `centerMesh`), avec résolution immédiate si `!conversion_pending` ; `loadProjectModels` charge le maillage moteur tout de suite quand `model.has_mesh`, sinon laisse l'objet en attente de conversion. Le câblage `onEvent('model.converted') → resolveConversion` préexistant est réutilisé. Gates verts : `bun run format`/`check` (**0 erreur**, 2 warnings préexistants sans rapport) / `lint` / `test` (49 fichiers, 343 tests — retrait de `loaders.test.ts` et des cas d'aperçu). **Portée** : un modèle dont la conversion a **échoué avant la session** (pas d'événement à venir) reste affiché « conversion en cours » au rechargement — la détection d'échec (sonde `/mesh` → 422 → badge) est l'objet de T127.*
- [ ] T127 [US1] UX pendant la conversion : par objet, placeholder « conversion en cours » jusqu'à `model.converted`, **badge d'erreur** sur échec (422) dans `frontend/src/routes/projects/[id]/+page.svelte` (+ état d'import `lib/editor/imports.ts`). Vérif Playwright : import STL/OBJ/3MF/STEP → objet affiché via mesh moteur ; fichier corrompu → badge d'erreur, aucune exception page. Dépend de T126.
- [ ] T128 Gate d'intégration + doc de décision : test `backend/tests/model_decode.rs` (upload STL/OBJ/3MF/STEP → worker → `/mesh` 200 WSMh valide ; fichier corrompu → 422, backend survit) ; consigner le **retournement de la décision R7** (aperçu client → décodage moteur) dans `specs/001-orcaslicer-web-parity/research.md` et la note « moteur-only » du `README.md`/`plan.md`. Dépend de T125, T127.

**Ordre :** T120 → T121 → T122 → T123 → T124 → T125 → T126 → T127 ; T128 clôt (après T125, T127). T120 ∥ T122 possibles (couches distinctes) avant leur point de jonction (T123).

---

## Dependencies & Execution Order

```text
Phase 1 (Setup) → Phase 2 (Foundational)
Phase 2 → Phase 3 (P1 engine) → Phase 7 (P5 slicing)
Phase 2 → Phase 4 (P2 auth/storage) → Phases 5, 6, 7, 8
Phase 5 (P3 presets/onglets) → Phase 7 (presets résolus requis)
Phase 6 (P4 scène) → Phase 7 (scène à trancher)
Phase 7 → Phase 8 (P6 Moonraker : envoie les G-codes produits)
Phases 5 ∥ 6 (parallélisables après Phase 4 ; Phase 3 indépendante d'elles)
Phase 9 après Phase 8
Phase 10 (assemblage) → Phase 11 (thème) → Phase 12 (câblage réel + disposition OrcaSlicer)
```

Dépendances intra-phase notables : T011 (suite générique) avant T012–T020 ;
T024 (traits + suite de contrat) avant T025–T031 ; T040 (codegen UI) avant
T041–T047 ; T052/T054 avant T055–T058 ; T063 (file) avant T064–T066.
Assemblage (Phase 10) : les composants scène (T050–T061), réglages (T042/T046)
et préviz (T068–T070/T084) précèdent T087 ; T087 (shell) avant T088 (slice/aperçu)
et T089 (import éditeur) ; T091 (parité formats) étend le jeu accepté par T089/T090.
Câblage réel (Phase 12, dérivée de `ui_inventory.json`) : T097 (refonte 3 zones)
est la fondation. T098 (sélecteurs), T099 (châssis réglages), T103 (rail gizmos),
T106 (barre plateau), T107–T111 (menus Fichier/Édition/Vue/Calibration/Aide), T112
(menu contextuel objet), T116 (aperçu), T117/T118 (onglets/accueil) dépendent de
T097. T100/T101/T102 (groupes de réglages) dépendent de T099 ; T104/T105 (outils/
peinture) de T103 ; T113 (primitives) de T112 ; T114 (raccourcis éditeur) de
T107–T113 ; T115 (raccourcis aperçu) de T116 ; T119 (validation + garde) clôt la
phase après T097–T118.

## Parallel Execution Examples

- **Phase 3** : T013 ∥ T014 ∥ T015 ∥ T016 (wrappers FFI distincts) après T012 ; T021 ∥ T020.
- **Après Phase 4** : une piste « P3 » (T035–T047) et une piste « P4 » (T048–T062) en parallèle.
- **Phase 6** : T056 ∥ T057 ∥ T058 (gizmos indépendants) après T052/T054.
- **Phase 7** : T068 ∥ T070 ∥ T071 côté frontend pendant T063–T067 côté backend.
- **Phase 9** : T079 ∥ T080 ∥ T083 ∥ T084 ∥ T085.
- **Phase 10** : séquentielle (T087 → T088) ; s'appuie sur des composants déjà livrés.
- **Phase 12** : T097 d'abord (fondation) ; puis en parallèle T098 ∥ T099 ∥ T103 ∥ T106 ∥ T107 ∥ T108 ∥ T109 ∥ T110 ∥ T111 ∥ T112 ∥ T116 ∥ T117 ∥ T118 ; T100 ∥ T101 ∥ T102 après T099 ; T104 ∥ T105 après T103 ; T113 après T112 ; T114 après T107–T113 ; T115 après T116 ; T119 en clôture.
- **Phase 13** : T120 (encodage moteur) ∥ T122 (infra spawn worker) en parallèle ; T121 après T120 ; puis chaîne séquentielle T123 → T124 → T125 (backend) → T126 → T127 (frontend) ; T128 en clôture.

## Implementation Strategy

- **MVP** : Phases 1–4 + chemin minimal de la Phase 7 (T063–T066 + bouton
  slice basique) = US1 démontrable (import → slice presets par défaut →
  export). Les onglets complets (Phase 5) et la scène complète (Phase 6)
  enrichissent ensuite sans bloquer la démo.
- **Livraison incrémentale** : chaque phase se termine par son gate
  (quickstart §0) et une démo ; commit atomique par tâche, tests inclus
  (constitution IV) ; check_traceability s'étend phase par phase (T006 →
  T047 → T062 → T082) pour que la parité reste mesurée en continu.
- **Risque en tête** : la Phase 3 (bridge FFI) était le chemin critique ;
  elle est désormais franchie (chargement, 3MF, STEP, réparation, arrangement,
  tranchage réel + préviz G-code, suite générique verte sur `FfiEngine`).
- **Dette d'intégration (Phase 12)** : les tests unitaires par composant ont
  masqué l'absence de câblage dans les pages (composants verts mais importés
  nulle part). La garde de traçabilité de T119 (chaque cible tracée gizmo/toolbar/
  menu/contextuel/raccourci doit être importée par une route) transforme ce
  silence en échec de gate — à câbler
  avant de considérer une future tâche « composant » comme livrée.
