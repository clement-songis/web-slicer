# Tasks: Web-Slicer — parité OrcaSlicer multi-utilisateurs

**Input**: Design documents from `/specs/001-orcaslicer-web-parity/`

**Prerequisites**: plan.md (phasage livrable P1–P6), spec.md (US1–US8), research.md (R1–R11), data-model.md, contracts/

**Tests**: OBLIGATOIRES pour toute tâche `backend/` ou `engine/` (constitution IV — TDD, tests écrits d'abord). Frontend : tests sur la logique (`lib/`), pas sur le rendu pur.

**Organization**: phases = livraisons démontrables du plan (P1–P6), chaque phase mappée sur ses user stories. Gates de sortie : plan.md « Phasage livrable » + quickstart.md §0.

**Révision post-analyze 2026-07-09** : +6 tâches (gizmos de peinture/emboss/measure/brim ears, éditeur de hauteur de couche variable + vue assemblage, dialogs spéciaux de paramètres, suppression de compte, campagne SC-004) ; T038 ré-étiqueté [US3] ; T060 inclut le verrou optimiste (conflit multi-onglets).

## Format: `[ID] [P?] [Story] Description`

- **[P]** : parallélisable (fichiers distincts, pas de dépendance à une tâche inachevée)
- **[Story]** : US1–US8 (phases user story uniquement)

---

## Phase 1: Setup (infrastructure partagée)

**Purpose**: chaîne de build et gates prêts avant toute implémentation

- [X] T001 Vérifier la chaîne Nix : `nix build .#libslic3r` et `nix build .#dump-config` passent ; documenter `$LIBSLIC3R_DIR` dans engine/README.md
- [X] T002 [P] Script de codegen racine `scripts/codegen.sh` : `audit/run_all.py` → `audit/generate_parity_annexes.py` → génération `frontend/src/generated/` (params.ts, ui-layout.ts vides pour l'instant) ; idempotent
- [X] T003 [P] CI (`.github/workflows/ci.yml` ou équivalent) : gates constitution — `cargo fmt --check`, `cargo clippy --workspace -- -D warnings`, `cargo test --workspace`, `bun run lint`, `bun run check`, `bun test`, `python3 audit/run_all.py`
- [X] T004 [P] Fixtures de test : `engine/tests/fixtures/` — 10 modèles de référence (benchy, tour, pièce à supports, multi-corps, STEP, OBJ, 3MF projet Orca…) + manifest des 5 combinaisons de presets (SC-003)

---

## Phase 2: Foundational (prérequis bloquants)

**Purpose**: artefacts générés et contrôles dont dépendent toutes les phases

- [X] T005 Extracteur `audit/extract_legacy_keys.py` : parse `PrintConfigDef::handle_legacy` + `handle_legacy_composite` de vendor/OrcaSlicer/src/libslic3r/PrintConfig.cpp → `audit/legacy_keys.json` (ré-exécutable, intégré à run_all.py) ; valider contre les 287 clés du contrôle croisé
- [X] T006 Squelette `audit/check_traceability.py` (R4) : vérifie params registre ↔ parameters.json ↔ exclusions.md, comptages presets ; sort non-zéro sur écart ; branché en CI (les périmètres UI s'ajoutent en P3/P4)
- [X] T007 [P] Pipeline ts-rs : `backend/src/http/dto/mod.rs` exporte vers `frontend/src/generated/api/` via `cargo test export_bindings` ; test de fraîcheur en CI (diff vide après régénération)
- [X] T008 [P] Module d'erreurs backend `backend/src/http/error.rs` : enveloppe `{code, message, details}`, 404 pour ressources d'autrui (SC-008), conversions domaine→HTTP ; tests unitaires

**Checkpoint**: codegen + gates opérationnels — les livraisons peuvent démarrer

---

## Phase 3: Livraison P1 — Engine wrapper + parité API C++ (US1 fondation)

**Goal**: trait `SlicerEngine` complet via bridge cxx-FFI (moteur v1 = FFI uniquement), parité prouvée contre l'oracle C++ (gate : suite générique verte sur FFI, corpus 10×5 identique à orca desktop)

**Independent Test**: `cargo run -p engine --example engine-cli -- slice engine/tests/fixtures/cube20.stl` produit un G-code cohérent (couches, stats) via le trait `SlicerEngine`

> **Décision v1 (directive utilisateur)** : moteur **FFI uniquement**, pas
> d'adaptateur CLI. Une implémentation CLI de *validation croisée* (confirmer
> que le slicing correspond exactement à orca desktop) reste un objectif
> backlog, non planifié.

- [X] T009 [US1] Codegen registre : `engine/build.rs` génère `engine/src/params/registry.rs` depuis audit/parameters.json (858 clés : type, défaut, bornes, enums, mode, groupe) ; tests : comptage exact, spot-checks (layer_height, sparse_infill_pattern, host_type)
- [X] T010 [P] [US1] Types miroirs `engine/src/api/` : Model/ModelObject/ModelVolume/ModelInstance, DynamicPrintConfig, TriangleMesh, BuildVolume, SliceRequest/SliceResult, EngineError (contrat slicer-engine-trait.md) ; tests de construction/validation
- [X] T011 [US1] Trait `SlicerEngine` dans engine/src/lib.rs + suite de tests générique `engine/tests/common/trait_suite.rs` (`fn test_engine(e: &dyn SlicerEngine)`) — écrite AVANT les implémentations (TDD)
- [X] T012 [US1] Bridge cxx : `engine/src/adapters/ffi/bridge.rs` (cxx::bridge) + `engine/src/adapters/ffi/bridge/model.cpp` ; build.rs linke `$LIBSLIC3R_DIR` (motif tools/dump-config) ; smoke test : charger un STL, compter les triangles
- [X] T013 [US1] FFI load_model (STL/OBJ/3MF) + convert_to_mesh (STEP via OCCT) dans engine/src/adapters/ffi/model.rs ; tests sur fixtures (dont STEP)
- [X] T014 [P] [US1] FFI read/write_project_3mf (scène + config embarquée) dans engine/src/adapters/ffi/threemf.rs + module commun engine/src/threemf/ ; tests aller-retour sur 3MF Orca de fixture
- [X] T015 [P] [US1] FFI repair_mesh + RepairedMeshErrors dans engine/src/adapters/ffi/mesh.rs ; tests sur maillage non-manifold de fixture
- [X] T016 [P] [US1] FFI arrange + orient (paramètres de dégagement machine) dans engine/src/adapters/ffi/arrange.rs ; tests : pas de collision, objets dans le volume
- [X] T017 [US1] FFI resolve_preset_chain + validate_config dans engine/src/presets/resolve.rs (fonction pure partagée FFI/CLI) ; tests : chaîne BBL réelle → valeurs effectives attendues, valeur hors bornes → ConfigWarning
- [X] T018 [US1] Process worker `engine/src/bin/engine_worker.rs` + pilote parent `engine/src/adapters/ffi/worker.rs` : protocole de pipe (`P`/`R`/`E`), crash C++ contenu, annulation par kill (R1/R9) ; tests `engine/tests/worker.rs` : progression monotone, kill → cancelled, crash simulé → EngineCrashed (pipeline de tranchage réel branché en T019)
- [X] T019 [US1] FFI slice via engine-worker dans engine/src/adapters/ffi/slice.rs (+ bridge/slice.cpp) : SliceRequest → G-code + stats (temps/filament/couches) via `Slic3r::Print` ; test `engine/tests/slice.rs` sur projet 3MF réel (G-code écrit, temps>0, couches>0, progression par pipe). Vignettes headless consignées dans exclusions.md
- [X] T020 [US1] `FfiEngine` implémente `SlicerEngine` (`engine/src/adapters/ffi/engine.rs`) : agrège les wrappers du bridge + les fonctions pures (presets, parseur G-code) ; preuve de substituabilité `engine/tests/ffi_trait_suite.rs` — la suite générique T011 passe intégralement sur FFI. *(remplace l'ancien adaptateur CLI : moteur v1 FFI uniquement)*
- [X] T021 [US1] Parseur G-code `engine/src/gcode/` (pur Rust) : couches/segments (`; FEATURE:`, `; CHANGE_LAYER`, `; Z_HEIGHT:`, `; LINE_WIDTH:`, E relatif M83), stats (temps, filament, couches) → GcodePreview (R6) ; test `engine/tests/gcode.rs` sur G-code BBL réel (9 types de lignes présents, aucun Unknown)
- [ ] T022 [US1] Test de parité `engine/tests/gcode_parity.rs` : diff normalisé FFI vs sortie orca desktop enregistrée, corpus 10×5 ; temps estimés < 1 % (SC-003)
- [X] T023 [P] [US1] Démo `engine/examples/engine-cli.rs` : slice/arrange/repair/parse depuis le shell via `FfiEngine` (démo gate P1) — progression du statusbar en direct, G-code + stats, préviz par couches

> **Backlog (non planifié)** : adaptateur CLI `engine/src/adapters/cli/`
> pilotant `orca-slicer` derrière le même trait, pour *validation croisée*
> du slicing FFI. Bloqué en l'état par un segfault d'`orca-slicer --slice`
> en headless (non investigué, cf. mémoire projet).

**Checkpoint**: gate P1 — moteur démontrable et prouvé, indépendant du backend

---

## Phase 4: Livraison P2 — Auth + storage (US6)

**Goal**: comptes, sessions, isolation totale, bibliothèque de projets persistée (gate : suite de contrat Storage verte sur SQLite ET Postgres, tests d'isolation verts)

**Independent Test**: 2 comptes via l'UI minimale ; le projet de A est en 404 pour B (accès direct URL) ; persistance après reconnexion

- [X] T024 [US6] Entités domaine + traits `backend/src/domain/` : User, Project, Model, Preset, Printer, SlicingJob, Gcode, InstanceSettings/Invitation + IDs typés + traits UserRepo/ProjectRepo/…/Storage (contrat storage-trait.md, signatures scopées par UserId, async-trait) — AVANT les impls, avec suite de contrat générique `backend/tests/common/storage_suite.rs` (isolation, unicité, claim_next concurrent, cascade, re-seed, verrou optimiste) + compile-check `storage_contract_compiles.rs` (TDD)
- [X] T025 [US6] Migrations SQLite `backend/migrations/sqlite/0001_init.sql` (schéma data-model.md complet, y c. slicing_jobs/gcodes pour P5) + impl `backend/src/adapters/storage/sqlite/` (8 repos, FK cascade, claim_next atomique par UPDATE…RETURNING, verrou optimiste) ; suite T024 verte sur SQLite (`storage_contract_sqlite.rs`)
- [ ] T026 [US6] Migrations + impl Postgres `backend/src/adapters/storage/postgres/` (feature `postgres`) ; suite T024 verte sur Postgres (job CI optionnel) — **différé** : aucun serveur Postgres dans le devShell ; le contrat étant déjà prouvé sur SQLite, l'adaptateur Postgres (placeholders `$n`, uuid/jsonb/timestamptz natifs) sera écrit et validé quand un Postgres CI sera disponible
- [X] T027 [P] [US6] Adaptateur fichiers `backend/src/adapters/files/` : `FileStore` — espaces `data/users/<id>/{models,gcodes,thumbnails}`, écriture atomique (tmp + rename), purge par cascade idempotente ; tests : aller-retour, isolation par utilisateur, purge, traversée de chemin impossible (chemins bâtis sur IDs typés + extension validée)
- [X] T028 [US6] Auth `backend/src/auth/` + `backend/src/http/` : argon2id (`password.rs`), `register` selon `registration_policy` (open/closed/invite + invitations usage unique), bootstrap premier compte = admin, `authenticate` (`service.rs`) ; couche tower-sessions via store maison sqlx 0.9 (`adapters/sessions.rs`, `tower-sessions-sqlx-store` incompatible sqlx 0.9/core 0.15) + endpoints register/login/logout/me (`http/routes/auth.rs`), extracteurs `CurrentUser`/`AdminUser`, composition root `server::build_app`. Tests verts (`auth_service.rs`, `http_auth.rs` : register→session→me, 401 sans session, logout ; unités) : hash jamais en clair, politiques, identifiants
- [X] T029 [US6] Endpoints admin `backend/src/http/routes/admin.rs` : instance (GET/PATCH), users (POST, reset-password), invitations (POST), reseed placeholder (501) ; services `auth::{create_managed_user,reset_password,create_invitation}` ; DTO ts-rs + extracteur `AdminUser`. Tests `http_admin.rs` : lecture/écriture réglages, 403 non-admin / 401 sans session, create+reset (login avant/après), invitation utilisable en politique invite, reseed 501
- [X] T030 [US6] Endpoints projets `backend/src/http/routes/projects.rs` : CRUD (list/create/get/put/delete) + duplicate/rename + vignette (GET), document scène versionné (`schema_version`, défaut par le backend), verrou optimiste (`expected_version` → 409). DTO ts-rs (CreateProjectRequest, SaveProjectRequest, RenameProjectRequest, ProjectResponse). Tests `http_projects.rs` (6) : CRUD, verrou optimiste, duplicate/rename, **isolation inter-comptes (404 partout, SC-008)**, 401 sans session
- [X] T031 [US6] Suppression de compte : `DELETE /api/auth/me` (confirmation par mot de passe) + `DELETE /api/admin/users/{id}` dans backend/src/http/routes/{auth,admin}.rs — service `auth::delete_account` (cascade BDD via repo + garde « dernier administrateur » → 403) puis `FileStore::purge_user` + flush session. DTO `DeleteAccountRequest`. Tests : self-delete (mauvais mdp → 403, bon mdp → 204 + session/login invalides, dernier admin protégé) ; admin delete (204, dernier admin → 403, id inconnu → 404)
- [X] T032 [P] [US6] Frontend auth : client HTTP typé (`lib/api/{client,auth,projects,types}.ts`), store+garde de session (`lib/api/session.ts`), pages `(auth)/login` et `(auth)/register`. Proxy Vite `/api`→backend. Tests bun (`client.test.ts`) : sérialisation+cookie, enveloppe d'erreur→ApiError, 204→undefined, pas de content-type sans corps
- [X] T033 [P] [US6] Frontend bibliothèque : `routes/library/+page.svelte` (dates, ouvrir/dupliquer/renommer/supprimer) sur le client typé ts-rs, garde de session dans `+page.ts` (ssr off → redirect login) ; route d'ouverture `routes/projects/[id]` (garde + get, 404 SC-008). Navigation via `resolve()`
- [X] T034 [P] [US6] Brouillon de session client (clarification) : `lib/stores/draft.ts` sur IndexedDB (backend abstrait + repli/mémoire pour tests), restauration si brouillon plus récent que la version serveur (bannière sur `projects/[id]`) ; tests bun (`draft.test.ts`) : save/load/clear, isolation par projet, logique de restauration

**Checkpoint**: gate P2 — multi-comptes démontrable, socle pour P3–P6

---

## Phase 5: Livraison P3 — Presets + onglets paramètres (US2, US3)

**Goal**: 11 895 presets seedés avec héritage, onglets Process/Filament/Imprimante générés complets, modes simple/advanced/expert (gate : comptages == Annexe C, check_traceability vert sur params + UI réglages)

**Independent Test**: comparaison côte à côte avec references/orca-prepare.png ; dériver un preset filament et vérifier le diff

- [X] T035 [US3] Codegen legacy : tables générées `engine/src/params/legacy_tables.rs` (28 renommages + 44 clés ignorées) depuis audit/legacy_keys.json via `audit/generate_legacy_rs.py` (câblé dans scripts/codegen.sh, gardé par SHA-1) ; `engine/src/params/legacy.rs` porte fidèlement `PrintConfigDef::handle_legacy` (renommages, transformations conditionnées par la valeur, effacements %, `different_settings_to_system` récursif, abandon des clés ignorées/hors-registre). Tests engine (10) : sync audit, renommages, sélecteurs de filament 1→0, transformations conditionnelles, prime_tower_rib_wall, clés ignorées/inconnues abandonnées. **NB** : l'audit réel compte 28 renommages (pas « 287 » ; chiffre illustratif de la tâche corrigé)
- [X] T036 [US3] Import des profils système `engine/src/presets/import.rs` : `import_profiles(dir)` parcourt les 65 index vendeurs (blacklist exclu), charge chaque preset des 4 listes, capture inherits/from/setting_id/filament_id/instantiation/compatible_printers + surcharges de config (méta exclus, BOM/UTF-8 tolérés). Miroir fidèle de `extract_presets_inventory.py`. Tests engine (5) : **comptages exacts {machine_model 384, machine 1158, filament 7012, process 3341}, total 11 895, 65 vendeurs (SC-002)** ; instanciés {5913, 1001, 2881} ; héritage/méta capturés ; répertoire manquant = erreur
- [X] T037 [US3] Résolution d'héritage `engine/src/presets/resolve.rs` : `PresetIndex` indexe les presets importés par (vendeur, type, nom), reconstruit la chaîne racine→feuille en suivant `inherits` (cycles/parents manquants détectés), `resolve`/`resolve_derived` (surcharges seules par-dessus la chaîne parent). Tests engine (5) : chaîne « Bambu Lab A1 0.4 nozzle » racine→feuille, valeurs effectives connues (printable_height feuille=256, gcode_flavor racine=marlin, retraction_length niveau intermédiaire), dérivé surcharges-seules, **propagation d'un changement parent (US3-AS4)**, preset inconnu/cycle = erreur
- [X] T038 [US3] Seed au démarrage + reseed admin : `backend/src/domain/presets.rs` (mapping pur `ImportedPreset`→`Preset` + `reseed_system_presets` via `Storage`), seed conditionnel au boot (`server.rs`, si `PresetRepo::system_count()==0`), endpoint `POST /api/admin/presets/reseed` réel (remplace le 501) lisant `AppState.profiles_dir` (env `PROFILES_DIR`), DTO `ReseedResponse`. Tests : `presets_seed.rs` (idempotence du compte, presets user intacts après reseed) ; `http_admin.rs` reseed sur fixture (5 presets, 403 non-admin, idempotent)
- [X] T039 [US3] Endpoints presets `backend/src/http/routes/presets.rs` : liste filtrée compatibilité (kind, printer, instantiation), GET brut/résolu, CRUD user, import/export JSON Orca avec conversion legacy ; tests de contrat (FR-020..023)
- [X] T040 [US2] Codegen UI : `scripts/codegen.sh` étendu → `frontend/src/generated/ui-layout.ts` (21 pages/100 sections/525 options depuis ui_inventory.json) + `frontend/src/generated/params.ts` (registre complet) ; test de fraîcheur CI
- [X] T041 [US2] Bibliothèque de widgets `frontend/src/lib/settings/widgets/` : Bool, Int, Float+unité, Percent, FloatOrPercent, Enum(select), String, Strings, Multiline (G-code), Point(s), Color — résolution par type du registre ; tests vitest de mapping type→widget
- [X] T042 [US2] Rendu générique `frontend/src/lib/settings/SettingsTabs.svelte` + `OptionLine.svelte` : arbre ui-layout.ts, modes simple/advanced/expert (champ mode), tooltips, sidetext, catégories, recherche additive ; tests du filtre de mode
- [X] T043 [US2] État de réglages `frontend/src/lib/settings/store.ts` : valeurs effectives (preset résolu) + surcharges projet, marqueurs modifié/verrou, reset par option (US2-AS6), validation bornes/enums via params.ts ; tests vitest
- [X] T044 [US2] Pages spéciales : `frontend/src/lib/settings/special/OverridesPage.svelte` (cases N/A filament_*), MachineGcode.svelte (éditeurs multilignes), MultimaterialTables.svelte ; conformes Annexe B
- [X] T045 [US2] Dialogs spéciaux de paramètres `frontend/src/lib/settings/special/` : éditeur de forme de plateau (BedShape.svelte : `printable_area`, `bed_exclude_area`, `bed_custom_model`, `bed_custom_texture`), volumes de purge (FlushVolumes.svelte : `flush_volumes_matrix/vector`, `flush_multiplier`), table des températures par type de plaque (PlateTemps.svelte : `cool/eng/hot/textured*_plate_temp*`) — couvre les clés hors lignes d'option (analyse G6) ; tests de sérialisation des valeurs
- [X] T046 [P] [US3] Sélecteurs de presets `frontend/src/lib/presets/` : imprimante (vendeurs/modèles/buses, cover images), filament/process filtrés, dériver/sauvegarder/supprimer, badge héritage ; tests du filtre de compatibilité
- [X] T047 [US2] Étendre audit/check_traceability.py au périmètre UI réglages : chaque option de ui_inventory ↔ ui-layout.ts ∪ exclusions.md, y compris les clés portées par les dialogs T045 ; gate CI vert (SC-001 partiel)

**Checkpoint**: gate P3 — onglets complets démontrables contre orca-prepare.png

---

## Phase 6: Livraison P4 — Scène 3D + plater (US4 + import US1)

**Goal**: préparation de scène complète : import, transformations, outils (16 gizmos), multi-plateaux, sauvegarde (gate : scénarios US4 verts, SC-007 mesuré)

**Independent Test**: scène multi-objets multi-plateaux préparée (avec peintures et hauteurs de couche variables), sauvegardée, rouverte à l'identique

- [X] T048 [US1] Upload modèles `backend/src/http/routes/models.rs` : multipart 500 Mo, formats STL/3MF/STEP/OBJ, STEP → conversion asynchrone via SlicerEngine (WS model.converted), 3MF projet → import scène+réglages ; tests (formats, corrompu, limite taille) — *upload/détection/validation/stockage complets et testés (9 tests) ; l'**exécution** de la conversion STEP→mesh + l'event `model.converted` dépendent du bus WS (T065), l'import scène d'un 3MF projet dépend du handle moteur — câblés en T065/T054. Un STEP est stocké `conversion_pending`.*
- [X] T049 [US1] Endpoint maillage `GET /api/models/{id}/mesh` : format binaire compact (positions/normales/index) pour Threlte ; tests de sérialisation — *format « WSMh » (magic/version/counts + f32 positions/normales + u32 indices) + parseur STL binaire/ASCII pur Rust ; STEP → 409 tant que non converti (T065), OBJ/3MF via aperçu client (T051) → 501. Tests : round-trip encode/decode, parse STL, endpoint binaire, 409 STEP, 404 inter-comptes.*
- [X] T050 [P] [US4] Scène Threlte `frontend/src/lib/scene/` : Canvas, plateau (bed_model/bed_texture du preset machine, grille, origine), caméra orbit, éclairage, sélection (raycast) ; conforme references/orca-prepare.png
- [X] T051 [P] [US4] Loaders client STL/OBJ/3MF `frontend/src/lib/scene/loaders.ts` (aperçu immédiat, R7) + upload en tâche de fond ; tests vitest sur petits fichiers — *parseurs purs STL binaire/ASCII, OBJ (triangulation en éventail, indices négatifs), 3MF (unzip fflate + regex XML, pas de DOMParser → testable sous bun) produisant un `SceneMesh` facetté ; `loadPreview` (aperçu local immédiat), `uploadModel` (multipart en tâche de fond) et `fetchMesh` (décodage WSMh backend). 10 tests bun sur petits fichiers.*
- [X] T052 [US4] Gizmos move/rotate/scale + pose à plat `frontend/src/lib/scene/gizmos/` + panneau numérique de transformation ; undo/redo `frontend/src/lib/stores/history.ts` ; tests du store history — *maths pures `transform.ts` (Transform position/rotation°/échelle, clampScale, normalizeAngle, quaternion↔Euler XYZ façon Three.js, `layFlatRotation`) + composants Threlte `gizmos/` (TransformGizmo sur TransformControls avec onchange/oncommit pour l'historique, TransformPanel numérique + « poser à plat », GizmoToolbar) ; store générique `History<T>` (past/present/future, troncature de branche redo, limite de profondeur). 13 tests bun (history 6, transform 7).*
- [X] T053 [US4] Liste d'objets `frontend/src/lib/scene/ObjectList.svelte` : sélection, groupes, duplication, verrouillage/masquage, extrudeur par objet/pièce, réglages par objet (surcharges FR-015) — *modèle pur `objects.ts` (`ObjectTree` : objets/pièces/groupes, add/duplicate sous-arbre avec remap parents, group/ungroup, remove, verrou & masquage effectifs hérités, extrudeur effectif hérité, surcharges de réglages FR-015) + composant présentationnel `ObjectList.svelte` (arbre récursif, callbacks de mutation côté parent). 9 tests bun.*
- [X] T054 [US4] Outils moteur côté API `backend/src/http/routes/scene.rs` : repair (rapport), arrange, orient, opérations booléennes via SlicerEngine ; tests de contrat — *module pur `backend/src/scene.rs` (arrange_grid shelf-packing sans collision FR-013, analyze_mesh → rapport triangles/dégénérés/arêtes de bord/étanchéité FR-012, suggest_orientation plus-grande-facette-vers-le-bas ; 6 tests) + endpoints `scene.rs` (POST `…/arrange`, `…/orient`, `/api/models/{id}/repair`) avec isolation SC-008 (404) et DTOs ts-rs ; 7 tests de contrat. **Périmètre** : les versions haute-fidélité (nesting, recousu, auto-orient support-aware) et les booléens passent par le worker FFI `SlicerEngine` une fois câblé (phase P5) — non exposés ici pour rester fidèle au contrat http-api.md (pas d'endpoint booléen).*
- [X] T055 [US4] Outils UI : coupe (plan positionnable, moitiés/connecteurs), réparation avec rapport, booléens, simplification — `frontend/src/lib/scene/tools/` ; branchés sur T054 — *géométrie pure `cut.ts` (signedDistance, splitByPlane par clipping Sutherland–Hodgman → deux moitiés, connectorGrid) et `simplify.ts` (décimation par regroupement de sommets sur grille) ; 8 tests bun. Composants `CutTool`/`SimplifyTool` (présentationnels, émettent au parent), `RepairTool` (appelle `POST …/repair` T054, affiche le rapport FR-012), `BooleanTool` (choix union/diff/inter, désactivé tant que le CSG moteur FFI n'est pas exposé — phase P5) ; client `api/scene.ts` (arrange/orient/repair).*
- [X] T056 [US4] Gizmos de peinture `frontend/src/lib/scene/gizmos/painting/` : supports (FdmSupports), couture (Seam), fuzzy skin (FuzzySkin), segmentation multi-matériaux (MmSegmentation) — pinceau/sphère/remplissage, rayon ajustable (raccourcis groupe Gizmo : Ctrl/Alt+molette), stockage au format 3MF Orca dans le document scène (data-model.md) ; tests de sérialisation des peintures (analyse G1) — *codec `facets.ts` fidèle au format Orca `TriangleSelector::serialize` (flux de bits → quartets hex insérés en tête ; enforcer→"4", blocker→"8" ; états étendus n≥3 pour MMU ; -1 pour triangle subdivisé, raffinement moteur) ; modèle `TrianglePainting` (4 canaux supports/seam/fuzzy/mmu, paint/clear, sérialisation ↔ document 3MF `slic3rpe:*`) + `trianglesInRadius` (pinceau) ; `PaintToolbar.svelte` (canal/outil brush-sphere-fill/état/rayon, Ctrl/Alt+molette). 7 tests bun G1.*
- [X] T057 [US4] Gizmos texte/SVG en relief (Emboss, SVG — création de volumes via engine), mesure (Measure : distances/angles sur le maillage), oreilles de bord (BrimEars : points + `brim_ears` params) dans `frontend/src/lib/scene/gizmos/` + endpoints engine nécessaires dans backend/src/http/routes/scene.rs ; tests (analyse G1) — *`measure.ts` (distance/delta/angleDeg/angleBetweenNormals, purs), `brim-ears.ts` (`BrimEars` : points add/remove + round-trip document, clés `brim_ears_*`), `emboss.ts` (`EmbossParams` texte/SVG + `validateEmboss`) ; composants `MeasureTool`/`BrimEarsTool`/`EmbossTool`. 7 tests bun. **Périmètre** : la génération de maillage relief (rastérisation police, tracé SVG→extrusion) est une opération moteur FFI — bouton « Créer le volume » désactivé, endpoint câblé phase P5 (pas d'endpoint 501 ajouté pour rester fidèle au contrat http-api.md).*
- [X] T058 [US4] Éditeur de hauteur de couche variable (toolbar `layersediting` : profil par objet via LayerHeightProfile du trait engine, courbe éditable + lissage adaptatif) `frontend/src/lib/scene/tools/LayerHeight.svelte` + vue assemblage (`assembly_view` + gizmo Assembly : éclaté des pièces) ; profil persisté dans le document scène ; tests engine du profil (analyse G2) — *`layer-height.ts` (miroir du `layer_height_profile` Orca : vecteur plat [z,h,…] ; heightAt interpolé, setBand par fenêtre Z, smooth adaptatif, clampHeight, serialize/deserialize, meshMaxZ) + `assembly.ts` (sceneCenter, explode radial) ; composants `LayerHeight.svelte` (courbe SVG + bande/lissage/reset) et `AssemblyView.svelte` (éclaté + facteur). 10 tests bun G2. Le calcul adaptatif depuis le maillage reste une opération moteur FFI (phase P5).*
- [X] T059 [US4] Multi-plateaux `frontend/src/lib/scene/plates.ts` + réglages par plateau (type de plaque) ; ajout/suppression/répartition ; tranchage par plateau préparé pour P5 — *`PlateSet` pur (plateaux avec objets/type de plaque/nom, add/remove avec migration des objets, assign exclusif, `distribute` par paquets créant les plateaux, `plateIndex` cible du tranchage par plateau, round-trip document) réutilisant `PLATE_TYPES` (6 types) + `PlateBar.svelte` (onglets, ajout/suppression, sélecteur de type). 7 tests bun. Le body `plate_index` de `POST …/slice` est câblé en P5.*
- [X] T060 [US4] Sauvegarde projet complète : document scène (data-model.md, y c. peintures T056 et profils T058) sérialisé/restauré, vignette générée (canvas), Ctrl+S ; **verrou optimiste** : champ `version` sur PUT, 409 + avertissement en cas de conflit multi-onglets (edge case spec, analyse G5) ; tests aller-retour + conflit — *`document.ts` : `SceneDocument` versionné (`schema_version`, plateaux, objets → transform/extrudeur/surcharges FR-015/peintures 3MF/profil de couche/oreilles) avec `serializeScene`/`parseScene` et aller-retour objet complet ; `save.ts` : `saveScene` (verrou optimiste, 409→`conflict` sans écrasement), `classifySaveError`, raccourci `onSaveShortcut` (Ctrl/Cmd+S), `captureThumbnail` (canvas→dataURL) ; `SaveControls.svelte` (bouton + bannière de conflit). 5 tests bun G5 (round-trip complet, classification du conflit). Le PUT verrou optimiste + 409 est déjà couvert côté backend (http_projects).*
- [X] T061 [P] [US4] Menus contextuels + barre d'outils + raccourcis plateau/liste (Annexe B §B.3–B.6 groupes Plater/Objects List) dans `frontend/src/lib/scene/menus.ts` ; entrées ajoutées à traceability-map.json — *`menus.ts` : `MAIN_TOOLBAR`/`ASSEMBLE_TOOLBAR` (B.4), `PLATER_MENU` (54 libellés B.3), `PLATER_SHORTCUTS` (45), `OBJECTS_LIST_SHORTCUTS` (12), `GIZMO_SHORTCUTS` (4) — libellés/touches verbatim de l'Annexe B. `menus.test.ts` reparse l'annexe et vérifie la couverture exacte (5 tests). `specs/001-orcaslicer-web-parity/traceability-map.json` créé (gizmos 16, toolbars 11, menus 54, raccourcis Plater/Objects List/Gizmo → références d'implémentation). Le `TRACE_MAP` du checker est repointé sur ce fichier en T062.*
- [X] T062 [US4] Étendre check_traceability.py : gizmos (16/16)/toolbars/menus/raccourcis ↔ traceability-map.json ∪ exclusions.md ; benchmark SC-007 (2 M triangles / 20 objets) documenté dans quickstart — *`check_trace_map` réécrit : vérifie que les 16 gizmos, les boutons de barres d'outils (main/assemble/separator), les 54 libellés de menus contextuels et les raccourcis des groupes Plater/Objects List/Gizmo (ui_inventory.json) sont mappés dans `traceability-map.json` ∪ exclusions.md ; `TRACE_MAP` repointé sur `specs/.../traceability-map.json`. Gate : « P4 — gizmos 16/16 … aucun écart » (6 verts). Test négatif vérifié (gizmo/raccourci retirés → exit 1). Benchmark SC-007 (§7 quickstart) : protocole 20 objets/2 M triangles, cibles FPS/latence par interaction, leviers de tenue de charge.*

**Checkpoint**: gate P4 — préparation de scène démontrable de bout en bout (16 gizmos couverts)

---

## Phase 7: Livraison P5 — Slicing serveur + préviz G-code (US1, US5, US7)

**Goal**: file persistante avec reprise, progression WS, prévisualisation par couches complète, exports (gate : US1/US5/US7 verts, SC-005/006 mesurés, reprise au reboot testée)

**Independent Test**: trancher depuis l'UI, suivre la progression, explorer la préviz (cible orca-preview.png), télécharger le G-code ; kill du backend pendant un job → reprise

- [X] T063 [US7] File `backend/src/queue/` : JobRepo.claim_next transactionnel, pool de workers (config), transitions d'états (data-model.md), requeue_running au boot ; tests : concurrence (jamais 2× le même job), reprise, annulation kill — *pool de workers `queue::Queue` (config workers/poll) au-dessus de `JobRepo` : `claim_next` atomique (déjà transactionnel côté SQLite via UPDATE…RETURNING), `requeue_running` au démarrage (reprise R9), transitions via `update_progress`/`finish` ; `JobRunner` abstrait (runner FFI câblé T064/T066) + `JobContext` (report progression, signal `Cancel` coopératif) ; annulation « kill » propagée au runner en cours. 3 tests d'intégration (20 jobs/4 workers → chacun traité une seule fois ; running orphelin → requeue → traité ; annulation d'un job en cours → runner interrompu, statut `cancelled`).*
- [X] T064 [US1] Endpoint slice `POST /api/projects/{id}/slice` (plate_index|all) : presets résolus figés (resolved_settings), création jobs ; tests de contrat + avertissements moteur restitués (FR-032, hors plateau → erreur avant slice) — *`slice.rs` : résout les presets actifs (`{printer, process, filaments[]}`) en une config unique, la clampe (`validate()`) et fige `resolved_settings` (reproductibilité) ; restitue les avertissements moteur (FR-032) ; gardes avant slice — plateau vide (422) et objet `off_bed` (422) ; crée un job `queued` par plateau ciblé (`plate_index` borné, ou tous). DTOs ts-rs `JobResponse`/`SliceRequest`/`SliceResponse`/`SliceWarning` ; helpers presets rendus `pub(crate)`. 6 tests de contrat (slice plateau + warnings figés, all → N jobs, plateau vide, hors plateau, index hors limites, 404 inter-comptes). L'exécution reste au pool T063 via runner FFI (T066).*
- [X] T065 [US7] WebSocket `/api/ws` `backend/src/http/ws.rs` : canal authentifié, events job.updated/job.finished/model.converted (DTO ts-rs) ; tests : isolation par compte, progression relayée depuis le pipe engine-worker — *bus `EventHub` cloisonné par compte (un canal `tokio::broadcast` par `UserId`) : un abonné ne reçoit que ses propres événements (isolation SC-008). Handler `/api/ws` : `CurrentUser` extrait avant l'upgrade → session absente = **401** avant toute négociation ; `on_upgrade` pompe le canal vers la socket (absorbe `Lagged`, se ferme sur `Close`). DTO ts-rs `ServerEvent` = union discriminée `event` (`job.updated`/`job.finished`/`model.converted`). Relais de progression : la file (T063) implémente un nouveau port `JobEventSink` (queue) — `JobContext::report` publie `job.updated` (running) et la fin publie `job.finished`/`job.updated` (failed/cancelled) au propriétaire ; `Queue::with_event_sink(hub)` branche le bus. `model.converted` publié directement par les handlers (câblage conversion STEP effective = T066/FFI). 3 tests : isolation du bus (event à A jamais vu par B), progression d'un job relayée au seul propriétaire (0.5 running puis failed), garde d'auth (401 sans session). Réexport frontend `ServerEvent`. (Fmt : réintégré le formatage de `presets.rs`/`slice.rs`/`http_slice.rs`, laissés non formatés par les éditions `sed` tardives de T064.)*
- [X] T066 [US1] Post-traitement job : G-code stocké (files adapter), stats extraites (GcodePreview), vignettes ; `GET /api/gcodes/{id}/download` + `/stats` ; tests — *module pur `backend/src/gcode.rs` : `parse_stats` lit les commentaires normalisés Orca (miroir `libslic3r/GCode.cpp` : `estimated printing time (normal mode)`, `total filament used [g]`/`cost`/`change`, `total layers count`, `filament used [mm|cm3|g]`/`cost` par extrudeur) → `GcodeStats` (temps en secondes via `parse_dhms` d/h/m/s) ; `extract_thumbnails` décode les blocs `; thumbnail begin WxH LEN … end` (miroir `GCode/Thumbnails.hpp`) → PNG base64. `store_gcode` orchestre le post-traitement : écrit le fichier via `FileStore::write_gcode` (clé de stockage propre, `file_path` fait foi) puis crée la ligne `gcodes` avec `stats`/`thumbnails` figés (`preview_path` binaire par couche = T067). Endpoints `backend/src/http/routes/gcodes.rs` : `download` (pièce jointe `text/plain`, `Content-Disposition`) et `stats` (JSON figé), tous deux 404 inter-comptes (SC-008). 11 tests : 6 unités du parseur (totaux+temps, listes par extrudeur, `parse_dhms`, vignette, tolérance G-code nu, JSON sans champ absent), 5 intégration (download pièce jointe, stats parsés, vignette persistée, 404 inter-comptes download+stats, id malformé → 404). L'invocation depuis un slicing réel dépend du runner FFI (T066+), `store_gcode` en est le point d'entrée.*
- [X] T067 [US5] Buffers préviz `backend/src/http/routes/preview.rs` : `/preview/meta` (couches, types présents, échelles) + `/preview/layers?from&to` (binaire R6) ; tests : format, plages, tailles — *parseur pur `gcode::parse_preview` reconstruit le modèle couches → segments typés depuis les commentaires stables Orca (`;TYPE:`/`; FEATURE:`, `;LAYER_CHANGE`/`; CHANGE_LAYER`, `;HEIGHT:`/`; LAYER_HEIGHT:`, `;WIDTH:`/`; LINE_WIDTH:`, `;Z:`) et les déplacements G0/G1 (E croissant = extrusion ; modes absolu/relatif M82/M83, G92, changement d'outil T0–T9) ; table rôle→id u8 miroir `ExtrusionEntity::role_to_string` (19 rôles + Travel). `PreviewModel::summary` → plages (feedrate/largeur/hauteur), types & extrudeurs présents, z/segments par couche ; `encode_range(from,to)` → buffer binaire `WSPv` (magic|version|from|to|count + N×40 o : 6×f32 pos, F/w/h f32, kind u8, extruder u8, layer u16), bornes clampées. Endpoints `routes/preview.rs` : `/preview/meta` (DTO ts-rs `PreviewMeta`/`PreviewLayerMeta`/`PreviewSegmentType`) et `/preview/layers?from&to` (octet-stream), parse à la volée depuis `file_path`, 404 inter-comptes (SC-008). 7 tests unités (couches/segments typés, couches de pur déplacement écartées, extrusion relative, résumé plages/types, en-tête & tailles d'enregistrement, clamp des bornes + modèle vide, round-trip noms de rôles) + 4 intégration (méta couches/types/échelles, en-tête `WSPv` + 3 enregistrements, plage par défaut = toutes couches, 404 inter-comptes meta+layers). Fidélité fine (arcs G2/G3, wipe, couture, débit exact) = parseur moteur FFI. `preview_path` (cache binaire précalculé) reste optionnel : la préviz est parsée depuis le fichier G-code.*
- [X] T068 [US5] Rendu préviz `frontend/src/lib/preview/` : BufferGeometry par plage, 7 colorations (type/vitesse/hauteur/largeur/débit/température/filament) avec légende/échelle, visibilité par type ; conforme references/orca-preview.png — *`decode.ts` : décode les buffers binaires `WSPv` (miroir exact de `PreviewModel::encode_range`, en-tête 18 o + enregistrements 40 o, little-endian) en tableaux parallèles ; rejette magic/tailles invalides. `colorations.ts` : palettes **fidèles OrcaSlicer** (`libvgcode` `DEFAULT_EXTRUSION_ROLES_COLORS` mappé sur les 19 rôles + Travel, `DEFAULT_RANGES_COLORS` 11 arrêts bleu→rouge, palette filament) + `sampleRange`/`rangeColor` ; types de légende (liste/échelle). `geometry.ts` (pur) : `buildPreviewGeometry` → positions+couleurs par sommet (2/segment) selon la coloration active, **filtrage par visibilité de type** ; les 7 colorations FR-041 — type (rôle), vitesse (F), hauteur, largeur, débit (`flowValue` = section×vitesse, mm³/s), filament (par extrudeur), température (neutre : donnée par-segment absente, piste moteur) ; `buildLegend` produit liste (type/filament) ou échelle mm/s·mm·mm³/s. `PreviewLines.svelte` (présentational, Threlte) : `LineSegments` à couleurs de sommet, dispose la géométrie au changement. 19 tests bun (round-trip décodage + rejets, interpolation/bornes du dégradé, fidélité palette de rôles, positions/couleurs, masquage par type, mode vitesse/température, débit, légendes). Les curseurs de couches/progression intra-couche = T069.*
- [X] T069 [US5] Curseurs couches (min–max) + progression intra-couche + raccourcis groupe Preview `frontend/src/lib/preview/sliders.ts` ; tests vitest des plages d'index — *`sliders.ts` (pur) : curseur vertical `LayerRange` (deux poignées low/high, poignée active, mode une-couche `L`) — `moveThumb` (bornage [0,total-1], `low`≤`high`, déplacement conjoint en mode une-couche), `toggleOneLayerMode`, `visibleRange`→`{from,to}` pour `?from&to`. Curseur horizontal `MoveCursor` (progression intra-couche = nombre de segments affichés de la couche du haut) — `moveCursorBy`/`Start`/`End`/`retargetCursor`. Raccourcis `PREVIEW_SHORTCUTS` (13, Annexe B « Preview » verbatim) + `resolvePreviewKey` (flèches→thumb/cursor avec flag rapide, `L`/`C`/`Tab`/`Home`/`End`) + réducteur `applyPreviewKey` (pas rapide ×5 Shift/Ctrl, bascule fenêtre G-code, `switch-tab` externe). **Groupe « Preview » ajouté à `traceability-map.json`** (13 clés → `sliders.ts`) : la parité l'enforce désormais (P4 vert). 15 tests bun des plages d'index (poignées & bornage, mode une-couche, curseur intra-couche Home/End, résolution des touches, pas rapide, verbatim des 13 raccourcis).*
- [X] T070 [P] [US5] Panneau stats `frontend/src/lib/preview/StatsPanel.svelte` : durées par type (%, utilisation), estimation totale, filament (longueur/volume/masse/coût), changements d'outil — parité orca-preview.png — *`stats.ts` (pur) : `buildPreviewStats(GcodeStats, segments?)` assemble le modèle de vue — totaux figés prioritaires (temps estimé, masse/coût, changements d'outil, couches ; FR-043) et **répartition par type de ligne dérivée des segments décodés** (`perTypeStats` : longueur euclidienne + temps = longueur/(vitesse/60) par rôle, triés par temps décroissant, part de temps/longueur en %) ; `buildFilaments` zippe les tableaux par extrudeur (longueur/volume/masse/coût) ; `formatDuration` façon Orca (d/h/m/s, composantes nulles omises). `GcodeStats` (interface miroir du backend). `StatsPanel.svelte` (présentational) : estimation globale (temps/couches/changements d'outil), table par type (pastille couleur du rôle + temps + %), table filament par extrudeur avec ligne de totaux (Σ masse/coût). 10 tests bun (formatage durées, agrégat par type, priorité des totaux figés vs calculés, zip filament, absence de segments/stats).*
- [X] T071 [P] [US7] Page file d'attente `frontend/src/routes/queue/+page.svelte` : états, progression, annulation, historique, notifications job.finished — *A d'abord câblé l'**API jobs** manquante (prérequis) : `routes/jobs.rs` — `GET /api/jobs` (liste+historique, plus récent d'abord, FR-031), `GET /api/jobs/{id}`, `POST /api/jobs/{id}/cancel` (annulation `queued|running→cancelled`, idempotente si déjà annulée, 409 si terminée), 404 inter-comptes (SC-008) ; `JobResponse` enrichi (`gcode_id`/`error`/`created_at`/`updated_at`) ; 5 tests de contrat. Frontend : `lib/api/jobs.ts` (client typé), `lib/queue/queue.ts` (pur : `applyEvent` réducteur d'événements WS, `partitionJobs` active/historique, `statusMeta`/`progressPercent`), `lib/queue/events.ts` (enveloppe `WebSocket` `/api/ws` reconnectante + `decodeEvent`), page `routes/queue/+page.svelte` (+`+page.ts` garde de session) : file active (badges d'état, barres de progression, bouton annuler), historique (table + lien de téléchargement G-code), notification éphémère sur `job.finished` (US7-AS1), abonnement WS temps réel au montage. 11 tests bun (réducteur `job.updated`/`job.finished`/inconnu/`model.converted`, partition, badges, progression bornée, décodage d'événements).*
- [X] T072 [US1] Export projet 3MF `GET /api/projects/{id}/export/3mf` (via engine::threemf, compatible Orca) + bouton UI ; test aller-retour desktop — *writer **pur Rust** `engine::threemf` (nouveau module, distinct du chemin FFI) produisant l'archive OPC standard qu'OrcaSlicer/PrusaSlicer lisent (miroir `libslic3r/Format/3mf.cpp`) : `[Content_Types].xml`, `_rels/.rels`, `3D/3dmodel.model` (géométrie 3MF core, un `<object>` par objet, volumes fusionnés dans le repère monde via leur matrice), `Metadata/Slic3r_PE.config` (config figée en lignes `; key = value`, `compatible_printers` exclu comme Orca) ; `read_project_bytes` pour la validation croisée. Endpoint `routes/export.rs` : assemble les maillages STL du projet (décodés serveur via `mesh::parse_stl`) + la config des presets actifs résolus (`slice::resolve_active_config` extrait, typé `DynamicPrintConfig`), pièce jointe `<nom>.3mf` (nom assaini), 404 inter-comptes (SC-008). Bouton « Exporter 3MF » sur la page projet. 5 tests (3 engine : entrées OPC standard, round-trip géométrie+config, matrice de volume appliquée ; 2 backend : archive valide géométrie+entrées, 404 inter-comptes). Le round-trip **desktop** réel (réouverture dans Orca) et la fidélité fine (config par objet, peinture, plateaux, formats non-STL) dépendent du writer libslic3r (`adapters::ffi::write_project_3mf`, T066+).*
- [X] T073 [US7] Tests de charge `backend/tests/queue_concurrency.rs` : 10 jobs / 2 comptes simultanés (SC-006) ; mesure du flux complet SC-005 documentée dans quickstart — *`queue_concurrency.rs` : 10 jobs répartis sur 5 comptes, pool de 4 workers, tous menés à `succeeded` **sans mélange de résultats** — chaque job porte un G-code lié au bon job (`gcode.job_id == job.id`), 10 G-codes distincts, et cloisonnement inter-comptes vérifié (le G-code d'un compte est introuvable pour un autre, SC-008). Le runner simulé matérialise un G-code par job/compte pour rendre le mélange détectable. Section « Flux complet (SC-005) & charge de la file (SC-006) » ajoutée à `quickstart.md` : protocole de mesure de bout en bout (import→tranchage<90s→préviz→export<3min) avec instrumentation par les transitions d'état de job (WS `job.finished`, `created_at→updated_at`), et documentation de la couverture SC-006 + tenue de l'interface (découplage file↔HTTP). La mesure de bout en bout dépend du runner FFI réel (T066).*

**Checkpoint**: gate P5 — flux cœur complet démontrable (US1 de bout en bout)

---

## Phase 8: Livraison P6 — Intégration Moonraker (US8)

**Goal**: déclaration d'imprimantes Klipper, envoi, suivi, contrôle (gate : US8 vert contre instance simulée en CI)

**Independent Test**: contre `virtual-klipper-printer` : déclarer, tester, envoyer avec démarrage, pause/reprise/annulation, états en direct

- [X] T074 [US8] Client Moonraker `backend/src/adapters/moonraker/` : /server/info, upload multipart (print=true), WS JSON-RPC subscribe (print_stats, heaters), pause/resume/cancel, X-Api-Key (R10) ; tests contre serveur mock in-process
- [X] T075 [US8] Endpoints imprimantes `backend/src/http/routes/printers.rs` : CRUD (api_key chiffrée au repos), test, upload (gcode_id, start_now), status, contrôles ; tests de contrat + échec réseau propre (FR-062)
- [X] T076 [US8] Relay WS printer.status : subscription Moonraker → événements clients ; tests d'isolation (imprimante de A invisible pour B)
- [X] T077 [P] [US8] Frontend imprimantes `frontend/src/routes/printers/+page.svelte` : déclaration/test, envoi depuis la préviz/file (start_now), panneau état (progression, températures), pause/reprise/annulation
- [X] T078 [US8] Test d'intégration CI `backend/tests/moonraker_integration.rs` contre le mock complet (upload 50 Mo simulé, suivi, SC-009)

**Checkpoint**: gate P6 — chaîne complète modèle → impression démontrable

---

## Phase 9: Polish & transverse

**Purpose**: parité finale vérifiée, i18n, aide, performance, validation utilisateur

- [X] T079 [P] Menus principaux restants (Fichier/Édition/Vue/Aide, Annexe B §B.2) + dialogue d'aide raccourcis (92 entrées, adaptations navigateur consignées dans exclusions.md) dans frontend/src/lib/menus/ — *`menus.ts` (MAIN_MENUS Fichier/Édition/Vue/Aide + sous-menus Import/Export, libellés verbatim), `shortcuts.ts` (92 raccourcis des 5 groupes), `ShortcutsDialog.svelte` (aide modale), `menus.test.ts` (9 tests). Traçabilité : `main_menu` (55 libellés impl) + groupe `Global shortcuts` ajoutés à traceability-map.json ; 25 libellés + Ctrl+M/Ctrl+Shift+M/Ctrl+Tab consignés dans exclusions.md. `check_traceability.py` étendu (main_menus §B.2 enforced ; test négatif vérifié).*
- [X] T080 [P] i18n additive fr/en `frontend/src/lib/i18n/` (libellés de parité anglais = clés, FR-072) — *`index.ts` : store `locale`, `translate(loc,key)` à repli additif sur la clé anglaise, helper réactif `$t`, `setLocale`/`initLocale` (mémorisation + langue navigateur). `fr.ts` : traductions des libellés de menus/vues/raccourcis (parité anglaise = clés). `ShortcutsDialog` et `+layout` câblés (`$t`, `initLocale`). `i18n.test.ts` (7 tests : identité anglaise, repli additif, couverture fr complète des menus/groupes).*
- [ ] T081 Résolution des entrées « à trancher à l'implémentation » d'exclusions.md (classement final de chaque clé restante) — check_traceability sans catégorie provisoire
- [ ] T082 Passe finale de traçabilité : `python3 audit/run_all.py && python3 audit/check_traceability.py` vert complet (SC-001) ; annexes resynchronisées ; registre d'exclusions revu
- [ ] T083 [P] Campagne d'isolation automatisée `backend/tests/isolation_campaign.rs` : accès direct à toutes les ressources d'autrui (SC-008 à 100 %)
- [ ] T084 [P] Performance : profil préviz/scène (SC-007), budget mémoire buffers, lazy-loading des couches ; résultats documentés dans quickstart.md
- [ ] T085 [P] Campagne SC-004 : protocole de test utilisateur (40 paramètres tirés au sort, cible 95 % trouvés au même endroit qu'OrcaSlicer) documenté et exécuté ; résultats dans specs/001-orcaslicer-web-parity/validation-sc004.md (analyse G4)
- [ ] T086 Documentation finale : README racine (installation nix, démarrage), engine/README.md (FFI/CLI), parcours quickstart.md rejoué intégralement

---

## Dependencies & Execution Order

```text
Phase 1 (Setup) → Phase 2 (Foundational)
Phase 2 → Phase 3 (P1 engine) → Phase 7 (P5 slicing)
Phase 2 → Phase 4 (P2 auth/storage) → Phases 5, 6, 7, 8
Phase 5 (P3 presets/onglets) → Phase 7 (presets résolus requis)
Phase 6 (P4 scène) → Phase 7 (scène à trancher)
Phase 7 → Phase 8 (P6 Moonraker : envoie les G-codes produits)
Phases 5 ∥ 6 (parallélisables après Phase 4 ; Phase 3 indépendante d'elles)
Phase 9 après Phase 8
```

Dépendances intra-phase notables : T011 (suite générique) avant T012–T020 ;
T024 (traits + suite de contrat) avant T025–T031 ; T040 (codegen UI) avant
T041–T047 ; T052/T054 avant T055–T058 ; T063 (file) avant T064–T066.

## Parallel Execution Examples

- **Phase 3** : T013 ∥ T014 ∥ T015 ∥ T016 (wrappers FFI distincts) après T012 ; T021 ∥ T020.
- **Après Phase 4** : une piste « P3 » (T035–T047) et une piste « P4 » (T048–T062) en parallèle.
- **Phase 6** : T056 ∥ T057 ∥ T058 (gizmos indépendants) après T052/T054.
- **Phase 7** : T068 ∥ T070 ∥ T071 côté frontend pendant T063–T067 côté backend.
- **Phase 9** : T079 ∥ T080 ∥ T083 ∥ T084 ∥ T085.

## Implementation Strategy

- **MVP** : Phases 1–4 + chemin minimal de la Phase 7 (T063–T066 + bouton
  slice basique) = US1 démontrable (import → slice presets par défaut →
  export). Les onglets complets (Phase 5) et la scène complète (Phase 6)
  enrichissent ensuite sans bloquer la démo.
- **Livraison incrémentale** : chaque phase se termine par son gate
  (quickstart §0) et une démo ; commit atomique par tâche, tests inclus
  (constitution IV) ; check_traceability s'étend phase par phase (T006 →
  T047 → T062 → T082) pour que la parité reste mesurée en continu.
- **Risque en tête** : la Phase 3 (bridge FFI) était le chemin critique ;
  elle est désormais franchie (chargement, 3MF, STEP, réparation, arrangement,
  tranchage réel + préviz G-code, suite générique verte sur `FfiEngine`).
